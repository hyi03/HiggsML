# Sprint M4-01 document review by gpt-5.5

Review target:

- `project/3-Plan/sprint-m4-01.md`
- `project/1-Requirement/FR-401-m4l-off-feature-attribution.md`

Reference authority:

- `project/m4l-off-feature-attribution-plan.md`

Evidence level:

- Static document review and source inspection only.
- No tests, no training, no inference, no MC validation, and no scientific runs were executed.
- I used the codebase knowledge graph for source discovery, then direct reads for the reviewed Markdown, scripts, schema, and exact source lines.

## Findings

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Requirement | M4DOC-G55-001: `project/3-Plan/sprint-m4-01.md:10`, `project/3-Plan/sprint-m4-01.md:20`, `project/1-Requirement/FR-401-m4l-off-feature-attribution.md:21` | The sprint/FR compress the `h4l_evaluate` migration into generic orchestration and final-report wording, but the approved plan requires a specific behavior change: a versioned off-only evaluation plan, default matrix without stress, optional separately registered stress, and safer `--plan-only` manifest checks. This is easy to miss because the current implementation and schema still make stress mandatory and emit all stress stages. | The source plan says `scripts/h4l_evaluate.py` currently has required stress and deferred plan-only identity checks, and must support a new version with optional stress and safe manifest preflight (`project/m4l-off-feature-attribution-plan.md:116`, `project/m4l-off-feature-attribution-plan.md:173`-`175`). Current schema requires `"stress"` (`config/schemas/h4l_evaluation_plan_v1.schema.json:5`) and constrains it to the old 16-scenario matrix (`config/schemas/h4l_evaluation_plan_v1.schema.json:35`-`42`). Current script validates stress budget (`scripts/h4l_evaluate.py:73`-`74`) and unconditionally expands stress stages from the plan (`scripts/h4l_evaluate.py:138`-`149`). | Add an explicit M4-01 acceptance/test bullet for the evaluation-plan migration: new off-only plan schema/version, required family/candidate bindings, optional stress with no default old `M3:42` stress expansion, plan-only safe manifest identity reporting as `unresolved` rather than validated, and legacy v1 compatibility tests. |
| High | Correctness | M4DOC-G55-002: `project/3-Plan/sprint-m4-01.md:17`, `project/1-Requirement/FR-401-m4l-off-feature-attribution.md:18`, `project/1-Requirement/FR-401-m4l-off-feature-attribution.md:21` | The documents name a connected M0off/common-grid/G1/freeze chain, but they do not enumerate the legacy hard-coded candidate assumptions that must be removed or parameterized. The approved plan specifically calls out old M4/M5 gates, M0c baseline behavior, and stress reference dependence; current source has all three patterns. Without making these migration targets explicit in the sprint, an implementation can satisfy the visible checklist while leaving a legacy candidate matrix underneath. | The source plan requires family-specific registered candidates and forbids using M0/M0c as the off-family empty set (`project/m4l-off-feature-attribution-plan.md:136`-`141`). Current workflow still requires `{'M0c:42','M2:42','M3:42','M4:42','M5:42'}` for G1 (`src/higgsml/workflow.py:517`-`521`) and requires the frozen stress reference candidate during freeze (`src/higgsml/workflow.py:551`-`554`). Current reporting builds feature-combination attribution with `M0c:<seed>` as the empty subset (`src/higgsml/inference/reporting.py:88`-`126`). Current MC bootstrap requires paired M4/M5 physical-CDF bundles (`src/higgsml/inference/bootstrap.py:38`-`56`). | Add a short implementation checklist under Sprint section 5 or 6 naming the legacy assumptions to retire for the off-only family: `expected_candidates`/freeze completeness, G1 minimum matrix, frozen stress reference, M0c empty-set reporting, M4/M5 bootstrap, and M5-vs-M4 paired coverage. Require tests that fail if the off-only family still depends on those legacy keys. |
| Medium | Clarity | M4DOC-G55-003: `project/3-Plan/sprint-m4-01.md:28`, `project/3-Plan/sprint-m4-01.md:37`, `project/1-Requirement/FR-401-m4l-off-feature-attribution.md:42` | Verification command selection is still under-specified in the sprint/FR. The baseline evidence now verifies the Python executable and worktree import location, but the sprint still says the local executable was corrected to plain `conda run`. On Windows, plain `conda` depends on PATH and can run a different installation than the audited `pytorch` environment unless the resolved Conda executable is recorded. | AGENTS requires explicit Conda commands from the repository root using `D:\apps\anaconda3\Scripts\conda.exe`; the sprint records that this path is absent and that local Python is under `C:/Users/whchen/anaconda3/envs/pytorch/python.exe` (`project/3-Plan/sprint-m4-01.md:28`, `project/3-Plan/sprint-m4-01.md:37`). The baseline confirms Python/import location but still records the command as `conda run -n pytorch` rather than an absolute Conda executable (`docs/4-Reviews/sprint-m4-01-baseline.md`). The FR repeats "local executable corrected to conda run -n pytorch" without an absolute executable path (`project/1-Requirement/FR-401-m4l-off-feature-attribution.md:42`). | Record the exact resolved Conda executable path to use for this worktree, for example the `C:\Users\whchen\anaconda3\Scripts\conda.exe` path if that is the matching installation, and keep the existing baseline/final logs that verify `sys.executable` and `higgsml.__file__`. |
| Low | Documentation | M4DOC-G55-004: `project/3-Plan/sprint-m4-01.md:28`, `project/1-Requirement/FR-401-m4l-off-feature-attribution.md:42` | The sprint says baseline and final output are persisted under `docs/4-Reviews`, while the FR also defines that path as the review directory. The newly supplied baseline/audit files show this is not just hypothetical: review reports, baseline logs, source manifests, and reuse audits now share the same root. It is not a scientific correctness issue, but it makes later review-confirm/code-review phases more error-prone because reviewers can accidentally treat generated evidence as review reports, or vice versa. | The sprint assigns baseline/final output to `docs/4-Reviews` (`project/3-Plan/sprint-m4-01.md:28`). The FR identifies `REVIEW_DIR=docs/4-Reviews` and `REVIEW_DONE_DIR=docs/4-Reviews/Done` (`project/1-Requirement/FR-401-m4l-off-feature-attribution.md:42`). The baseline and reuse audit are stored as `docs/4-Reviews/sprint-m4-01-baseline.md`, `docs/4-Reviews/sprint-m4-01-baseline-tests.txt`, and `docs/4-Reviews/sprint-m4-01-reuse-audit.json`. The approved plan requires new run artifacts under fresh run roots and preserves old runs, while review output is a separate workflow artifact (`project/m4l-off-feature-attribution-plan.md:12`, `project/m4l-off-feature-attribution-plan.md:178`). | Clarify that `docs/4-Reviews` is for review reports and compact review evidence summaries. Put implementation baseline/final command logs, reuse audits, and source manifests in a named evidence subdirectory or an explicit manifest path, and link them from the review-confirm table instead of mixing all artifact classes at the review root. |

## Evidence Reviewed

- Repository instructions: `AGENTS.md`.
- Project scope/status documents: `README.md`, `docs/README.md`, `docs/research-design.md`, `docs/results-and-limitations.md`, `docs/implementation-and-reproduction.md`, `config/protocols/h4l_protocol.json`.
- Reviewed documents: `project/m4l-off-feature-attribution-plan.md`, `project/1-Requirement/FR-401-m4l-off-feature-attribution.md`, `project/3-Plan/sprint-m4-01.md`.
- Orchestration evidence supplied during review: `docs/4-Reviews/sprint-m4-01-baseline.md`, `docs/4-Reviews/sprint-m4-01-baseline-tests.txt`, `docs/4-Reviews/sprint-m4-01-reuse-audit.json`, `docs/4-Reviews/sprint-m4-01-source-manifest-inventory.json`.
- Source evidence: `src/higgsml/workflow.py`, `src/higgsml/inference/bootstrap.py`, `src/higgsml/inference/assessment.py`, `src/higgsml/inference/reporting.py`, `scripts/h4l_evaluate.py`, `config/schemas/h4l_evaluation_plan_v1.schema.json`, `config/examples/h4l_evaluation_plan.json`.

## Review Result

Finding count: 4 actionable findings.

The FR and sprint are aligned with the approved plan at the goal level. The main risk is that they rely on "implement the full plan" for several source-critical migrations where the current code has explicit legacy M4/M5/M0c/stress assumptions. Tightening those acceptance bullets before implementation would reduce the chance of a mathematically correct but workflow-disconnected Stage B/C/E delivery.
