# Sprint M4-01 Code Review by GPT-5.5

Review type: static code review of the current working-tree changes for `project/3-Plan/sprint-m4-01.md`, `project/1-Requirement/FR-401-m4l-off-feature-attribution.md`, `project/m4l-off-feature-attribution-plan.md`, and the requested implementation/config/test files.

Scope inspected: `git diff`, untracked attribution implementation files, off-only schemas/examples, the new CLI/orchestration path, changed inference/reporting/template/discriminant code, targeted tests, applicable repository instructions, and the supplemental Stage A/B replay note in `docs/4-Reviews/sprint-m4-01-mc-stage-b.md`. I did not run tests or scientific workloads. The findings below are static-review findings; the supplemental A/B replay is treated only as observed Stage B evidence and not as coverage or assessment validation.

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Correctness | M4CODE-G55-001: `src/higgsml/inference/attribution_workflow.py:387-398`, `src/higgsml/inference/assessment.py:128`, `src/higgsml/inference/likelihood.py:190-196` | The registered `model-self` Toy path cannot complete when the joint template-parent construction succeeds. `evaluate()` sets `role="template"` for `stage=="model-self"` and calls `infer_assessment(..., parent_role="template")`. `_joint_mother()` now accepts the template parent, but `infer_assessment()` passes the resulting template-role frame directly into `paired_event_toys()`, whose first guard still requires `set(frame.role) == {"assessment"}`. The fallback branch only runs when `_joint_mother()` raises; it does not catch this later role-contract failure. | The plan requires D model-self Toys at mu=0/1/2 and shared physical joint construction when legal. The code path is: `role='template' if stage=='model-self'` and `infer_assessment(... parent_role=role)` in `attribution_workflow.py`; `infer_assessment()` calls `paired_event_toys(work, ...)`; `paired_event_toys()` rejects any non-assessment role before drawing Toys. The supplemental Stage A/B replay reports C/D/E as `not_run`, so this path has not been exercised by the observed controlled-MC replay. | Make the paired-toy generator support the already-validated template parent for model-self closure, or normalize the validated parent frame to the expected internal role before calling it. Add an integration test that runs `evaluate(... stage="model-self", mu=1)` through the joint path and verifies the result is not forced into the marginal fallback or a role error. |
| Medium | Binding / Risk | M4CODE-G55-002: `src/higgsml/inference/attribution_workflow.py:443-454`, `scripts/h4l_evaluate.py:226-230` | Final off-only report assembly accepts evaluation runs based on upstream IDs and stage name, but does not re-check the evaluation manifest context against the registered family, mu cell, or fixed `BUDGETS`. This is weaker than the `--reuse-stage` preflight, which explicitly checks upstream IDs, `context.mu`, and `context.budgets`. A stale or malformed completed `attribution-*` run with matching upstreams could be folded into `report.json` without the budget/family binding that the plan requires for recovery and layered evidence. | In `report()`, each evaluation path is loaded with no stage tuple, the stage is read from the manifest, duplicate `(stage, mu)` cells are rejected, and `evaluation.json` is inserted into the evidence layer. There is no check for `item.manifest["context"]["family_id"] == FAMILY`, `item.manifest["context"]["budgets"] == BUDGETS`, or valid mu/stage compatibility. By contrast, `scripts/h4l_evaluate.py` validates reused stage context before accepting a skip. | Mirror the `--reuse-stage` binding checks in `workflow.report()`: require the expected stage set, `family_id`, fixed budget digest/content, legal mu for the stage, and the registered upstream IDs before importing an evaluation layer. Add a negative test that a completed evaluation artifact with missing or changed context is rejected by `attribution report`. |

Evidence reviewed but not treated as validation:

- Static source/config/test review only; no pytest, pip check, or MC workload was run by this reviewer.
- `docs/4-Reviews/sprint-m4-01-mc-stage-b.md` reports Stage A/B controlled-MC replay through `report-B` completed, with no assessment payload decoded. It also records C event-MC bootstrap, D Toys, E T2, and native ARM64 as `not_run`.

Finding count: 2
