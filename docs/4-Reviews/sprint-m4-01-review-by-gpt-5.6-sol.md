# Independent document review: Sprint M4-01 and FR-401

## Review outcome

The documents preserve the approved plan's overall A–E sequence, off-only scope, 80 registered identities, immutable-source rule, evidence layering, and core counts. Four actionable issues remain: one direct contradiction in the FR's failure semantics, two underspecified statistical execution contracts, and one stale sprint-status record.

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Correctness | `project/1-Requirement/FR-401-m4l-off-feature-attribution.md:29-30` | **M4DOC-001 — The global rule that failed replicas “suppress formal intervals” incorrectly covers Toy/coverage stages.** Read literally, a single failed Toy fit would suppress the coverage intervals that Stage D is required to publish. | The approved plan applies all-or-nothing percentile-interval suppression specifically to the 200-replica event bootstrap (`project/m4l-off-feature-attribution-plan.md:96-97`). For Stage D it instead requires successful-fit conditional coverage, the planned-denominator success-and-coverage fraction with binomial intervals, and failure rate (`:154-159`). Current static code reflects the planned-denominator distinction: `src/higgsml/inference/reporting.py:28-37` retains a Wilson interval for success-and-coverage even when `coverage` is incomplete. | Scope the suppression sentence to Stage C event-bootstrap percentile intervals. Add separate Stage D wording requiring both coverage views, binomial intervals, and failure denominators; state the Stage E failure policy independently rather than grouping every failure under “replicas.” |
| Medium | Requirement | `project/1-Requirement/FR-401-m4l-off-feature-attribution.md:20-21`; `project/3-Plan/sprint-m4-01.md:19-20` | **M4DOC-002 — The Stage B Asimov and Stage E T2 summaries do not freeze the registered injection at `mu=1`.** Both documents enumerate the three Stage D injection points nearby, so the omitted T2/Asimov injection leaves an avoidable ambiguity about whether the three-point matrix also applies to those stages. | The approved plan fixes the Stage B model-self Asimov at `mu=1` (`project/m4l-off-feature-attribution-plan.md:140-142`) and fixes Stage E T2 to T1 inner inference at `mu=1` (`:162-169`). The current evaluation-plan example independently records `t2.mu=1` (`config/examples/h4l_evaluation_plan.json:14`). | Add `T1 model-self Asimov at mu=1` to Stage B and `T1, mu=1` to Stage E in both the FR and sprint checklist. Keep `mu=0,1,2` confined to Stage D. |
| Medium | Requirement | `project/3-Plan/sprint-m4-01.md:19-21`; `project/1-Requirement/FR-401-m4l-off-feature-attribution.md:20-21` | **M4DOC-003 — The Stage D/E summaries do not make the family-wide pairing unit executable.** The sprint says “valid joint sampling” and “preserved outer dependence,” while the FR only gives marginal budgets. Neither summary explicitly requires one shared physical pseudo-event construction for all candidates in Stage D, one shared outer calibration-group draw across all 80 identities in Stage E, and the registered joint inner-Toy rule. An implementation could therefore meet the listed counts while destroying the covariance needed for paired comparisons. | The approved plan requires shared physical pseudo-events or equivalent joint counts for assessment (`project/m4l-off-feature-attribution-plan.md:154-160`) and, for T2, shared outer calibration-group resampling across all registered identities plus registered joint pseudo-events for inner Toys (`:164-169`). Existing static services show the intended reuse points: `src/higgsml/inference/assessment.py:101-219` constructs one joint mother draw for candidates, and `run_assessment_t2` at `:222-306` fits all bundles from each common outer bootstrap before calling the joint assessment path. No runtime validation was performed in this review. | Expand the D/E acceptance bullets to name the shared unit: one joint physical draw per Toy index across eligible candidates; one outer calibration multiplicity vector shared across all 80 identities; one joint inner-Toy rule; pairing marked `unavailable` when a valid joint construction cannot be established. Add explicit artifact checks for these identities, not only count checks. |
| Low | Documentation | `project/3-Plan/sprint-m4-01.md:16,28,36-37` | **M4DOC-004 — The sprint status no longer records or links the completed baseline and reuse-audit outcomes.** It records only the environment-path correction and says later findings will be recorded, while the baseline now contains three pre-existing failures and the audit shows that payload contracts pass but complete reuse eligibility remains false. Leaving those facts out of the sprint makes the pre-implementation gate state easy to misread. | `docs/4-Reviews/sprint-m4-01-baseline.md:3-10` records 448 passed, 3 failed, 5 skipped, `pip check` clean, and the 75-row audit. `docs/4-Reviews/sprint-m4-01-reuse-audit.json` contains 75 `metadata_payload_contract_valid` rows but `historical_access=pending_metadata_history_review`, `independent_qualification=pending`, and `complete_reuse_eligibility=false`. These are software-test and static metadata-audit evidence only; no event or assessment payload and no actual MC validation were reviewed. | Link the baseline and reuse-audit artifacts from the sprint conclusion. Record the three baseline failures as pre-existing and state explicitly that 75/75 payload contracts passed while historical-access review, independent qualification, and complete reuse eligibility remain pending/false. Keep the implementation and actual-MC status `not_run`. |

## Evidence boundary

This review used the approved plan, FR, sprint document, repository guidance, maintained research/protocol documentation, codebase-graph snippets for the current workflow, reporting, bootstrap, and assessment services, and the newly recorded baseline/reuse-audit artifacts. The baseline test log is observed evidence produced outside this reviewer pass; this reviewer ran no tests and no scientific workflow. Source inspection and the 75-row metadata audit do not establish full-MC behavior, independent scientific qualification, assessment validity, or native ARM64 authority validation.

The report does not rely on or inspect any other reviewer's report.
