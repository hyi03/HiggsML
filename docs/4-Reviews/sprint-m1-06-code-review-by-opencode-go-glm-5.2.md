# Sprint M1-06 Code Review Report

**Review target**

- Git diff from `HEAD` (`85b67d1`) to the working tree: tracked changes in `.gitignore`, `neural/README.md`, `neural/docs/sprint-m1-06.md`.
- Untracked M1-06 implementation files:
  - `neural/docs/runbook.md`
  - `neural/docs/artifact-schema.md`
  - `neural/docs/m1-06-verification-evidence.md`
  - `neural/docs/final-technical-report.md`
- Document-review artifacts (read as context for the change set's baseline):
  - `docs/4-Reviews/sprint-m1-06-review-by-opencode-go-kimi-k2.7-code.md`
  - `docs/4-Reviews/sprint-m1-06-review-by-opencode-go-glm-5.2.md`
  - `docs/4-Reviews/sprint-m1-06-review-confirm.md`
  - `docs/4-Reviews/sprint-m1-06-code-review-by-opencode-go-kimi-k2.7-code.md`

**Review type:** Code review

**Reviewer:** opencode-go / GLM-5.2

**Availability note (required):** The configured GLM primary was unavailable, and this review was completed by fallback `deepseek/deepseek-v4-flash`.

**Date:** 2026-09-02

**Sources of truth:**

- `neural/docs/sprint-m1-06.md`
- `neural/docs/FR-001-adversarial-mlp-refactor.md`
- `neural/AGENTS.md`
- `neural/docs/preprocess-protocol-v1.md` and `neural/config/preprocess_protocol_v1.yaml`
- `neural/docs/development-protocol-v1.md`, `neural/docs/adversarial-mlp-protocol-v1.md`, `neural/docs/test-opening-protocol-v1.md` and `neural/config/adversarial_mlp_protocol_v1.yaml`
- Existing implementation under `neural/src/`, `neural/tests/`, `neural/config/`, `pyproject.toml`, root `.gitignore`, `neural/README.md`

**Scope and independence discipline:** Strictly MC-only. No real data was read, hashed, probed, preprocessed, scored, plotted, or published. No held-out test was opened or decoded. No `open-test` command was executed. No authority or full-data run was executed. No implementation or documentation file was modified; only this report file was written. Independent verification was performed only on Windows/local evidence that the change set itself records (synthetic test suites, CLI help, `pip check`, byte-freeze `git diff`, static audit grep, `git check-ignore`, `git diff --check`, platform/environment probe, local file hash).

---

## Executive Summary

The M1-06 change set is a documentation-and-hygiene implementation, not a source-code change: it adds the four scheduled deliverables (`runbook.md`, `artifact-schema.md`, `m1-06-verification-evidence.md`, `final-technical-report.md`), updates the README banner to the authority-blocked status and links the new documents, adds the precise `.gitignore` rule for `neural/config/preprocess_run.local.yaml`, inlines the bound ROOT/golden lineage hashes, adopts the orthogonal `method × platform × data_scope × authority` evidence taxonomy, records the Windows/AMD64 preflight blocker with the authority gates `blocked`/`not_run`, and fixes the previously accepted document-review findings (notably the GLM F01 gate-invocation gap: `run_authority_gate` now has an explicit, documented invocation and the pytest module is correctly scoped as contract-only).

Every local evidence claim that the change set records was independently reproduced in this session: `pip check` exit 0; both `--help` smokes exit 0; focused synthetic suite `23 passed`; full local suite `227 passed, 2 skipped` with byte-identical skip reasons (`authoritative_gate_not_run: external r3-ARM64 table is absent`; `directory symlinks are unavailable on this platform`); scientific byte freeze vs `85b67d1` exit 0; `git check-ignore` on the local run config matching rule `/neural/config/preprocess_run.local.yaml`; the forbidden-locator audit grep with no hits; `git diff --check` exit 0; platform probe Windows/AMD64 / Python 3.12.13 / PyTorch 2.7.1 / threads 8 / ambient deterministic false; and local `osx.yml` SHA-256 `f54522ac…` (a stable fingerprint because `.gitattributes` pins `eol=lf` and `core.autocrlf=false`). The bound hashes, pre-registered counts, split/development numbers, duplicate facts, schema names, and artifact layouts in the new documents all match the sealed protocol YAMLs and the implemented `src/` code paths read for this review.

No scientific-safety boundary violation and no evidence overclaim were identified. Remaining findings are concentrated on the operational path of the single authority gate that M1-06 must eventually run: `run_authority_gate` has no automated exercise, is reachable only via a manually pasted `python -c` one-liner, raises an unmapped `FileExistsError` on a duplicate evidence path, and performs no allowed-root containment or protocol-path injection; plus a set of low-severity documentation-precision and audit items. None of these relax scientific constraints, and none should be fixed inside a running authority workflow — any `src/` change must land first as a reviewed change against the frozen baseline. M1-06 stays blocked at `authority_environment_preflight` and must not be committed or marked complete until the locked native `osx-arm64` full-data gates pass.

Finding counts: **0 Critical, 0 High, 2 Medium, 7 Low, 5 Info.**

## Findings

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| Medium | Test | `neural/tests/golden/test_preprocess_authority.py` L10–14, L41–92; `neural/src/preprocessing/authority.py` L193–237; `neural/docs/runbook.md` L112; `neural/docs/sprint-m1-06.md` L191 | `run_authority_gate` orchestration (lineage verify → table compare → counts/duplicates → baseline-bound cutflow → `xb` evidence write) has zero automated coverage. The golden module pins the contract, hashes the external r3 table only when present, tests platform refusal, and unit-tests `compare_tables`; it never calls the gate. The gate is exercised only by the manually pasted `python -c` command that the runbook and sprint document, so a regression in the gate would surface first on the authority host, exactly where a failure is most expensive and least recoverable. | `rg run_authority_gate` across `neural/` matches only the definition in `authority.py` and Markdown references in the docs; the test file imports `AuthorityGateError`, `compare_tables`, `require_authority_platform` only. Sprint §5.2 L120–122 itself concedes the pytest module is insufficient for closure. | Add a reviewed synthetic (non-authority) test that monkeypatches `require_authority_platform` (and supplies the lineage/baseline inputs) to run `run_authority_gate` end-to-end against a micro preprocess run, asserting the evidence JSON content and the exclusive-create refusal behavior. A test-only addition does not require reopening the scientific `src/` review. If it is deferred, at minimum perform a rehearsed run of the exact documented command on the authority host before the full-data gate. |
| Medium | Risk | `neural/docs/runbook.md` L109–113, L115–117; `neural/docs/sprint-m1-06.md` L188–192; `neural/src/preprocessing/authority.py` L193–237 | The single authority closure gate depends on an unversioned, manually pasted one-liner with no stable error semantics. Because it is `python -c`, failures escape the CLI exit-code contract: e.g. a duplicate evidence path raises an uncaught `FileExistsError` from `destination.open("xb")` (authority.py L236), producing a Python traceback and an exit code 1 that is not in the declared table {0,2,3,4,5,70}; the evidence transcription rules ("record exit code") cannot represent that outcome, and a partial evidence file or rerun with the same path has no documented handling. | `git grep run_authority_gate` shows no script or module entry point; `authority.py` L236 `with destination.open("xb") as stream:` is unwrapped. | In a reviewed change, wrap exclusive-create failure in `AuthorityGateError`/exit 3 or `RunPathError`/exit 4, or expose a thin controlled runner under `neural/scripts/` (not a third console entry point, per the review-confirm decision) with locked cwd/path validation. Until then, add runbook text stating that a duplicate evidence path aborts the run and requires a fresh unique evidence path, and that any exit code outside the table is a blocker requiring manual audit. |
| Low | Risk | `neural/src/preprocessing/authority.py` L198–202, L234–237; `neural/docs/runbook.md` §4/§6 | `run_authority_gate` does not enforce (a) containment of `new_run_dir` and the evidence file under the allowed `neural/runs/` root, (b) that the evidence path did not exist before creation, and (c) that the run path is not a symlink/escape; it also hard-codes the protocol at `repository / "neural/config/preprocess_protocol_v1.yaml"` and resolves `repository_root` against the caller's cwd. Correctness currently relies entirely on operator discipline documented in the runbook. | `run = Path(new_run_dir).resolve()` (L199) with no `relative_to(allowed_root)` check; `protocol = load_preprocess_protocol(repository / "neural/config/preprocess_protocol_v1.yaml")` (L200–202); the documented invocation fixes `repository_root='..'`. | Add allowed-root containment checks for run and evidence paths plus a protocol-path parameter (defaulting to the sealed path) in a reviewed change, or strengthen runbook preflight step 5 to assert resolved paths under `neural/runs/` and confirm the evidence file is absent immediately before execution. |
| Low | Consistency | `neural/docs/runbook.md` L115–117; `neural/docs/sprint-m1-06.md` L106–109, L155–156; `neural/docs/artifact-schema.md` §6; `neural/src/preprocessing/authority.py` | The documented scope of the gate overstates what `run_authority_gate` verifies. Runbook L116–117 says the comparator validates "…canonical table、逐列 golden、cutflow 与 manifest" and §5.2/§6 mention "canonical hash … manifest". The gate never opens the new run's `manifest.json`, never recomputes the new table's canonical/gzip content hash, and never re-verifies the new manifest's binding of its outputs. It verifies the five lineage artifact hashes (including the baseline manifest), element-wise table equivalence vs the golden table, summary counts/duplicate facts, and the baseline-manifest-bound legacy cutflow. | Code reads only `processed/mc_events.csv.gz`, `artifacts/mc_summary.json`, `artifacts/cutflow.json` and the lineage artifacts; no read of the new run's `artifacts/manifest.json`. | Reword runbook/§6 to say the gate validates: approved lineage SHA-256s, 29-column table vs the r3 golden, counts/duplicates, and cutflow vs the baseline-manifest-bound cutflow; and state that the new run's own manifest binding is established by the preprocess run's manifest-last transaction and is not re-verified by the gate. This prevents the future evidence record from implying manifest validation that did not occur. |
| Low | Clarity | `neural/docs/sprint-m1-06.md` §6 L155; `neural/docs/runbook.md` L116; `neural/config/preprocess_protocol_v1.yaml` `output_columns`; `neural/src/preprocessing/authority.py` L63–103 | §6's acceptance bullet says the gate must pass "…cutflow、19 features、provenance、hash 和 manifest", which understates the compared surface: the gate compares all 29 canonical columns (the 19 model-candidate features plus `m4l`, `label`, `split`, `physical_weight`, `train_weight`, `source_sample`, `source_entry`, and the three run/event/channel integer columns) with exact structural/enum/integer predicates. A reviewer could under-scope what the gate passing proves. | `compare_tables` iterates `protocol.output_columns` (29 names); runbook correctly says 29 columns, §6 says 19. | Align §6 wording with the runbook/protocol: "29 canonical columns (including the 19 model-candidate features), per-column golden, counts/duplicates, cutflow". |
| Low | Risk | `neural/docs/sprint-m1-06.md` L208–211; `neural/docs/runbook.md` L73–75; evidence record at `m1-06-verification-evidence.md` L30 | The byte-freeze check `git diff --exit-code 85b67d1 -- src config/preprocess_protocol_v1.yaml config/adversarial_mlp_protocol_v1.yaml` is blind to untracked additions under the frozen paths and excludes the CLI modules and `tests/golden/test_preprocess_authority.py`. A new file added under `src/`, or a change to the comparator contract or CLI behavior that affects gate/evidence format, would not trip the freeze gate. | `git diff` does not consider untracked files; the command covers `src` and only the two sealed YAMLs. | Before authority runs, add a companion `git status --porcelain` audit asserting no untracked files under the frozen paths, and either extend the freeze list to `src/cli/preprocess.py`, `src/cli/train.py`, `tests/golden/test_preprocess_authority.py` or state that any change to CLI/golden-test evidence format is a review trigger. |
| Low | Clarity | `neural/docs/sprint-m1-06.md` §9 L234; `m1-06-verification-evidence.md` L31 | The fixed static-audit regex `xgboost[\\/]+src` does not match Python dotted imports such as `import xgboost.src` or `from xgboost.src import …`, so the audit as written cannot prove absence of runtime dotted access to `xgboost/src`; the accompanying real-data tokens (`full-baseline-2026-08-10`, `700600`) are a spot list, not an exhaustive rule. | The regex only matches slash/backslash separators; the executed audit produced "no match" but would also report "no match" for a dotted-import violation. | Extend the audit to `xgboost[.\\/]+src` plus a companion pattern for `(from|import) xgboost`, and state that approved MC lineage references are allowed only at the protocol's exact locators so the pass/fail criterion is deterministic. |
| Low | Consistency | `neural/docs/runbook.md` §4 step 4 (L79–80); `preprocess-protocol-v1.md` §7.1 | The preflight instruction "确认协议 §7.1 的四项 legacy lineage artifact、baseline manifest 与 r3-ARM64 golden table 位于其固定 locator" is ambiguous: §7.1's table already contains the authoritative (r3) table as one of its four rows and binds the baseline manifest separately, so the enumeration can be read as double-counting the golden table and separating the baseline manifest in a way §7.1 does not. | §7.1 lists Identity manifest, Identity table, Enrichment manifest, 权威逐列表 (r3 table); baseline manifest is bound indirectly (L342–346). | Re-state the preflight set as the five §7.1-consistent items (identity manifest, identity table, enrichment manifest, baseline manifest, r3 golden table) with their exact locators/hashes, matching the bound-reference table in `sprint-m1-06.md` §2. |
| Info | Correctness | `neural/docs/m1-06-verification-evidence.md` L37–38; `tests/unit/test_transaction.py` L57; `tests/golden/test_preprocess_authority.py` L46 | The full local suite record is labeled `data_scope: synthetic_mc`, but one of the two skipped items (`test_external_r3_table_hash_when_available`) is a `source_only` external-table hash check; bundling it under `synthetic_mc` slightly blurs the taxonomy the change set itself introduced. | `-rs` output shows the two skip reasons; the evidence doc records them exactly. | Add a footnote to the full-suite record noting the skipped external-table item is `source_only`, or record that skip under `source_only` separately while keeping the synthetic tests under `synthetic_mc`. Cosmetic; no authority impact. |
| Info | Documentation | `neural/docs/artifact-schema.md` §2 preprocess-manifest table (first block); `neural/src/preprocessing/pipeline.py` L134–141 | The first preprocess manifest audit block is named "identity" although it lists top-level header/status fields (`schema_version`, `status`, `run_type`, `protocol_id`, UTC times), which collides with the `identity` duplicate-count block inside `mc_summary.json` used by the gate. | Table row "identity" vs manifest code that writes header fields plus a separate `counts` block; `_verify_counts` reads `summary["identity"]`. | Rename the block to "header" (or "status") to avoid confusion with the identity duplicate facts. |
| Info | Consistency | Bound references in `sprint-m1-06.md` §2; `m1-06-verification-evidence.md` §4; `final-technical-report.md` §4 | All bound SHA-256s (two MC ROOTs, identity manifest/table, enrichment manifest, baseline manifest, r3 golden table), the pre-registered counts (read 419,943/554,279/974,222; selected 187,128/11,976/199,104; train/validation/test totals 119,676/39,719/39,709; development 159,395; duplicates 2/4), the 15-feature classifier and 29-column schema, the schema names, and the artifact layouts in the new documents match the sealed YAMLs and implemented `src/` exactly. | Cross-checked against `preprocess_protocol_v1.yaml` L9/L43/L147–151, §7.1/§7.2, `adversarial_mlp_protocol_v1.yaml`, `development.py`, `test_opening.py` (`TEST_SCORE_COLUMNS`), `qualification.py` (`OOF_COLUMNS`). | No action; preserve these invariants when the authority evidence is transcribed. |
| Info | Correctness | Local evidence claims across `m1-06-verification-evidence.md` §3, `final-technical-report.md` §3, `sprint-m1-06.md` §10 | Independent replay in this review session reproduced every recorded Windows/synthetic claim: `pip check` exit 0 (`No broken requirements found.`); `higgsml-preprocess --help` and `higgsml-train --help` exit 0; focused synthetic suite `23 passed`; full suite `227 passed, 2 skipped`; skip reasons byte-identical; freeze `git diff --exit-code 85b67d1 -- src config/preprocess_protocol_v1.yaml config/adversarial_mlp_protocol_v1.yaml` exit 0 at HEAD `85b67d1…`; `git check-ignore` on `config/preprocess_run.local.yaml` matching `/neural/config/preprocess_run.local.yaml`; audit grep no hits; `git diff --check` exit 0; platform probe Windows/AMD64, Python 3.12.13, PyTorch 2.7.1, threads 8, ambient deterministic false; `osx.yml` SHA-256 `f54522acff344e2644ad8dd03b3a913b6d38fd2e097cfe9a00244748fae84430`. | Commands executed in `neural/` on 2026-09-02 returned identical values to the evidence records. | No action; re-verify the evidence file again after any future edit before closure. |
| Info | Requirement | `m1-06-verification-evidence.md` §4/§5; `sprint-m1-06.md` §10; git status | The M1-06 state is correctly captured: workflow stops at `authority_environment_preflight`; the authority gates are `blocked`/`not_run`; `test_opening` is `not_run` and correctly excluded from closure requirements; no real-data/held-out exposure is claimed; no authority run path or evidence file was created; the working tree contains only the three tracked edits plus the new documentation/review files, with `neural/runs/` unchanged (only M1-04/M1-05 synthetic fixtures). | `git status --porcelain`; `Get-ChildItem runs` shows only `m1-04-synthetic-cli-smoke-20260902-01`, `m1-05-synthetic-cli-smoke-20260902-01/02`, `.gitkeep`; `.gitignore` L226–228. | No action. Do not commit or close M1-06 until the locked native `osx-arm64` gates pass; a follow-on sprint should be empty while blocked. |
| Info | Documentation | `m1-06-verification-evidence.md` L29; `.gitattributes` L1–2; `git config core.autocrlf=false` | The local `osx.yml` bytes observation (SHA-256 `f54522ac…`) is a stable fingerprint of the repository bytes on this host: `.gitattributes` pins `neural/osx.yml text eol=lf` and `core.autocrlf=false`, so the Windows-computed hash equals the bytes an authority host would read from the same commit. The evidence doc correctly labels it non-authoritative ("这不是 authority environment restore 证据"), and the sprint doc requires the authority host to compute and transcribe `osx.yml` SHA-256 fresh. | `.gitattributes` L1; recorded hash matched `Get-FileHash` output. | No action; keep the non-authoritative labeling so this value is never mistaken for the authority environment-restore proof. |

## Independent verification performed (replayed evidence)

All commands executed from `neural/` (Windows/AMD64, conda env `pytorch`) during this review; results match the change set's records exactly.

| Check | Command | Result |
|---|---|---|
| Dependency consistency | `conda run -n pytorch python -m pip check` | exit 0, `No broken requirements found.` |
| CLI help smokes | `higgsml-preprocess --help`; `higgsml-train --help` | both exit 0 |
| Focused synthetic suite | `python -m pytest -q tests/integration/test_preprocess_micro_root.py tests/integration/test_development_run.py tests/integration/test_open_test_cli.py` | `23 passed` |
| Full local suite | `python -m pytest -q` | `227 passed, 2 skipped` |
| Skip reasons | `python -m pytest -q -rs` | skip at `test_preprocess_authority.py:46` (`authoritative_gate_not_run: external r3-ARM64 table is absent`) and `test_transaction.py:57` (`directory symlinks are unavailable on this platform`) |
| Scientific byte freeze | `git diff --exit-code 85b67d1 -- src config/preprocess_protocol_v1.yaml config/adversarial_mlp_protocol_v1.yaml` | exit 0 (HEAD `85b67d1704815079bb3aa4fe8f1c9a5eba9ece9d`) |
| Local config ignore | `git check-ignore -v config/preprocess_run.local.yaml` | exit 0, rule `/neural/config/preprocess_run.local.yaml` (.gitignore L226) |
| Forbidden-reference audit | `rg -n "xgboost[\\/]+src\|full-baseline-2026-08-10\|700600" src config` | no matches (exit 1) |
| Whitespace hygiene | `git diff --check` | exit 0 |
| Platform/environment probe | `python -c "import platform,sys,torch; …"` | Windows / AMD64 / 3.12.13 / 2.7.1 / 8 / False |
| Local osx.yml fingerprint | `Get-FileHash -Algorithm SHA256 osx.yml` | `f54522acff344e2644ad8dd03b3a913b6d38fd2e097cfe9a00244748fae84430` |
| Working-tree hygiene | `git status --porcelain`; `Get-ChildItem runs` | unchanged tracked set; runs contain only M1-04/M1-05 synthetic fixtures |

## Priority-axis assessment

| Axis | Assessment |
|---|---|
| Authority-gate executability | Now documented and explicit (`run_authority_gate` one-liner in runbook §6 and sprint §7; pytest module correctly scoped as contract-only). Residual operational risk: no automated exercise, no stable error-code mapping, no containment/protocol injection (F01–F03). |
| Evidence integrity | All recorded Windows/synthetic claims independently reproduced; taxonomy consistent across the four documents and §6's four-field table; taxonomy blur only in the full-suite `source_only` skip item (Info). |
| Scientific safety and blinding | Fully preserved: MC-only, no real-data probing, no full-data/authority run attempted, no held-out test exposure, `test_opening` `not_run` without separate authorization, scientific bytes frozen vs `85b67d1`. |
| Document accuracy vs code/protocol | Strong; residual precision items are gate-scope wording (manifest/canonical-hash), 19-vs-29 column phrasing, and the §4 lineage-enumeration ambiguity (F04–F06). |
| Change discipline | `.gitignore` rule effective and verified; freeze command blind to untracked additions and CLI/golden-test changes (Low); M1-06 correctly left uncommitted. |

## Conclusion

The M1-06 change set is an acceptable documentation-and-boundary implementation that resolves the accepted findings of the prior document review, accurately records the Windows/AMD64 preflight blocker, does not overclaim authority evidence, and introduces no scientific-safety regression. The two Medium findings concern the reliability and testability of the single authority gate that M1-06 will eventually have to run on the locked native `osx-arm64` host; they should be addressed as a declared, reviewed change (synthetic gate test and/or controlled runner plus error-code mapping) that lands before the authority execution, not during it. The Low findings are documentation-precision items.

Do **not** mark M1-06 complete, and do **not** commit it, until:

1. the locked native `osx-arm64` environment is restored from `osx.yml` with zero-skip full pytest on the authority host;
2. the full-data preprocess run passes `run_authority_gate` against the r3 golden table with an independent, exclusive evidence file;
3. the complete five-candidate five-fold development OOF is run and its actual qualification branch is audited per the frozen rules (unobserved branch `not_applicable`, referencing M1-04 synthetic coverage);
4. all evidence is transcribed into `m1-06-verification-evidence.md` with the `method × platform × data_scope × authority` fields.

`open-test` remains out of scope unless the user separately and explicitly authorizes it against a specific eligible frozen development run; its correct state is `not_run`.

**Review boundaries restated:** MC-only; no real data read/hashed/probed/preprocessed/scored/plotted/published; held-out test not opened or decoded; `open-test` not executed; no authority or full-data gates and no tests involving real/full-MC data were run — only the local synthetic/static checks the change set itself records, replayed read-only. No implementation or documentation file was modified; only this report was written. The configured GLM primary was unavailable; this review was completed by fallback `deepseek/deepseek-v4-flash`.
