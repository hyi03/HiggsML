# Sprint M1-02 Code Review

- **Reviewer:** opencode-go / glm-5.2
- **Review type:** code review (implementation change set)
- **Review date:** 2026-09-02
- **Worktree:** `D:\code\HiggsML-worktrees\xgboost-refactor` (branch `codex/xgboost-refactor`, base HEAD `409a728` = sprint M1-01 commit, plus the uncommitted M1-02 change set)
- **Authoritative inputs:** `AGENTS.md`, `docs/1-Requirement/FR-001-angular19-xgboost-refactor.md`, approved design `docs/superpowers/specs/2026-09-01-xgboost-refactor-design.md`, confirmed plan `docs/3-Plan/sprint-m1-02.md`, `docs/4-Reviews/sprint-m1-02-review-confirm.md` (decisions 1-17)

**Verdict: Pass with required actions.** No Critical and no High findings. The sprint
delivers a scientifically faithful, MC-only, byte-equivalent migration: the domain move is
verified byte-identical to the M1-01 implementations, the new pipeline is golden-verified
against the legacy orchestration, the published schema/layout matches the approved design
exactly, and the full-suite failure set is proven **identical test-by-test** to the M1-01
baseline. Three Medium findings (test strength on the import boundary, a new-chain
dependency on the temporary compat stubs, and a code-hardcoded luminosity constant) should
be resolved or explicitly triaged in code-review-confirm before M1-03 begins.

## 1. Change set reviewed

Tracked modifications (10 files, `+56/−1446`):

- `src/angular5.py`, `src/features.py`, `src/pairing.py`, `src/reconstruction.py`,
  `src/selection.py`, `src/split.py`, `src/weights.py`, `src/input_profiles.py` — each
  reduced to a 7-line compatibility alias (`sys.modules[__name__] = <new module>`)
- `src/artifacts/transaction.py` — `publish_manifest` gains an optional
  `relative_path` parameter (default `"manifest.json"`, backward compatible)
- `src/cli/preprocess.py` — M1-01 stub replaced by the real CLI

Untracked implementation/test paths:

- `src/domain/` (`__init__.py`, `angular5.py`, `features.py`, `pairing.py`,
  `reconstruction.py`, `selection.py`, `split.py`, `weights.py`)
- `src/preprocessing/` (`__init__.py`, `application.py`, `pipeline.py`, `profiles.py`,
  `reader.py`)
- `tests/unit/test_refactor_domain.py`, `tests/unit/test_refactor_preprocessing.py`,
  `tests/golden/test_refactor_preprocess_golden.py`,
  `tests/integration/test_refactor_preprocess_cli.py`

Not modified (verified against HEAD): `pyproject.toml`,
`config/preprocessing_protocol_v1.yaml` (plan §9 requires byte-for-byte consumption —
holds), `config/preprocessing_run.example.yaml`, all legacy execution modules, and all
legacy tests. `git diff --check` is clean.

## 2. Verification performed

All commands run from `xgboost/` with `.venv/Scripts/python.exe` (Python 3.12.13; numpy
2.5.2, pandas 3.0.5, PyYAML 6.0.3, uproot 5.7.6, awkward/xgboost per venv).

| Check | Command / method | Result |
|---|---|---|
| Focused M1-02 + M1-01 suites | `python -m pytest -q tests/unit/test_refactor_domain.py tests/unit/test_refactor_preprocessing.py tests/golden/test_refactor_preprocess_golden.py tests/golden/test_refactor_characterization.py tests/unit/test_refactor_config.py tests/unit/test_refactor_artifacts.py` | **58 passed, 2 skipped** (Windows symlink-privilege skips) |
| CLI integration | `python -m pytest -q tests/integration/test_refactor_preprocess_cli.py tests/integration/test_refactor_cli.py` | **12 passed** (includes `test_higgsml_preprocess_micro_root_smoke`, exit 0, exact run layout) |
| Full suite, M1-02 change set | `python -m pytest -q` | **211 failed, 791 passed, 3 skipped** (104.61s), exit 1 |
| Full suite, M1-01 baseline (reproduced) | isolated `git worktree` at HEAD `409a728` + meta-path shim removing the editable install finder, `--basetemp` outside the worktree | **211 failed, 776 passed, 2 skipped** (84.96s) — exactly the M1-01 recorded baseline |
| Failure-set diff | per-test comparison of both `FAILED` inventories (`Compare-Object`, both directions) | **failure sets identical test-by-test**; 0 new, 0 fixed, 0 grown. `+15 passed / +1 skipped` reconciles exactly with the 16 new tests (3 domain + 9 preprocessing [8 pass + 1 symlink skip] + 1 golden + 3 integration) |
| Legacy-domain test surface through aliases | `pytest -q tests/test_selection.py tests/test_features.py tests/test_weights.py tests/test_pairing.py tests/test_split.py tests/test_input_profiles.py tests/test_selection_cutflow.py tests/test_cutflow.py tests/test_angular5.py tests/test_preparation_pipeline.py` | **155 passed** |
| Domain migration byte-identity | `git show HEAD:./src/<m>.py` vs `src/domain/<m>.py` via `git diff --no-index` for all 7 modules + `input_profiles` → `preprocessing/profiles` | **code byte-identical**; only additions are module docstrings (pairing/reconstruction/features/weights/selection +2 lines; split +2/−1 trailing blank line; angular5 one docstring line reworded; profiles +2 lines) |
| Alias mechanism probe | import `src`, `src.selection`, `importlib.import_module`, `from src.selection import X`, package-attribute access, on Python 3.12.13 | all styles resolve to `src.domain.selection`; package attribute equals the implementation module; string-based monkeypatching (`tests/test_selection.py:441` patches `src.selection.reconstruct_candidate`) still targets the same module object |
| Import-boundary test strength probe | replicate the substring check of `test_new_preprocess_imports_do_not_reach_legacy_execution_modules` against candidate import statements | `from ..pipeline import …`, `from ..preparation import …`, `from ..provenance import …`, `import mplhep` **all escape**; `import matplotlib`, `from ..plots import …`, `run_prediction` caught only coincidentally by substrings |
| Micro-ROOT CLI probe (manual) | build fixtures via the test helpers in `%TEMP%`, run the `higgsml-preprocess` console entry | exit 0, `preprocessing complete: 8 development, 4 test events`; run dir contains exactly the 6 approved files; CSV header is exactly 32 columns in frozen order (19 model features then `m4l,label,split,physical_weight,train_weight,channelNumber,eventNumber,runNumber,mcWeight,xsec,kfac,filteff,sum_of_weights`); manifest verified to bind protocol/run-config SHA-256 + resolved paths, git commit `409a728…` + `tracked_worktree_dirty` + code SHA-256, software versions, per-input `device/inode/size_bytes/mtime_ns/sha256`, dual compressed/canonical CSV hashes, counts (8/4/12), schema, `test_opened: false`; second run reproduces identical canonical CSV hashes |
| Luminosity constant vs frozen configs | grep `luminosity` across `config/*.yaml` | all frozen configs use `luminosity_pb: 10000.0`, equal to the hardcoded `LUMINOSITY_PB` |
| Whitespace / repo hygiene | `git diff --check`; `git status --short` before and after review | clean; worktree unchanged by this review (all probes confined to `%TEMP%\opencode` and removed; baseline worktree removed via `git worktree remove`) |

Boundaries observed during this review: no real data exists in or was read from this
worktree; no frozen run or held-out test artifact exists in or was opened from this
worktree; the authoritative 345060/363490 ROOT count/cutflow reproduction gate was **not
executed** and no evidence was fabricated for it; no implementation or test file was
modified by the reviewer.

## 3. Findings

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| Medium | Test / Boundary enforcement | `tests/unit/test_refactor_preprocessing.py:212-224` | The M1-05 deletion-readiness guard `test_new_preprocess_imports_do_not_reach_legacy_execution_modules` is a plain substring match over source text. Relative imports of legacy modules and non-`matplotlib` plotting imports escape it, and `src/config.py` (part of the new-chain import graph and of the manifest code identity) is not covered at all. | Reviewer probe (§2): `from ..pipeline import …`, `from ..preparation import …`, `from ..provenance import …`, and `import mplhep` are all accepted by the implemented check; only `matplotlib`/`plots`/`predict` are caught, and only coincidentally by substring. Plan §5.2 requires the *import graph* to be verified; review-confirm #8 accepted exactly this guarantee. | Replace with an AST-based import check that resolves import/from-import targets (including relative `..x` forms) against module paths, modeled on `tests/unit/test_refactor_domain.py:28-43`; apply it to `src/cli/preprocess.py`, `src/preprocessing/*.py`, `src/config.py`, and `src/artifacts/*.py`, forbidding resolved imports of legacy execution modules (`src.pipeline`, `src.preparation`, `src.provenance`, `src.io`, `src.plots`, real-data and generic-predict paths, `matplotlib`/`mplhep`, `xgboost` where out of place). |
| Medium | Maintainability / M1-05 readiness | `src/config.py:10-11`; `src/preprocessing/application.py:68-85` | The **new** chain imports the frozen feature constants through the temporary compatibility stubs (`from .angular5 import ANGULAR5_FEATURES`, `from .features import FEATURES`). When M1-05 deletes the historical module set, removing those stubs breaks the new main chain unless `src/config.py` is edited in the same change. Additionally, `_code_sha256` hashes `src/config.py` but not the stub files it imports, so a modified alias would change runtime behavior without changing the manifest's recorded code identity. | `src/config.py:10-11` is the only new-chain import routed through a compat alias (pipeline/reader/application/cli all import `..domain`/`.reader` directly — verified by grep). `application.py:70-77` enumerates hashed sources; `src/angular5.py`, `src/features.py` are absent while `config.py`, which imports them, is present. | Change `src/config.py` to `from .domain.angular5 import …` / `from .domain.features import …` now (2-line, zero-behavior change, no protocol impact), or make it the first committed step of M1-05; extend `_code_sha256` to cover every transitively imported new-chain file (including `src/__init__.py`, `src/cli/__init__.py`, `src/artifacts/__init__.py`, which are also currently unhashed). |
| Medium | Requirement / Auditability | `src/preprocessing/pipeline.py:23`; `config/preprocessing_protocol_v1.yaml:51-53`; `src/preprocessing/application.py:153-193` | The integrated luminosity (10,000 pb) — the multiplicative constant of every `physical_weight` — is hardcoded in code. It is absent from the frozen protocol (the `weighting` section declares semantics only) and from the published manifest. Design §7.1/§7.3 place scientific behavior in the versioned protocol, so today the constant is auditable only by reading source. | `LUMINOSITY_PB = 10_000.0` (`pipeline.py:23`) is the sole source of the value; grep confirms every frozen config used `luminosity_pb: 10000.0`, and the golden test pins equivalence to the legacy default, so the *value* is correct; the gap is traceability only. Plan §9 forbids modifying protocol v1 in this sprint, so the omission is inherited from M1-01. | Without touching protocol v1: record `luminosity_pb` explicitly in the run manifest (e.g., a `weighting` block or inside `mc_summary.json`) so each run is self-describing; register an explicit `luminosity_pb` field for the next protocol version so future changes require a version bump instead of a code edit. |
| Low | Test / Schema boundary | `tests/unit/test_refactor_preprocessing.py:70-76`; `tests/integration/test_refactor_preprocess_cli.py:155-156,171`; `src/domain/features.py:33-45` vs `config/preprocessing_protocol_v1.yaml:99-107` | No test directly asserts that the 19 model features and 13 metadata columns are disjoint, that `len(OUTPUT_COLUMNS) == 32`, or that `MODEL_FEATURES` excludes the *protocol's* forbidden list. The protocol forbidden list (`physical_weight`, `train_weight`, `source_sample`, `source_entry`, …) and the domain `FORBIDDEN_FEATURES` (`mcWeight`, `xsec`, … `source_file`, `period`) are two different lists, and the domain-level leakage test (`test_refactor_domain.py:12-15`) checks only against the domain list. | Column checks stop at `tuple(columns) == OUTPUT_COLUMNS` and `columns[:19] == MODEL_FEATURES` — a duplicated label in `OUTPUT_COLUMNS` would still satisfy both (duplicate-preserving `.loc[:, OUTPUT_COLUMNS]` selection); the metadata/model disjointness currently rests only indirectly on the separately frozen `FEATURES`/`ANGULAR5_FEATURES` lists. Reviewer probe confirms the published CSV is correct (32 unique columns, exact order) — the gap is regression protection, not a present defect. | Add cheap exact assertions: `len(OUTPUT_COLUMNS) == 32`, `len(set(OUTPUT_COLUMNS)) == 32`, `set(MODEL_FEATURES).isdisjoint(METADATA_COLUMNS)`, and `set(MODEL_FEATURES).isdisjoint(protocol.raw["forbidden_features"])`; optionally assert the manifest's `forbidden_model_features` covers the weight/identity metadata names. |
| Low | Consistency | `src/preprocessing/pipeline.py:96-99` vs `pipeline.py:217-223` | `cutflow.json` / `mc_summary.json` key samples as `"higgs"` / `"zz"` (the `PreparedMCSample.name`), while each nested cutflow still carries `sample_name: "higgs_345060"` / `"zz_363490"` (`f"{name}_{expected_channels[0]}"`). The legacy convention (`src/pipeline.py:125-140` `build_cutflow`) keyed samples by the DSID-suffixed name. Two conventions coexist in one artifact. | Reviewer probe (§2): cutflow top-level keys are `['schema_version', 'samples']` with `samples` keys `higgs`/`zz`, while `samples.higgs.sample_name == "higgs_345060"`. M1-03 (develop) and M1-04 consumers will read these artifacts and must guess which key is authoritative. | Unify: either key the samples dict by the DSID-suffixed name (matching the inner `sample_name` and legacy convention) or document the short-key contract where the artifact schema is described; do it now, before M1-03 builds parsers on top. |
| Low | Test / Evidence | `tests/golden/test_refactor_preprocess_golden.py:9,49-114`; `src/pipeline.py:11-16` | The golden "legacy vs migrated" comparison is now orchestration-only: after the alias swap, the legacy arm (`src.pipeline`) shares the exact same domain modules as the new pipeline, so a domain-level migration regression could never be caught by this test. It also covers a single higgs/GeV/no-override event; the zz path (MeV, open-data profile, protocol-supplied normalization) has no legacy-golden arm. Plan §9 assumed the old implementation is *retained* until golden comparison completes; instead it was moved and aliased in the same change set. | `src/pipeline.py:11-16` imports `.features/.selection/.weights/.split/.input_profiles`, all of which are now aliases of `src.domain.*`/`src.preprocessing.profiles` (verified). Equivalence therefore rests on (a) the byte-identical move — verified in this review (§2) — and (b) M1-01 characterization values passing through the new paths. Both hold today; neither is reproduced by a live old-vs-new comparison. | Record the byte-identity evidence in §10.3 delivery (the `git diff --no-index` output or per-file content hashes of `HEAD:src/<m>.py` vs `src/domain/<m>.py`); optionally add a zz-path golden that exercises the legacy `normalization_override` API against the protocol-normalization path so both weighting routes are golden-pinned. |
| Low | Test / Coverage | `tests/integration/test_refactor_preprocess_cli.py:179-217`; `tests/unit/test_refactor_preprocessing.py:112-127` | DSID binding is tested with a monkeypatched event stream at unit level (wrong `channelNumber` → rejected) and real-data rejection at protocol level (unknown `samples.data` key → rejected before the run dir is created), but there is no CLI-level end-to-end test that `higgs_root`/`zz_root` are actually bound to their DSIDs (e.g., swapping the two micro ROOTs must fail closed). | Plan §5.3 test requirement: real-data/DSID run configs not allowed by the protocol must fail closed; the implemented integration test only mutates the *protocol*, not the run-config-to-ROOT binding. The binding itself is enforced only by post-read `validate_channel_numbers` (`pipeline.py:134`, `reader.py:158-169`). | Add one integration case reusing the existing fixture builder with the two ROOT paths swapped (and/or a wrong-DSID micro ROOT), asserting exit 1, a failure receipt, and no success manifest. Cheap: all helpers already exist in the test module. |
| Low | CLI / Diagnosability | `src/cli/preprocess.py:29-31` | Every failure collapses to `higgsml-preprocess failed: {exc}` on stderr with no exception type and no traceback. The failure receipt records `error_type` (`transaction.py:119-124`), but the terminal message does not, so bug-class errors (e.g., a `TypeError` from a code defect) surface as one opaque line. | `except Exception as exc: print(f"higgsml-preprocess failed: {exc}", file=sys.stderr)` — the type is discarded; `KeyError` messages render as bare quoted strings, making CLI-level triage needlessly hard. | Print `f"higgsml-preprocess failed: {type(exc).__name__}: {exc}"` at minimum; consider an opt-in verbose/debug mode (env var) that re-raises for full tracebacks. Keep normalized exit codes unchanged. |
| Low | Consistency / Input binding | `src/preprocessing/application.py:114-119,136-139`; `src/preprocessing/reader.py:51-62` | ROOT inputs get the full regular-file/no-symlink/stat+SHA-256 binding with before/after re-verification, but the protocol and run-config files are read with plain `read_bytes()` — symlinks are accepted and no stat signature is recorded; only a mid-run *content* change is detected by the two re-reads. Design §11 rejects symlinked inputs generally. | `run_preprocessing` reads `protocol_source.read_bytes()` / `run_config_source.read_bytes()` without any `is_symlink`/`S_ISREG` check (`application.py:116-117`), unlike `inspect_mc_input` (`reader.py:55-62`). Impact is low (configuration, not data; the run-config snapshot and both SHA-256s are recorded and re-verified). | For symmetry, reject symlinked/non-regular protocol and run-config files (a two-line check each), or record the accepted difference explicitly where the input-binding contract is documented. |
| Info | Verification (positive) | whole change set | Full-suite gate satisfied with set-level proof, not just counts. | M1-01 baseline reproduced at HEAD `409a728` in an isolated worktree: 211 failed / 776 passed / 2 skipped. M1-02 change set: 211 failed / 791 passed / 3 skipped. Per-test diff: **identical failure sets**; the +15 passed / +1 skipped delta reconciles exactly with the 16 new tests; all 211 failures live in 18 legacy `*_run`/script test modules untouched by M1-02 (Windows symlink privilege, POSIX-only `os.O_DIRECTORY`, absent frozen runs — the known historical classes). | Record this set-level identity (not only the counts) in §10.3 delivery evidence, per plan §7/§10.3. |
| Info | Correctness (positive) | `src/domain/*`, `src/preprocessing/profiles.py`, compat aliases | Domain migration is verified byte-identical (only module docstrings added; zero code changes) across all eight modules; the `sys.modules` alias mechanism was probed and resolves every import style (including package-attribute access and string-based monkeypatch targets) to the domain modules on Python 3.12 (project pins `requires-python >= 3.12`); the 155 legacy domain tests pass unchanged through the aliases. | §2 verification table: per-module `git diff --no-index` outputs; alias probe; legacy-domain pytest run. | None. This is the equivalence evidence the sprint set out to produce. |
| Info | Correctness (positive) | `src/preprocessing/pipeline.py`, `src/preprocessing/reader.py` | MC-only reader is a faithful subset of legacy `iter_events` (same chunk-size validation, canonical-branch request construction, missing-branch check, awkward event materialization) with `is_data`/`entry_stop`/`source_entry` removed and `tree_name` pinned to the frozen profile — double-enforced by the protocol's exact-key sample validation. Signed `physical_weight` and abs/mean `train_weight` semantics are golden-verified including a negative-`mcWeight` event; duplicate canonical identity is rejected before partitioning; development=train+validation and test=test verified end-to-end on the micro fixture; degenerate Angular5 geometry fails closed exactly as the legacy enrichment chain did. | Golden test `test_migrated_preprocess_matches_frozen_domain_weight_split_and_cutflow` asserts `check_exact=True` over the 27 shared columns plus cutflow equality; probe run shows counts 8/4/12 from 2/2/2 split buckets per sample. | None. |
| Info | Artifact (positive) | `src/preprocessing/application.py`, `src/artifacts/transaction.py` | End-to-end artifact semantics verified: exactly the approved 6-file layout; 32-column ordered schema; deterministic gzip (`mtime=0`) with identical canonical CSV hashes across repeated runs; manifest binds protocol + run-config (SHA-256 + resolved path), Git commit/dirty + code SHA-256, software versions, per-input stat signature (device/inode/size/mtime_ns) + SHA-256 with post-read re-verification, dual compressed/canonical hashes, counts, schema, `test_opened: false`; mid-run input replacement writes `failure.json` and no manifest; an occupied run dir fails before any ROOT inspection; `publish_manifest(relative_path=...)` is the only change to the M1-01 transaction and is backward compatible (M1-01 artifact tests pass unchanged). | §2 probe outputs; `tests/unit/test_refactor_preprocessing.py:172-209,227-253`; `tests/unit/test_refactor_artifacts.py` (58 passed, 2 skipped run). | None. |
| Info | Process / Delivery obligations | plan §10.3, §10.4 | Remaining sprint-process items (not code defects): (a) §10.3 must record the failure-set diff vs M1-01 (now proven identical — see above), the environment/dependency versions, and the explicitly **not executed** authoritative-ROOT gate (345060 read `419943`/selected `350928`; 363490 read `11260`/selected `471`) without borrowing frozen artifacts; (b) §10.4 must stage only M1-02 plan/reviews/implementation — `docs/3-Plan/sprint-m1-03.md`…`sprint-m1-06.md` are currently untracked and must remain so; (c) two code reviews + code-review-confirm are still required before the sprint closes (this report is one of the two). | `git status --short` shows M1-03..M1-06 plans untracked; no commit exists yet for M1-02; the parallel code review file `sprint-m1-02-code-review-by-opencode-go-kimi-k2.7-code.md` has appeared but no confirm document exists yet. | Complete the evidence sections verbatim from actual command output at delivery time. |

## 4. Acceptance criteria assessment (plan §6)

| Criterion | Status | Notes |
|---|---|---|
| Output contains exactly the 19 frozen model features (protocol order) + 13 required metadata; metadata never in the model allowlist | **Met** (verified empirically: 32 ordered columns, first 19 = `MODEL_FEATURES`) | Direct disjointness/count assertions missing → L-level finding |
| Development = train+validation, test = test bucket; per-row `split` preserved, published as separate files | **Met** (probe: dev split set `{train, validation}`, test `{test}`; counts 8/4) | — |
| New preprocessing has no real-data input surface | **Met** (run-config schema has only `higgs_root`/`zz_root`/`chunk_size_events`; protocol exact-keys pin DSIDs/profiles; reader is MC-only) | CLI-level DSID-swap case untested → L-level finding |
| Manifest binds protocol, run config, Git/code identity, software, input stat/SHA-256, outputs, schema, counts, compressed+canonical dual hashes | **Met** (all verified in the probe run) | Luminosity and split authority bound only transitively via protocol SHA-256; luminosity not recorded → M-level finding |
| Focused tests all green; full suite adds no failures, no failure-set growth, no M1-02-attributable failures | **Met and proven at set level** (failure sets identical test-by-test vs reproduced M1-01 baseline) | Record the set-level proof in §10.3 |

## 5. Boundary statement

This review did not read real data (none present in the worktree), did not open any frozen
run, held-out test, or authoritative ROOT artifact, and did not execute the authoritative
345060/363490 count/cutflow reproduction gate — consistent with plan §9, which declares
that gate not executable in this worktree and forbids substituting frozen artifacts as
evidence. All reviewer probes used temporary micro fixtures under `%TEMP%\opencode` and
were deleted; the baseline full-suite run used a temporary `git worktree` at HEAD that was
removed afterwards. No implementation, test, or configuration file was modified by this
review; the only file written is this report.

## 6. Required actions before M1-03

1. **[M-1]** Replace the substring import-boundary test with an AST-based, relative-import
   aware check covering the full new-chain import graph (including `src/config.py`).
2. **[M-2]** Repoint `src/config.py` at `src.domain` (and extend `_code_sha256` coverage),
   so the new main chain no longer routes through the temporary compat stubs.
3. **[M-3]** Record `luminosity_pb` in the run manifest now; register it as an explicit
   protocol field at the next protocol version.
4. Triage the six Low findings in code-review-confirm (the schema-disjointness assertions,
   the cutflow/summary key convention, the golden byte-identity evidence record, the
   CLI-level DSID-swap test, the CLI error type, and the protocol/run-config symlink
   symmetry), accepting or deferring each explicitly.
5. Complete §10.3/§10.4 delivery evidence verbatim from actual command output, including
   the failure-set identity proof and the declared-not-executed authoritative-ROOT gate,
   then commit only the M1-02 plan/reviews/implementation set.

— opencode-go / glm-5.2, 2026-09-02
