# 使用机器学习寻找 \(H\rightarrow ZZ^*\rightarrow4\ell\) 信号

## 1. 项目目标

本项目使用 ATLAS 公开数据，通过机器学习区分希格斯玻色子信号与标准模型背景，并在真实碰撞数据的四轻子不变质量

\[
m_{4\ell}\approx125\ \text{GeV}
\]

附近寻找信号增强。

需要注意，项目寻找的不是质量为 125 GeV 的单个轻子，而是以下衰变过程中四个轻子的总不变质量：

\[
H\rightarrow ZZ^*\rightarrow4\ell,\qquad \ell=e,\mu
\]

可能的末态包括：

- \(4e\)
- \(4\mu\)
- \(2e2\mu\)

该过程被称为希格斯玻色子的“四轻子黄金道”。最终目标是展示机器学习能否比传统选择方法更有效地提高希格斯信号的可见度，而不是宣称独立发现希格斯玻色子。

## 2. 理论背景

### 2.1 对撞分析中的希格斯玻色子

希格斯玻色子是标准模型中的自旋 0 中性粒子，其质量约为 125 GeV。在 LHC 中，它由质子内部的夸克或胶子参与硬散射产生，并在极短时间内衰变，因此探测器不能直接观测希格斯本身，只能根据其衰变产物重建候选事件。

本项目关注

\[
pp\rightarrow H+X\rightarrow ZZ^*+X\rightarrow4\ell+X,
\]

其中 \(X\) 表示产生过程中可能伴随的 jet、矢量玻色子或顶夸克等物体。分析的核心是利用四轻子共振峰和事件运动学，将希格斯信号与产生相同或相似末态的标准模型背景区分开。

### 2.2 LHC 中的希格斯产生

在质子—质子对撞中，质子内部的夸克和胶子参与硬散射。质量约为 125 GeV 的标准模型希格斯玻色子主要通过以下机制产生：

1. **胶子融合（ggF）**

   \[
   gg\rightarrow H
   \]

   该过程主要通过顶夸克圈图发生，截面最大，是本项目最主要的信号来源。

2. **矢量玻色子融合（VBF）**

   \[
   qq\rightarrow qqH
   \]

   典型特征是两个高能、前向且赝快度间隔较大的 jet。

3. **伴随矢量玻色子产生（VH）**

   \[
   q\bar q\rightarrow WH/ZH
   \]

   末态中可能出现额外轻子、jet 或缺失横动量。

4. **伴随顶夸克产生**

   \[
   pp\rightarrow t\bar tH,\quad tH
   \]

   通常具有更多 jet、\(b\)-jet 和轻子。

不同产生机制的事例拓扑不同，因此 jet 数量、jet 运动学和四轻子系统横动量可以帮助机器学习模型区分信号与背景，或进一步区分希格斯产生模式。

### 2.3 \(H\rightarrow ZZ^*\rightarrow4\ell\) 衰变

质量约为 125 GeV 的希格斯玻色子不足以同时产生两个在壳 \(Z\) 玻色子，因为

\[
m_H < 2m_Z \approx182.4\ \text{GeV}.
\]

因此，在

\[
H\rightarrow ZZ^*
\]

中通常只有一个 \(Z\) 接近其在壳质量，另一个必须是离壳的虚粒子 \(Z^*\)。随后两个 \(Z\) 玻色子分别衰变为带电轻子对：

\[
Z\rightarrow\ell^+\ell^-,
\qquad
Z^*\rightarrow\ell^+\ell^-.
\]

这解释了为什么事件中通常有：

- 一个质量接近 \(m_Z\) 的轻子对 \(Z_1\)
- 一个质量更低且分布较宽的轻子对 \(Z_2\)
- 四轻子总质量在 \(m_H\approx125\) GeV 附近形成窄峰

虽然该衰变道的分支比很小，但电子和缪子的动量可以被较精确地测量，而且最终态可以被完整重建，因此它具有很好的质量分辨率和较低背景。

### 2.4 四动量与不变质量

相对论性粒子的四动量写作

\[
p^\mu=(E,p_x,p_y,p_z).
\]

在自然单位制 \(c=1\) 下，其不变质量满足

\[
m^2=p^\mu p_\mu=E^2-\lvert\vec p\rvert^2.
\]

对于由多个粒子组成的系统，需要先将四动量相加：

\[
P^\mu=\sum_i p_i^\mu,
\]

然后计算组合系统的不变质量：

\[
m^2=
\left(\sum_iE_i\right)^2
-
\left(\sum_ip_{x,i}\right)^2
-
\left(\sum_ip_{y,i}\right)^2
-
\left(\sum_ip_{z,i}\right)^2.
\]

不变质量不依赖参考系，因此是在对撞机实验中识别短寿命共振粒子的核心变量。若中间粒子具有确定质量，其衰变产物的组合不变质量会在该质量附近聚集。

### 2.5 对撞机常用运动学变量

由于质子内部的部分子携带未知比例的纵向动量，对撞事件的纵向中心质心系通常未知。因此，实验分析经常使用横向变量：

\[
p_T=\sqrt{p_x^2+p_y^2}.
\]

粒子相对于束流方向的位置通常用赝快度表示：

\[
\eta=-\ln\tan\left(\frac{\theta}{2}\right).
\]

方位角为

\[
\phi=\operatorname{atan2}(p_y,p_x).
\]

两个物体在探测器中的角距离定义为

\[
\Delta R=
\sqrt{(\Delta\eta)^2+(\Delta\phi)^2}.
\]

\(\Delta R\) 同时编码赝快度和方位角的分离程度，常用于粒子隔离、轻子配对和 jet 匹配。机器学习模型可以利用多个粒子之间的角关联，学习标量希格斯衰变和连续背景在角分布上的差异。

### 2.6 自旋与角分布

标准模型希格斯玻色子是自旋 0 粒子。其衰变没有首选空间方向，但中间 \(Z\) 玻色子的极化及其矢量—轴矢量耦合会在最终四轻子的角分布中留下相关性。

常见的角变量包括：

- 两个 \(Z\) 候选在希格斯静止系中的产生角
- 每个 \(Z\) 静止系中轻子的衰变角
- 两个 \(Z\) 衰变平面之间的夹角

这些变量包含 \(m_{4\ell}\) 以外的信号—背景区分信息。相比直接使用 125 GeV 质量峰，使用角变量训练分类器更能体现希格斯粒子的自旋和衰变动力学。

### 2.7 主要背景过程

最重要的不可约背景是连续产生的四轻子过程：

\[
q\bar q\rightarrow ZZ^*/Z\gamma^*\rightarrow4\ell
\]

以及较小的

\[
gg\rightarrow ZZ^*\rightarrow4\ell.
\]

这些过程具有与信号相同的可观测末态，因此无法只依靠粒子种类将其排除，只能利用四轻子质量、角分布、四轻子横动量和伴随 jet 等统计差异进行区分。

可约背景主要包括：

- \(Z+\text{jets}\)：jet 或重味强子衰变产物被误识别为轻子
- \(t\bar t\)：顶夸克衰变产生真实轻子、\(b\)-jet 和缺失横动量
- \(WZ+\text{jets}\)：包含三个真实轻子及一个误识别轻子

轻子鉴别、冲击参数和隔离度可以显著压低可约背景。但如果公开数据缺少完整的假轻子估计信息，应在报告中明确说明这一限制。

### 2.8 碰撞分析的基本策略

传统分析通过轻子鉴别、\(p_T\)、\(Z\) 候选质量和隔离度等条件逐步抑制背景。机器学习则组合多种运动学变量及其相关性，为每个事件输出信号判别分数。

分类分数本身不是“发现希格斯的概率”。最终仍需比较模型选择后的预期信号 \(S\)、背景 \(B\) 和真实数据中的 \(m_{4\ell}\) 分布。高分区域应具有更高的信号—背景比，并在约 125 GeV 处显示更明显的四轻子质量峰。

由于模型在 MC 上训练，还必须在质量边带检查真实数据与 MC 的一致性。轻子效率、能量或动量刻度、jet 测量、积分亮度和理论截面等误差会影响最终结果，但本项目可以先把它们作为分析局限讨论，再根据项目深度逐步加入定量评估。

## 3. 推荐数据

建议使用 ATLAS 13 TeV Open Data 中经过预筛选的 `exactly4lep` 数据：

- [真实碰撞数据：exactly4lep](https://opendata.cern.ch/record/atlas-93924)
- [配套蒙特卡洛模拟：exactly4lep](https://opendata.cern.ch/record/atlas-93928)

`exactly4lep` 数据要求事件中恰好存在四个轻子，并且每个轻子的横动量至少为 7 GeV，因此特别适合研究 \(ZZ\) 和 \(H\rightarrow4\ell\) 过程。

训练标签来自蒙特卡洛模拟：

- **信号**：\(ggH\)、VBF、VH 等产生模式中的 \(H\rightarrow ZZ^*\rightarrow4\ell\)
- **主要背景**：非共振 \(q\bar q/gg\rightarrow ZZ^*\rightarrow4\ell\)
- **其他背景**：\(Z+\text{jets}\)、\(t\bar t\) 等可能产生误识别轻子的过程

真实碰撞数据没有已知标签，因此不应直接用于监督训练，只用于最终的物理检验。

## 4. 四轻子质量重建

四轻子不变质量为：

\[
m_{4\ell}^2=
\left(\sum_{i=1}^{4}E_i\right)^2-
\left|\sum_{i=1}^{4}\vec p_i\right|^2
\]

若数据中存在希格斯信号，\(m_{4\ell}\) 分布应在约 125 GeV 处出现一个窄峰，其下方是相对平滑的连续背景。

基本事件选择可以包括：

1. 要求事件中恰好有四个电子或缪子。
2. 四个轻子的总电荷为零。
3. 将轻子组成两个“同味异号”（SFOS）轻子对。
4. 选择质量最接近 \(m_Z\approx91.2\) GeV 的轻子对作为 \(Z_1\)。
5. 另一个轻子对作为可能的离壳 \(Z^*\) 候选 \(Z_2\)。
6. 对轻子的横动量、赝快度、隔离度和质量窗口施加合理要求。

## 5. 推荐输入特征

可以考虑以下输入变量：

- 四个轻子的 \(p_T\)、\(\eta\) 和 \(\phi\)
- 两个轻子对的不变质量 \(m_{Z_1}\) 和 \(m_{Z_2}\)
- 四轻子系统的 \(p_T^{4\ell}\) 和 \(\eta_{4\ell}\)
- 轻子之间的 \(\Delta R\)、\(\Delta\eta\) 和 \(\Delta\phi\)
- 四轻子衰变角变量
- jet 数量
- leading-jet \(p_T\)
- 双 jet 不变质量
- 缺失横动量
- 末态类型：\(4e\)、\(4\mu\) 或 \(2e2\mu\)

### 防止质量泄漏

主分类模型中不应直接输入 \(m_{4\ell}\)。

否则模型可能只是学会“质量接近 125 GeV 的事件是信号”，而不是学习信号与背景之间更一般的运动学差异。这还可能改变背景的质量分布，在 125 GeV 附近人为制造峰结构，即质量塑形（mass sculpting）。

训练完成后，应分别检查低模型分数和高模型分数区域的背景 \(m_{4\ell}\) 分布，确认分类器没有人为制造窄峰。

## 6. 模型选择

### 6.1 主模型：XGBoost

最推荐使用 **XGBoost**。

该数据属于典型表格型数据：每个事件是一行，输入为 \(p_T\)、\(\eta\)、角距离和不变质量等高层物理特征。XGBoost 对这类数据通常具有以下优势：

- 对中小规模样本表现稳定
- 不要求大量训练数据
- 可以处理非线性特征关系
- 训练速度较快
- 便于通过特征重要性或 SHAP 解释模型

初始参数可以设置为：

```python
from xgboost import XGBClassifier

model = XGBClassifier(
    n_estimators=500,
    max_depth=3,
    learning_rate=0.03,
    subsample=0.8,
    colsample_bytree=0.8,
    objective="binary:logistic",
    eval_metric="auc",
    reg_lambda=2,
    random_state=42,
)
```

这些参数只适合作为起点，最终应通过验证集或交叉验证调整。

### 6.2 对照模型

建议设置三层模型对照：

1. **Logistic Regression**：提供简单、可解释的线性基线。
2. **XGBoost**：作为项目的主要结果。
3. **多层感知机（MLP）**：作为神经网络对照。

如果后续获得重建粒子序列或更低层级的探测器数据，可以尝试 ParticleNet 或 Particle Transformer。但 `exactly4lep` ntuple 主要提供高层表格特征，因此复杂深度模型不一定优于 XGBoost。

## 7. 训练方法

推荐流程如下：

1. 读取 ROOT 文件，可使用 ROOT、uproot 和 Awkward Array。
2. 对事件实施统一的四轻子预选择。
3. 构造运动学特征，并排除 \(m_{4\ell}\)。
4. 将 MC 划分为训练集、验证集和测试集。
5. 使用 MC 事件权重训练模型。
6. 使用验证集进行超参数选择和早停。
7. 在独立测试集上评估模型。
8. 根据预期物理显著性选择模型分数阈值。
9. 将固定后的模型应用于真实碰撞数据。
10. 比较低分区和高分区的 \(m_{4\ell}\) 分布。

数据划分应尽可能按 MC 数据集、生成过程或独立样本分组，避免高度相似或同源事件同时进入训练集和测试集。

类别不平衡时，应优先使用物理事件权重或 `sample_weight`，不建议通过简单复制少数类事件来平衡样本。

## 8. MC 事件权重

不同 MC 样本的生成事件数和物理截面不同，不能把每个模拟事件视为具有相同物理意义。典型事件权重包含：

\[
w_i=
\frac{\sigma_i\,\mathcal L\,\epsilon_i}{N_i}
\times w_{\text{generator}}
\times w_{\text{correction}}
\]

其中：

- \(\sigma_i\)：过程截面
- \(\mathcal L\)：积分亮度
- \(\epsilon_i\)：过滤效率等归一化因子
- \(N_i\)：生成事件数
- \(w_{\text{generator}}\)：生成器事件权重
- \(w_{\text{correction}}\)：实验修正权重

模型训练和最终事件产额估计都应正确处理这些权重。

## 9. 评价指标

不应只使用 accuracy，因为希格斯信号稀少，而且物理目标不是平均分类正确率。

机器学习指标可以包括：

- ROC-AUC
- Precision–Recall AUC
- 固定信号效率下的背景抑制率
- 校准曲线

最终物理指标应使用预期显著性，例如 Asimov significance：

\[
Z_A=
\sqrt{
2\left[
(S+B)\ln\left(1+\frac{S}{B}\right)-S
\right]
}
\]

其中 \(S\) 和 \(B\) 分别是模型选择后预期的信号和背景事件数。

可以比较：

- 传统 cut-based 选择的 \(Z_A\)
- Logistic Regression 的 \(Z_A\)
- XGBoost 的 \(Z_A\)
- MLP 的 \(Z_A\)

从而判断机器学习是否真正提高了信号灵敏度。

## 10. 真实数据上的信号检验

为了减少人为偏差，可以采用盲分析思想：

1. 在 MC 和真实数据质量边带上完成特征设计、训练及模型选择。
2. 在模型确定前隐藏 125 GeV 附近的信号窗口。
3. 固定模型、阈值和选择条件后再查看信号窗口。
4. 比较不同模型分数区间中的 \(m_{4\ell}\) 分布。

理想情况下，高 XGBoost 分数区域的 125 GeV 信号峰应比低分区域更加明显，而远离信号峰的背景分布仍保持平滑。

还需要检查 MC 与真实数据在质量边带中的特征分布是否一致，以评估 simulation-to-data domain shift。

## 11. 项目结果展示

最终报告建议至少包含：

- 主要输入变量在信号和背景中的分布
- 四轻子事例选择流程
- cut-flow 表格
- 各模型的 ROC 和 Precision–Recall 曲线
- XGBoost 特征重要性或 SHAP 图
- 模型阈值与预期显著性的关系
- 不同模型的 \(Z_A\) 对比
- 低分区和高分区的 \(m_{4\ell}\) 分布
- MC 与真实数据边带一致性检验
- 统计误差、系统误差和模型局限性讨论

## 12. 预期局限

ATLAS 教学版 13 TeV 数据对应的积分亮度有限。在 \(10\ \text{fb}^{-1}\) 数据中，选择前预期产生的四轻子希格斯事件也只有约 29 个，因此真实数据的统计涨落会比较明显。

本项目适合展示：

- 四轻子希格斯信号重建
- 机器学习事件分类
- 背景抑制
- 预期显著性优化
- 模拟与真实数据的一致性检查

但不适合声称完成了具有实验合作组精度的希格斯发现或测量。

## 13. 推荐技术栈

- Python
- uproot
- Awkward Array
- NumPy
- pandas
- Matplotlib / mplhep
- scikit-learn
- XGBoost
- SHAP
- iminuit 或 pyhf（用于后续统计拟合）

## 14. 结论

推荐以 **XGBoost** 作为主模型，以 Logistic Regression 和简单 MLP 作为对照。训练时排除 \(m_{4\ell}\)，使用正确的 MC 权重，并以预期显著性而非 accuracy 作为最终优化目标。

最终通过高模型分数区域的四轻子质量分布，检验机器学习是否增强了 \(m_{4\ell}\approx125\) GeV 附近的希格斯信号。
