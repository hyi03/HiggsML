# Sprint M2-01 Code Review by GPT-5.5

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Correctness | 1. [neural/src/research/training_subsets.py](../../src/research/training_subsets.py):107-119, [neural/src/research/training_subsets.py](../../src/research/training_subsets.py):148-155 | `training-subsets.json` omits the required per-subset total summary. FR-SE-02 requires each subset summary to contain both per-label and total counts/statistics, but the implementation only publishes `summary_by_label`. | FR-SE-02 says the plan summary contains “每subset逐类与总计” and defines group/row counts, signed/absolute/sumw2/neff, and m4l min/max (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:33`). `_summary()` returns only two label records and `subset_entries` stores only `"summary_by_label": summary`; there is no total record or total field. The test suite checks status and nesting but never asserts total counts/statistics exist. | Add a `summary_total` object for each subset, computed from all selected groups using the same group-level weight convention. Add tests that assert total `group_count`, `row_count`, `sum_signed_weight`, `sum_abs_weight`, `sumw2`, `neff_abs`, and m4l range match the per-label records and raw fixture. |
| Medium | Input validation / Security | 2. [neural/src/research/training_subsets.py](../../src/research/training_subsets.py):50-53; [neural/src/research/data.py](../../src/research/data.py):218-219, [neural/src/research/data.py](../../src/research/data.py):453-457 | The M2 reader accepts identities with either `development` or `exploratory` split, independent of the bound base protocol. That is looser than the existing research data reader and permits a malicious prepared run whose events mix or switch split identities while still being rebound into a sample-efficiency overlay. | Existing `load_research_data()` requires `identity["split"] == _research_split(protocol)` for the bound protocol. `_research_split()` is `exploratory` only for all-MC protocols and `development` otherwise. The M2 reader instead accepts any row whose split is in `{"development", "exploratory"}` and only includes split in the recomputed population digest. FR-SE-02 requires strict identity validation before payload access (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:29`). | Reuse the same split rule as `load_research_data()` or import a small shared helper so M2 rejects split drift before building stats. Add a negative test with a valid-receipt prepared run whose identity split is changed to the wrong protocol split and rebound into an overlay. |
| Medium | Canonical artifact validation / Tests | 3. [neural/src/research/training_subsets.py](../../src/research/training_subsets.py):210-218; [neural/tests/research/test_training_subsets.py](../../tests/research/test_training_subsets.py):165-173 | `read_training_subsets()` verifies membership objects semantically but does not enforce canonical JSONL bytes. A receipt-bound artifact with pretty-printed, key-reordered, or otherwise noncanonical membership lines would pass as long as the parsed objects equal `expected_members`, despite FR-SE-02 requiring canonical JSONL ordering/records. | The publisher writes canonical rows, but the reader uses `json.loads()` for the header and each record, then compares parsed objects to `expected_members`. It does not check that the header line equals `canonical({"schema_version": ...})` or that each record line equals `canonical(record)`. The current tamper test mutates `training-subsets.json` without updating the receipt, so it exercises only manifest receipt mismatch, not a valid-receipt semantic/canonical validation path. | In the reader, compare each raw line to the expected canonical bytes/string while reading. Add a test that republishes a membership file with updated receipt but noncanonical JSON for the same records and expects `training_subset_binding_mismatch`. |
| Low | Test coverage | 4. [neural/tests/research/test_training_subsets.py](../../tests/research/test_training_subsets.py):176-195 | The negative test named `test_train_payload_nonfinite_and_identity_conflict_are_binding_failures` only mutates `physical_weight` to NaN; it does not cover the identity-conflict branch or the role/label conflict cases listed in FR-SE-02. | FR-SE-02 acceptance requires role/label conflict coverage and summary tamper coverage (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:63`). The current test changes only `physical_weight` and asserts the generic mismatch status. It does not add an identity key to train payload, mutate a group’s label across rows, mutate a group’s role, or recompute receipts for semantic tampering. | Split this into focused negative tests: train payload identity override, mixed label within one `event_group_id`, role hash mismatch, nonfinite train summary value, and valid-receipt semantic tampering of plan/ledger/membership. |
