# Sprint M5-01 代码评审（GPT-5.6-sol / high）

评审范围为相对提交 `8ac1df1` 的当前未提交 M5-01 实现，并对照已确认的 FR-SE-05、Sprint、review-confirm、M4 artifact/reader 合同与仓库科学安全约束。本次仅做静态评审，未运行测试或读取研究数据。

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Strict schema / replay | `src/research/sample_efficiency.py:49-62,113-127,192-204` | 已发布的 bootstrap 和 curve-row evaluation payload 不符合已确认的 exact contract；reader只是重建同一个非合同对象并比较 bytes，因此不会发现实现自身的 schema 漂移。 | 每个 `evaluation_auc_bootstrap` 通过 `**result` 多写入未注册的 `used_count`，并把 `method` 写成 `paired-event-group-weighted-mann-whitney-v1`，而协议方法是 `paired_event_group_bootstrap_v1`。FR 要求它精确记录 method/unit/seed/requested/valid/status/mean/std/q16/q84并绑定 resample-plan；代码还写入 `resample_plan_id`，却未建立版本化 exact validator。反过来，curve-row `evaluation_uncertainty.validation_absolute_weight_auc` 只有 `_stats()` 的 `used_count`，缺少 FR 要求的 `requested_replicates`、`valid_replicates` 与 `resample_plan_id`。没有任何 `_keys`/类型/nullability validator在发布前或 reader 中执行。 | 为 contract、record、bootstrap、summary及全部嵌套对象实现独立 exact validators，并在 publication 与 read replay 两侧调用。区分 protocol bootstrap method 与 weighted-AUC algorithm ID；按最终 schema统一 `resample_plan_id` 和计数字段，删除未注册字段。加入代码自己生成的 payload 逐层 schema 测试及未知/缺失字段自洽重签测试。 |
| High | Trusted reader boundary | `src/research/sample_efficiency.py:323-325,353-358,386-390,403-407` | engineered19 baseline 的 inference 在计算每个 delta/ratio 时被再次按调用方原始 `batch_path` 读取，却没有核对 ledger artifact ID；这重新打开了 review-confirm 要求关闭的 use-time source replacement 窗口。 | 初始 M4 reader把 `batch_path` resolve成可信 `batch.path`，但后续所有 cell path又使用 `Path(batch_path)`。当前 cell inference在388-389行核对 artifact ID，baseline inference在403-406行只做通用 `read_run()`和 `_w68()`，没有与 `baseline_outcome["stage_artifact_ids"]["inference"]` 比较。若原始路径含可替换祖先或 baseline run 在两次读取间被另一个合法自签 inference替换，delta/relative可使用与已验证 ledger不同的 W68；report run会先以complete发布，之后的返回时 reader重放才可能报错。 | 后续路径全部从 M4 reader返回的 resolved `batch.path` 派生。预先一次性读取每个 complete cell 的 inference，立即核对ledger ID并缓存 verified W68；配对只引用该缓存，不重复打开 baseline path。若保留重读，每次都必须核对 artifact ID、stage、receipt和M4 envelope。增加 M4 reader 返回后替换 baseline inference/祖先路径的 use-time 测试，并断言不会发布 complete report。 |
| High | Human output correctness | `src/research/sample_efficiency.py:28-34,267-320` | PNG、plot spec、CSV和Markdown没有实现 FR 中已确认的人类输出语义。机器 reader 能逐字节重放这些简化输出，但不能使缺失的误差层、失败信息和完整性信息成立。 | `_plot()`只画 mean W68/relative W68 折线，未使用任何 network/subset/evaluation uncertainty字段，也没有对应图例；`plot_spec`只列 panel、row IDs和claim scope，缺少已确认的 series、x-axis label和error-layer semantics。CSV只导出 network/subset W68 与 evaluation AUC std，省略其余指标的network/subset状态、evaluation mean/quantiles/count/plan ID及calibration/template层。Markdown只给总record数、Q*状态名称和一条通用误差句，没有 required completeness、逐层状态或失败表，也不报告 Q*估计值/区间来源。 | 让 plot spec 完整列出每个series、点、缺失断线、误差层、轴标签与图例，并由 renderer只消费该spec；实际绘制合同允许的误差点/带。冻结并输出完整CSV列。Markdown从 records/summary生成 planned/complete/terminal/pair计数、每层状态、terminal失败表和Q*值/边界row IDs，同时保留MC-only声明。针对每个human view增加内容级断言与自洽重签测试。 |
| High | Test authenticity | `tests/research/test_sample_efficiency_report.py:50-104`; `tests/research/test_h4l_sample_efficiency_report_script.py:8-20` | 新测试远未覆盖 Sprint §5 的核心统计和安全矩阵，当前 E2E主要证明happy path可以产文件，无法支持“可重放、严格配对、分层误差和安全 reader”结论。 | service测试仅有 weighted-AUC两个简单例子、Q*三种情况和一个全部complete/单draw/Q* disabled的E2E。没有 golden PCG64 multiplicity、weighted ties with nonuniform/group weights、0/1有效replicate、aggregate-per-replicate、network/subset多draw、terminal与baseline missing、actual-count来源、record/summary/CSV重签、cell use-time替换、assessment poison或PNG spec检查。唯一篡改测试只改Markdown，并用宽泛的 `pytest.raises(Exception)`，没有验证 `ResearchError` status或失败发生层。CLI测试只检查parser字段和`--help`，未执行config/root/fresh-child/退出码路径。 | 按 Sprint清单补 targeted unit tests，避免依赖长E2E覆盖所有语义。所有篡改测试重签file receipt和manifest并断言具体 `ResearchError.status`；为统计函数使用小型golden输入；构造M4 terminal ledger、两draw×五seed和部分缺失矩阵；加入publisher/read use-time替换、assessment payload sentinel及 CLI `main()` 的边界/退出码测试。 |
| Medium | Uncertainty status | `src/research/sample_efficiency.py:142-164,189-191,280-283` | network uncertainty 的状态由“有多少个 draw-level标准差”决定，而不是由每个draw是否至少有两个network seeds决定；合法的单draw、五seed实验会被错误标为 `partially_estimated`。subset对象还把draw均值覆盖成其标准差，造成字段语义自相矛盾。 | network分支先正确计算每个拥有至少2 seed的draw标准差，但随后 `_stats(deviations)` 对只有一个draw的单元素列表返回 `partially_estimated`。FR只规定不足2 seed为`not_estimated`，并允许协议减少draw数量；一个draw内五seed应产生可用的network estimate。subset分支 `_stats(draw_means)` 已把跨draw样本标准差放在 `std`，紧接着163行又令 `mean = std`；CSV再从 `mean` 读取 `subset_w68_std`，使同一对象的mean不再是其输入draw means的均值，而quantiles仍描述draw means。 | 为每层使用专用schema/计算器，避免复用一个含mean/std/quantile的通用对象。network结果记录eligible/used draw数、每draw seed数和 `std_mean`，只按seed支持与缺失draw规则决定状态；subset结果把目标量放在明确的 `std_across_draw_means` 字段，不改写普通mean。覆盖单draw五seed、部分draw不足seed和两draw共同seed交集。 |
| Medium | Numerical validity | `src/research/sample_efficiency.py:41-62,65-92,118-127,187-202` | 非有限统计量被静默过滤而不是按合同拒绝，并可造成 `valid_replicates` 与实际用于统计的数量不一致。 | `weighted_auc()`只检查输入数组有限，没有检查权重求和、numerator、denominator或最终ratio仍有限；有限但极大权重可在求和/乘法中 overflow。`_bootstrap()`把所有非None结果计入 `values` 和 `valid_replicates`，而 `_stats()`又静默丢弃其中的NaN/Infinity，因此record可同时声称更多valid replicates和更小的`used_count`。`_mean()`也未做输出有限性检查。FR明确要求非有限输入/输出禁止进入artifact。 | weighted AUC每个累积量和最终结果都执行 `math.isfinite`，overflow作为binding/numerical error或明确invalid replicate处理；valid count只能由最终有限值计算。`_stats`/`_mean`遇到非有限值应失败，不应过滤。加入极大有限权重、overflow和计数一致性测试。 |
| Medium | Terminal-only bootstrap | `src/research/sample_efficiency.py:360-374,375-424` | 全部 cell terminal 时会发布一个未在合同中注册的伪 resample plan：声明正的 replicate预算却具有0 group和空multiplicity矩阵。 | 没有complete handle时，代码不加载validation，而是构造 `group_count=0`、`replicates=overlay配置正整数`、`multiplicities=[]`，再给它正常 plan ID。FR要求plan ID绑定排序group digest与“完整multiplicity矩阵”；对于声明N个replicate的计划，空矩阵不满足算法。报告必须保留全terminal batch，但合同没有允许这个特殊普通plan形态。 | 明确定义无complete model时的状态对象，例如 `status="not_applicable_no_complete_cells"`、`resample_plan_id=null`、requested预算保留但不声称已构造plan；或通过不依赖model的verified prepared role reader仍构建正常batch plan。exact validator必须检查普通plan的matrix行数等于replicates、每行长度等于group_count且计数和等于group_count。增加全terminal batch测试。 |
| Medium | Report artifact boundary | `src/research/sample_efficiency.py:456-474` | Report reader在验证report manifest/receipt之前先完整重跑M4、加载validation并对全部模型预测；同时不拒绝manifest中的额外文件。这既违背先验证artifact再解析/重放的reader边界，也允许固定输出集合外的自签人类文件混入一个“verified report”目录。 | `_build()`在458行先执行，`read_run(path)`到461行才发生；一个不存在、失败或receipt损坏的report会先触发全部上游数据读取与bootstrap。随后只逐个比较六个expected文件并读取PNG，没有断言 `manifest.files` 精确等于`protocol.json`加固定六文件。攻击者可加入误导性附加Markdown/CSV并重签manifest，reader仍返回成功。 | 先读取report run并验证status、protocol、manifest、固定file set及唯一batch upstream；完成这些廉价检查后再执行上游semantic replay。要求manifest file keys精确等于协议snapshot和六个输出。加入invalid report零上游payload访问 sentinel、extra-file自洽重签和missing-file测试。 |
| Medium | CLI / exit semantics | `src/cli/sample_efficiency_report.py:54-101`; `src/artifacts/transaction.py:26-29,99-109` | CLI没有把publication路径错误稳定映射到已确认的binding/incomplete exit 3；dangling link或检查后占用目标会落入generic exception并返回70，且通常在完整M4/bootstrap计算之后才发现。 | CLI的 `target.exists()` 不识别dangling symlink，也无法排除检查与 `ResearchRun` claim之间的并发占用。`RunTransaction`会用 `os.path.lexists` 正确拒绝，但抛出 `RunPathError(exit_code=4)`；CLI只专门捕获`ResearchError`，其余全部转换为70。`publish_sample_efficiency_report()`又在创建transaction前先运行完整 `_build()`。这与FR列出的usage=2、binding/incomplete=3、unknown=70不一致。 | 在任何上游读取前用安全的claim/preflight service验证并独占fresh child，或让publisher transaction包围构建；CLI显式捕获并规范化 `RunPathError` 到已冻结路径/binding退出语义。加入dangling symlink、并发占用、普通existing target和输出根重解析测试，并验证这些路径不读取M4/validation payload。 |
