# Sprint M6-01 Code Review by GPT-5.5

## Scope

Reviewed the current M6 working-tree implementation against `AGENTS.md`, `neural/AGENTS.md`, `FR-SE-06-controls-and-confirmation.md`, `sprint-m6-01.md`, the document review-confirm, and `artifact-schema.md` §12. The review focused on the new controls/confirmation implementation and the related changes in the sample-efficiency model reader boundary.

Note: CodeRabbit CLI was not installed in this environment, so this is a local evidence-based code review. No further tests were run after the user clarified that only code review was needed; the full regression command was interrupted and is not counted as completed evidence.

## Findings

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Correctness | `neural/src/research/sample_efficiency_controls.py:329` | The controls public reader accepts enabled capacity/CDF artifacts by checking only the manifest receipt, payload self-digests, upstream prefix, and disabled empty-shape guards; it does not replay the enabled capacity model/calibration/template/inference cells or recompute CDF mapping/metrics. This falls short of FR-SE-06 and artifact-schema §12, which require reader replay and rejection of self-consistent semantic drift. | `read_sample_efficiency_controls()` revalidates M4/M5 inputs at lines 331-336, reads `capacity-control.json`/`cdf-check.json` at line 343, checks only each top-level digest at lines 344-348, checks only the upstream prefix at lines 349-352, and then returns the loaded payload at line 358. It never re-runs `build_capacity_control_plan()`, never checks capacity trailing upstreams against `capacity["cells"]`, never re-reads capacity model runs, and never recomputes `mapping`, `thresholds`, `metrics`, `template_id`, `inference_result_id`, or `w68` for enabled cells. | Make `read_sample_efficiency_controls()` a replaying reader. For capacity, rebuild the registered plan, require exact cells, require exact trailing capacity-model upstreams, re-read each capacity model with the capacity architecture variant, and recompute/bind calibration, template, inference ID and W68. For CDF, replay the raw M4 cell reader, physical mapping, mapped thresholds, and `cdf_metrics()` for every registered cell, then compare the expected canonical payload to the artifact. Add tamper tests that rewrite enabled `capacity-control.json` or `cdf-check.json` with a recomputed top-level ID and verify the reader rejects it. |
| Medium | Risk | `neural/src/research/sample_efficiency_confirmation.py:262` | Confirmation accepts external evaluation receipts as self-attested JSON shape rather than receipt-bound artifacts, yet still publishes CI numbers and `rule_satisfied` for `controlled_mc` inputs. The final status is correctly forced to `external_pending`, but the artifact can still carry arbitrary untrusted numeric estimates from a self-consistent package. | `_validate_payload()` only checks that `evaluation_artifact_receipts` is a nonempty list with `artifact_id`, `stage`, `sha256`, and `pointer` fields and that its digest equals the preclaim `evaluation_package_id` at lines 262-273. It does not open or verify the referenced artifacts, does not replay W68 pointers, and does not bind receipts to the M4/M5 upstreams passed to `publish_confirmation()` at lines 320-324. `publish_confirmation()` then computes `estimate`, CI bounds, and `rule_satisfied` from those payload differences at lines 415-443 even when `source_kind == "controlled_mc"` and status is `external_pending`. | Until M7 supplies a trusted evaluation-package reader, reject `controlled_mc` confirmation packages before payload numeric decoding or publish an external-pending artifact without CI/rule fields derived from untrusted differences. If controlled-MC payloads must be structurally accepted in M6, require a dedicated receipt-bound reader interface that opens the package artifacts, validates stages/receipts/population, and replays each replicate W68 before any numeric fields are written. |

## Checks Performed

- Inspected the working-tree diff and the new M6 files listed in the request.
- Reviewed FR-SE-06, sprint M6-01, document review-confirm, `AGENTS.md`, `neural/AGENTS.md`, and artifact-schema §12.
- Did not inspect or modify `xgboost/`.
