# Sprint M3-01 Document Review by GPT-5.5

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Executability / Consistency | 1. `FR-SE-03` §影响范围/§需求描述 10/§不纳入范围；`sprint-m3-01` §3/§5.2/§4 | M3 simultaneously requires `workflow.py`/downstream integration and says no CLI parameters or existing run script changes, leaving no executable ingress for producing a v2 `train` run through the current workflow. This makes the requested raw calibration/template/model-self lineage propagation ambiguous: tests could only hand-build v2 model payloads, while the actual `execute()` train stage still has no way to load a training-subset run/fraction/draw. | FR-SE-03 includes `src.research.workflow`, calibration, templates, and inference in scope and requires lineage propagation into raw calibration/template/model-self inference (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:9`, `../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:38`). The Sprint also includes `workflow.py` and requires downstream lineage (`../3-Plan/sprint-m3-01.md:29`, `../3-Plan/sprint-m3-01.md:72`). But FR-SE-03 excludes adding CLI parameters and modifying `h4l_run.py` (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:59`). The current workflow train stage only calls `train_discriminant(... candidate=args.candidate, seed=args.seed, target_lambda=args.strength, groups=args.groups)` and has no subset args or loader hook (`../../src/research/workflow.py:310` in the current implementation). The overall development plan originally expected train-stage subset CLI parameters (`../sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:225`). | Decide the M3 integration boundary explicitly. Either keep M3 as pure service/API work and remove `workflow.py`, calibration/template/inference path integration from this Sprint, or allow the research workflow train stage to accept a minimal internal subset selection input path/fraction/draw while still leaving `h4l_run.py` batch orchestration to M4. Add focused tests for the chosen ingress path. |
| High | Binding / Security | 2. `FR-SE-03` §需求描述 2–4; M1 freeze contract; M2 plan payload | The proposed v2 training API does not carry enough freeze evidence to enforce the compact representation binding it requires. `TrainingSubset` is specified to carry M2/subset identities and summaries, while `train_discriminant()` receives `training_subset`, `sample_efficiency_protocol`, and `representation_id`; neither input is guaranteed to include the validated compact freeze candidate groups, ordered inputs, and input dimension needed to reject a forged or mismatched compact representation. | FR-SE-03 requires compact ID to map to `candidate M3` with groups exactly equal to the freeze candidate groups, and ordered inputs must match overlay/freeze (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:32`). M1 says the freeze candidate owns `groups`, `ordered_inputs`, and `input_dimension`, all recomputed from the registry (`../1-Requirement/Done/FR-SE-01-sample-efficiency-contracts.md:93`, `../1-Requirement/Done/FR-SE-01-sample-efficiency-contracts.md:95`). M2 plan entries carry compact freeze artifact ID and SHA, but not the freeze candidate payload itself (`../../src/research/training_subsets.py:204`, `../../src/research/training_subsets.py:207`). FR-SE-03’s `TrainingSubset` payload list mentions M2/prepared/population/overlay/subset/membership identities, `summary_by_label`, and `summary_total`, but not freeze candidate groups or ordered inputs (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:30`). | Add `compact_candidate_freeze_artifact_id`, `compact_candidate_freeze_sha256`, and a validated freeze candidate descriptor (`representation_id`, `groups`, `ordered_inputs`, `input_dimension`) to `TrainingSubset`, or require a validated `CompactCandidateFreeze` object as part of the v2 parameter group. Then state that `train_discriminant()` recomputes representation features from that descriptor before training. |
| High | Requirement / Correctness | 3. `FR-SE-03` §需求描述 10; `sprint-m3-01` §5.2 | The downstream lineage contract is too abstract to implement safely across calibration, templates, and model-self inference. It says to copy and verify lineage but does not define the exact payload fields for each artifact, how template entries prove all candidates belong to the same subset/fraction/draw/network cohort, or how model-self inference results bind back to the template artifact and v2 model lineage. | Current raw calibration bundles are assembled in workflow from `model.json` and store `model_id`, `candidate_id`, `seed`, and legacy key before optional transform changes (`../../src/research/workflow.py:407`, `../../src/research/workflow.py:429`). Template entries currently add `mapping_id`, `candidate_id`, `seed`, `prepared_artifact_id`, and luminosity (`../../src/research/workflow.py:457`, `../../src/research/workflow.py:461`). Non-assessment inference writes per-template results with status, Asimov, and seed, while the pyhf model binding only returns mapping/candidate IDs (`../../src/research/inference.py:25`, `../../src/research/inference.py:53`). FR-SE-03 only names a generic v2 lineage helper and says raw calibration bundle/template/model-self result must copy and verify fields (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:38`), but it does not define artifact-specific schemas or validation points. | Add an artifact-specific lineage table for M3: model payload fields; calibration bundle fields; template entry fields; inference result fields. For each, specify source artifact, copied fields, recomputed IDs, rejection rule, and whether v1 entries/M0 mass-only are allowed in the same object. This should be mirrored in `docs/sw-dev/artifact-schema.md` as part of the Sprint. |
| Medium | Correctness / Determinism | 4. `FR-SE-03` §需求描述 5–7 | The requirement says training must recompute actual membership and summaries and compare them to M2, but it does not define the exact canonical recomputation algorithm or numeric equality rule. Without that, implementers may compare pandas/numpy aggregates, Python sums, or rounded floats differently from M2 and create either false mismatches or undetected drift. | M2 defines summary semantics at group level: group signed weight is the sum of all train row physical weights, `sum_abs=sum(abs(group_weight))`, `sumw2=sum(group_weight^2)`, and m4l min/max are included (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:33`). Current M2 code computes group stats by iterating sorted identity/payload rows and using Python floats (`../../src/research/training_subsets.py:131`, `../../src/research/training_subsets.py:159`). FR-SE-03 requires actual train group set and per-label/total summaries to equal M2 selection (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:33`) and stores those summaries in v2 (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:35`), but does not say whether equality is bytewise canonical JSON, exact float equality, or a tolerance-bound comparison. | Specify a shared summary recomputation helper or byte-canonical algorithm for M3. Require sorting by canonical identity/group ID, using the same group-level aggregation as M2, and comparing canonical JSON-compatible numbers exactly after the shared helper computes them. Add tests where row order and duplicate source rows would expose mismatched aggregation. |
| Medium | Compatibility / Scope | 5. `FR-SE-03` §需求描述 8/10; current calibration transform path | The v2 ID formula hardcodes transform `"raw"`, while the same Sprint asks template and inference lineage to work through existing downstream paths. Current calibration can derive transformed candidates (`cdf`, `absolute`) and template construction consumes calibration bundles by key; the document does not state whether transformed v2 bundles are rejected in M3, inherit the raw experiment cell, or get separate transform-specific IDs. | FR-SE-03 defines `experiment_cell_id` and `pairing_id` with the literal `"raw"` transform component (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:36`) and limits lineage wording to “raw calibration bundle” (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:38`). Current workflow calibration changes candidate IDs and keys when `args.transform != 'raw'`, including CDF and absolute transforms (`../../src/research/workflow.py:419`, `../../src/research/workflow.py:429`). Template construction accepts whatever calibration runs are supplied and keys them by bundle key (`../../src/research/workflow.py:447`, `../../src/research/workflow.py:449`). | State an explicit M3 rule: v2 lineage is supported only for raw calibration and templates built from raw v2 bundles, with non-raw v2 calibration rejected until FR-SE-06/M6; or define transform-specific lineage IDs now. Add a test that a v2 model with `--transform physical/absolute` is rejected or assigned the declared transform identity. |
| Medium | Test Plan | 6. `sprint-m3-01` §7; `FR-SE-03` §验收要点 | The verification plan names focused tests but does not include workflow/downstream guard tests even though the FR scope includes workflow, calibration, templates, and inference lineage. This leaves the highest-risk compatibility boundary under-specified. | Sprint focused verification lists `test_training_subsets.py`, `test_discriminants.py`, and a new `test_sample_efficiency_training.py` (`../3-Plan/sprint-m3-01.md:96`). FR-SE-03 acceptance requires raw calibration, template, and model-self inference v2 lineage plus replacement-field failures (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:71`). Existing workflow guard tests cover assessment and population safety, but M3’s plan does not name an equivalent focused downstream lineage test module. | Add focused tests such as `tests/research/test_sample_efficiency_lineage.py` or explicitly include workflow guard tests in the M3 focused command. Cover calibration bundle lineage copy, template entry lineage validation, model-self inference result binding, v1/v2 mixed behavior, and one field-replacement failure at each layer. |
| Low | Documentation / Traceability | 7. `FR-SE-03` §前置依赖; current file locations | FR-SE-03 already references `FR-SE-02` under `Done/`, but the Sprint dependency cites only commit hashes and generic schema names. It does not name the M2 review-confirm or artifact-schema §8 as the authority for the revised M2 contract, which matters because M2’s final contract changed during review. | FR-SE-03 points to `[FR-SE-02](../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md)` (`../1-Requirement/Done/FR-SE-03-subset-bound-discriminant-v2.md:8`). The Sprint lists M2 commit `881b4f4` and schema names (`../3-Plan/Done/sprint-m3-01.md:14`, `../3-Plan/Done/sprint-m3-01.md:15`) but does not cite the final M2 confirmation or `artifact-schema.md` §8. Artifact schema §8 now contains important final constraints: public APIs re-read runs from paths, split validation, duplicate key rejection, canonical membership bytes, and JSON float `1.0` (`../sw-dev/artifact-schema.md:139`). | Add explicit dependency bullets for `docs/4-Reviews/sprint-m2-01-code-review-confirm.md` or the final M2 review-confirm document if present, and `docs/sw-dev/artifact-schema.md` §8, so implementers do not accidentally follow the earlier draft M2 semantics. |
