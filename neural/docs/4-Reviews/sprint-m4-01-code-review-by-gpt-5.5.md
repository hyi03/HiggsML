# Sprint M4-01 独立代码评审（gpt-5.5/high）

评审范围：当前未提交的 M4 文档、代码与测试，相对 commit `8bd4cb1`。对照 `FR-SE-04-learning-curve-batch-workflow.md`、`sprint-m4-01.md`、`artifact-schema.md` §10、M1—M3 已完成契约与 `AGENTS.md` / `neural/AGENTS.md`。本次为静态代码评审；按后续指令中止了长时间 E2E focused test，未把其作为通过证据。

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Correctness / Schema | `neural/src/research/sample_efficiency_workflow.py:508-652` | Batch reader 的 cell 状态机没有闭合校验，可能接受“科学 terminal 但仍带有后续 stage artifact”的自洽重签 ledger，也允许 complete cell 的 `grid_id` 为 `null`。这会破坏 artifact-schema §10 要求的“已发布 stage facts、terminal/blocked stage、四项计数”和 M4 review-confirm 中接受的状态矩阵。 | Reader 逐个读取所有非空 `stage_artifact_ids` 后，只在 complete 分支检查四个 artifact 都存在、`terminal_stage is None`、`blocked_stages` 为空和 metric sources；在 terminal 分支仅检查 `terminal_stage` 合法、`blocked_stages` 序列、metric sources 为 null、status 在允许集合内。它没有断言 terminal stage 之后的 artifact 必须为 `None`、terminal stage 之前的 artifact 必须按阶段存在，也没有在 complete 分支要求 `outcome["grid_id"] == grid["grid_id"]`。外层 summary 又按 ledger 重算，因此 receipt 自洽的语义篡改可能被读者接受。 | 在 `read_learning_curve_batch()` 中集中实现 stage matrix validator：`complete` 必须四个 artifact 全部非空且 row `grid_id` 等于 common grid；`scientific_terminal` 必须只允许 terminal stage 之前的成功 artifact 非空、terminal stage 及 blocked stages 的 artifact 形态与写入语义一致，并要求 grid_id 只在共同 grid 已发布后的 terminal 中出现。增加 receipt 自洽重签负例测试。 |
| Medium | Identity | `neural/src/research/sample_efficiency_workflow.py:211-217`; `neural/docs/sw-dev/artifact-schema.md:157`; `neural/docs/4-Reviews/sprint-m4-01-review-confirm.md:25` | `execution_plan_id` 把 `progress`/`--no-progress` 绑定进 ID，超出已确认契约。执行 ID 应绑定完整 scientific plan、resolved output root/name 与 resources；进度显示是 UI/运行时表现，不应改变同一输出与资源下的执行身份，也会影响 `--clean` 对 marker 的匹配。 | Plan 的 `execution` 字段含 `"progress": bool(progress)`，随后 `execution_plan_id` 对除自身外的完整 plan 做 digest，因此同一批次、同一 output root/name、同一 resources 仅切换 `--no-progress` 会得到不同 execution plan ID。artifact schema §10 写明 execution plan ID 绑定 resolved output 与 resources；review-confirm 接受项也只说后者绑定 resolved output/resources，科学 ID 与资源/路径分离。 | 将 `progress` 从 canonical plan/`execution_plan_id` 中移出，或至少从 digest preimage 排除；保留为 CLI 本地选项或非身份 context。为 `--no-progress` 与默认进度生成相同 execution ID 添加测试，同时确认 scientific ID 不变。 |
| Medium | Security / Clean | `neural/src/cli/sample_efficiency.py:57-90` | `clean_unstarted_batch()` 的安全性依赖调用方已传入规范化 target；函数内部使用 `.absolute()` 和 lexical `relative_to()`，没有显式拒绝 `..`、没有 final resolved commonpath 校验，也没有限制为 CLI 生成的直接 child target。虽然当前 `main()` 用 safe `output_name` 构造 target，但该 helper 是公开导入函数，代码自身没有完整的 Windows 删除证明。 | `target = Path(target).absolute()`、`root = Path(allowed_root).absolute()` 后立即用 `target.relative_to(root)` 建立包含关系，最后 `shutil.rmtree(target)` 删除。与 `RunTransaction` 一样，lexical path 可能在存在 `..` 或非常规路径拼接时通过前缀检查；本函数没有像发布路径那样在删除前把 resolved target 与 resolved root 做 `commonpath` 复核。 | 在 clean helper 内部独立完成删除前证明：拒绝 `..` path parts；使用 `root.resolve(strict=True)` 和 `target.resolve(strict=True)`；用 `os.path.commonpath` 确认 resolved target 位于 root 下且不是 root；按 M4 CLI 契约要求 target 为 `root / output_name` 的单层 child；删除前对即将传给 `rmtree` 的 resolved path 再做一次相同断言。补充 direct-helper 负例测试。 |
| Medium | Gate / Strict Schema | `neural/src/research/sample_efficiency_workflow.py:68-89` | T1/G1 gate validator 只做最小字段检查，未执行 exact schema。它接受含任意额外字段的 T1 snapshot，也不校验 `g1.json` 的 `allowed_roles`/`reasons` 形态；这让无语义影响的额外字段改变 batch identity，或让格式异常的旧 gate 被当作 M4 前置证据。 | `_validate_t1_gate()` 比较外部 evidence 与 gate snapshot 的 canonical bytes，并检查 `status/correlation/auxiliary/modifier/pyhf_version/evidence_id`，但没有 `set(evidence) == {...}`。对 `g1.json` 只检查 `type(g1) is dict`、`status == "passed"`、`assessment_used is False`，未要求 allowed roles 固定为 calibration/template、reasons 为空或无未知字段。review-confirm 已接受 gate/T1 来源进入 identity 且 stage reader 使用严格 schema 的要求。 | 定义 `g1.json` 与 `t1-validation.json` 的 exact key set、类型和固定值；拒绝额外字段或缺字段。若 T1 evidence 需要可扩展元数据，应把扩展字段版本化进 schema 并让 `run_asimov()`/`build_model()` 也消费或明确忽略同一规范字段。 |
| Medium | Test | `neural/tests/research/test_sample_efficiency_workflow.py:42-106`; `neural/tests/research/test_h4l_learning_curve_script.py:12-44` | M4 focused tests 主要覆盖 happy path、一个 receipt tamper、parser surface 和简单 clean；缺少本 Sprint 最关键的 receipt 自洽语义篡改、plan-only CLI 零写入、clean Windows 路径边界、T1/G1 schema 负例和状态机矩阵测试。当前测试不足以证明 strict schema/ID、clean safety、gate/T1 和 M5 可消费 ledger。 | Workflow test 只篡改 ledger 的 `experiment_cell_id`，这会被明显身份比较拦截；没有重写 manifest receipt 的语义攻击测试。Script test 只运行 `--help`，没有 `python -m src.cli.sample_efficiency --plan-only` 的 no-write 断言，也没有 `--clean` 的 CLI 路径和 marker mismatch 覆盖。 | 增加 targeted unit tests：自洽重签 ledger 中 terminal/complete artifact matrix 和 `grid_id` tamper；T1 evidence/g1 extra/missing/wrong allowed_roles；CLI plan-only 输出 canonical plan 且不创建 output root/target；clean helper/CLI 拒绝 `..`、junction/symlink/reparse、非直接 target、marker ID mismatch、任意 manifest/failure evidence。 |
| Low | Resource / Scope | `neural/src/research/sample_efficiency_workflow.py:92-98`; `neural/src/cli/sample_efficiency.py:143-147` | M4 固定拒绝 `workers != 1`，但仍把 `worker_threads/root_max_entries/root_threads` 纳入 resources 和 execution identity，且执行路径没有使用这些值。未使用资源键会让 execution ID 因无效参数变化而变化，容易被误认为执行语义不同。 | `_validate_resources()` 只验证四个键为正整数并固定 `workers=1`；`execute_learning_curve()` 没有将 `worker_threads/root_max_entries/root_threads` 传入训练、ROOT 或 pyhf 调用。FR-SE-04 允许 resources，但 review-confirm 将 M4 缩小为 workers=1 reference execution。 | 对 M4 reference path 只保留真正生效的资源字段，或在 schema 中明确这些字段为“保留但不影响当前执行”，并从 execution ID 中排除未使用字段。若保留在 ID 中，应在代码中把它们实际传递到对应执行边界，或增加测试证明改变这些值不会被误解为科学/执行差异。 |
