# Sprint M6-01 独立文档评审（gpt-5.5/high）

## 评审范围

- 需求：`neural/docs/1-Requirement/FR-SE-06-controls-and-confirmation.md`
- Sprint：`neural/docs/3-Plan/sprint-m6-01.md`
- 对照：sprint-workflow、样本效率主开发计划、M1—M5已实现接口与artifact reader/publisher边界、scientific boundary、验收与追踪性。

结论：M6文档已经覆盖核心方向，但仍有若干会阻塞安全实现或验收的契约缺口。以下意见均应在实现前写回FR/Sprint或artifact schema，避免实现阶段自行解释科学与可信边界。

| ID | Severity | File / Location | Finding | Evidence | Actionable recommendation |
|---|---|---|---|---|---|
| M6-DOC-001 | High | `FR-SE-06-controls-and-confirmation.md` §容量控制 lines 9-11；`sprint-m6-01.md` lines 15, 24 | capacity模型与现有M3/M4模型reader接口的兼容边界未定义。文档要求训练唯一`match_engineered19_parameter_count_v1`变体并发布模型/指标，但没有说明这是扩展`research-discriminant-v2`、新增capacity-only模型schema，还是新增只由capacity reader消费的受控训练路径。 | 当前实现中`sample_efficiency_training.py`固定`ARCHITECTURE_VARIANT = "baseline-fixed64x64x32"`，`validate_discriminant_v2()`按`[d,64,64,32,1]`和`ResearchClassifier(d)`重算参数量，`_train_model()`拒绝非baseline architecture。M6 FR line 11又要求capacity artifact绑定“模型/指标”和实际参数量。若文档不先定义接口，实现在满足capacity训练时可能会放松M3 reader，或生成无法由可信reader验证的模型。 | 在FR和artifact schema §12中明确capacity模型的artifact surface：模型schema/version、allowed architecture variant枚举、width解析与实际参数量重算函数、`experiment_cell_id/pairing_id`是否沿用或新增包含capacity variant的ID规则、哪些现有reader必须继续拒绝capacity模型、capacity专用reader如何从M2/M3/M4直接上游重放验证。 |
| M6-DOC-002 | High | `FR-SE-06-controls-and-confirmation.md` §Confirmation input与identity-first边界 lines 21-23、§预注册CI与确认payload lines 33-35；`sprint-m6-01.md` lines 17-18, 27-29 | confirmation payload把关键科学证据压缩成“paired W68 difference数组”和未定义的evaluation receipts，无法证明这些差值确实来自冻结compact/reference、独立assessment population、预注册event-group bootstrap unit/seed/algorithm。 | M1协议字段已冻结`ci_algorithm=paired_event_group_percentile_v1`、`bootstrap_unit=event_group_id`、seed、replicate数和`D=W68(C)-W68(F)`语义；M6 FR line 33只要求差值数量等于`bootstrap_replicates`且有限，line 21仅说payload含`evaluation artifact receipts`，没有给出receipt schema、源artifact reader、replicate multiplicity plan或每replicate compact/reference W68的可重放绑定。这样一个自洽外部文件可以填入任意有限差值并通过CI计算。 | 将`h4l-noninferiority-evaluation-input-v1`拆成可验证的exact schema：至少绑定独立evaluation artifact的schema/version/artifact IDs、compact/reference model或prediction/template receipts、canonical event-group ordering、PCG64 multiplicity plan ID或完整multiplicity digest、每replicate两侧W68来源与差值重算规则。confirmation reader必须从这些字段重算或逐receipt验证差值数组；若M6只做external-pending synthetic路径，也要显式标注“不构成科学确认”并禁止发布`confirmed_*` scientific claim。 |
| M6-DOC-003 | High | `FR-SE-06-controls-and-confirmation.md` §Confirmation input与identity-first边界 lines 21-23、§独立性与durable claim lines 27-29；主开发计划 §10.2 lines 390-396 | claim-before-decode所需的preclaim identity/header字段不够完整，导致claim key和独立性检查的输入来源不闭合。文档说payload decode前要拒绝非独立population，并让claim key覆盖base/overlay/freeze/delta/confidence/CI设置，但preclaim可读区域只明确了identity五个键，payload才包含base/overlay/freeze digest和bootstrap配置。 | FR line 21把identity exact keys限定为`event_group_id,label,role,split,dataset`，payload exact才记录base/overlay/freeze digests、bootstrap配置和receipts。FR line 29要求claim key覆盖这些payload级字段，并在decode前持久化claim。若实现从payload取这些值就违反claim-before-decode；若从CLI/config取值，则必须在文档中说明header/config/freeze与payload的双向校验关系。 | 明确第一行canonical header的exact keys，并把claim key所需的所有非payload输入来源列清楚：dataset、base digest、overlay digest、freeze artifact/digest、delta/confidence/CI算法/bootstrap unit/count/seed、evaluation file SHA、schema/version。规定preclaim只能使用header+identity+本地已验证freeze/config形成claim key，decode后payload必须逐字段等于preclaim值，否则发布claim后失败run。 |
| M6-DOC-004 | Medium | `FR-SE-06-controls-and-confirmation.md` §精简physical CDF检查 lines 15-17；`sprint-m6-01.md` lines 16, 26 | CDF检查的artifact reader/publisher契约仍停留在指标描述层，没有冻结文件surface、direct upstream、多阶段重放和支持失败的exact状态/nullability。 | M4/M5已实现reader都要求直接upstream、manifest receipt、exact schema、逐字节/语义重放，并通过`read_learning_curve_batch()`和`read_sample_efficiency_report()`重新验证上游。M6 FR line 17只列出cell绑定字段和两个指标，Sprint line 20仅说更新artifact schema §12；尚未定义`cdf-check`运行目录、JSON/JSONL文件名、disabled零payload形态、terminal状态枚举、分母为零时的字段nullability、以及reader如何重放`fit_calibration(target="physical")`、mapping应用和template acceptance。 | 在FR或artifact schema §12补全`h4l-sample-efficiency-cdf-check-v1`的exact contract：run stage、文件列表、direct upstream为哪些M4 cell/batch/report artifacts、逐cell keys、metric算法ID、有效分母字段、terminal枚举、disabled schema、reader重放步骤和状态到exit code的映射。验收测试应覆盖receipt自洽篡改、分母为零、support failure和raw主report不变。 |
| M6-DOC-005 | Medium | `FR-SE-06-controls-and-confirmation.md` §CLI与输出 line 41；`sprint-m6-01.md` lines 19, 30 | controls/confirmation CLI的exact config没有继承M5级别的完整上游绑定字段，可能不足以保证dataset、prepared、training-subsets、gate/T1、batch、report、freeze之间的一致性。 | M5 CLI config固定列出dataset/protocol/sample_efficiency_protocol/prepared/compact_freeze/training_subsets/gate/T1/batch/output并在publisher中重跑M4 reader和M2 reader。M6 FR line 41只列出dataset/base/overlay/freeze/batch/report/confirmation input/output root/name/repeat，缺少prepared、training-subsets、gate run、T1 validation等M4/M5 reader重放所需输入来源，且`--controls-only`仍需要M4 verified raw cell lineage。 | 扩展M6 exact config字段到足以调用M4/M5 public readers并重验完整链路，或明确这些字段由report artifact reader可靠封装且不需要再次传入。CLI测试应验证跨dataset、跨prepared、替换batch/report、替换T1 evidence、路径自洽但语义漂移的拒绝。 |
| M6-DOC-006 | Medium | `sprint-m6-01.md` §4 测试 lines 24-30；`FR-SE-06-controls-and-confirmation.md` §验收 lines 45-50 | 验收测试列表缺少若干会直接保护scientific boundary的负例，特别是payload poison、post-claim drift和capacity/CDF隔离回归。 | FR已声明capacity/CDF不能进入candidate freeze、M5主曲线或confirmation，confirmation不能在claim前decode payload，claim后失败不可作为新设计重试；但Sprint测试只覆盖若干身份错误、claim文件可见和CI边界，尚未要求测试capacity结果不能被M5/report/confirmation消费、CDF physical mapping不能替换raw inference、decode sentinel中带有会改变候选/容差的payload被拒绝、claim后payload/config drift生成失败run且不能重新选择。 | 补充可执行验收项：assessment/payload poison sentinel、payload中candidate/delta/bootstrap drift、claim后失败run保留与repeat拒绝、capacity artifact试图进入M5/confirmation被拒绝、CDF artifact试图替换raw W68被拒绝、registered-coordinate之外的capacity/CDF cell全链拒绝、Windows synthetic与external pending标签不可升级为scientific confirmation。 |

## 接受前建议

建议先把以上6项写回FR、Sprint和计划中的artifact schema §12，再进入实现。特别是M6-DOC-001至M6-DOC-003会影响代码结构和安全边界，应作为实现前置条件处理。
