# Sprint M4-01 Document Review by GPT-5.5

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Requirement / Executability | 1. `FR-SE-04` §范围 2–6; `sprint-m4-01` §5.1 | `--plan-only` is required to produce a canonical batch plan containing M2 subset identities and per-cell experiment/pairing identities, but the CLI has no `--training-subsets-run` input and `--plan-only` is forbidden from creating runs or loading event payload. M2 subset IDs cannot be known from only prepared/freeze/overlay without running M2, and M2 generation decodes train payload weight/mass for summaries. | FR-SE-04 says CLI args include prepared/freeze but not an existing training-subsets run (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:21`). It says `--plan-only` must not create/modify directories, load event payload, train, or initialize RNG (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:22`). The same FR requires the plan to include “M2 subset plan identity” and every cell’s subset/experiment/pairing identity (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:24`). M2’s final contract computes subset membership and summaries from prepared `events.jsonl`, including train payload `physical_weight` and `m4l` (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:29`, `../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:33`). | Pick one executable contract: add `--training-subsets-run` as a required or optional input for plan-only, or split plan-only into a pre-M2 coordinate plan that omits subset/model IDs. If plan-only may compute M2 in memory, explicitly allow the required train payload read and state it writes nothing. Update tests to prove the chosen mode. |
| High | Binding / Schema | 2. `FR-SE-04` §范围 7–10; M3 artifact schema §9 | The batch plan/ledger does not define enough schema to verify calibration/template/inference stage artifacts. It names stage directories and artifact IDs, but does not define per-stage payload filenames, IDs, required lineage fields, calibration/template/result digests, or how a reader recomputes those identities from the M3 verified handles. | M3 artifact schema §9 defines `RawCalibrationBundle`/template/model-self result lineage and digest expectations (`../sw-dev/artifact-schema.md:153`). FR-SE-04 says each stage has its own manifest and readers verify receipt/stage/upstream/payload identity (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:28`) and that successful cells record four stage artifact IDs, common grid ID, lineage, and W68/AUC input locations (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:29`). It does not specify the actual stage schemas, filenames, or digest fields for calibration/template/inference. | Add an M4 artifact schema table for `sample-efficiency-calibration`, `sample-efficiency-template`, `sample-efficiency-inference`, and `sample-efficiency-batch`: filenames, schema IDs, required upstreams, digest fields, lineage source, and reader recomputation rules. |
| High | Safety / Gate Evidence | 3. `FR-SE-04` §范围 14; `sprint-m4-01` §2 | `--gate-run` and `--t1-validation` are required, but their accepted source, schema, and binding rules are underdefined. In particular, the docs say gate must bind the same prepared/freeze/base and assessment-not-used, but current M3/M2 contracts do not produce a dedicated sample-efficiency gate artifact, and the old research workflow G1/templates/freeze artifacts have different candidate-matrix assumptions. | FR-SE-04 requires `--gate-run` and `--t1-validation` (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:21`) and says gate/T1 evidence only verifies existing frozen results; gate must declare assessment unused and bind the same prepared/freeze/base (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:33`). The development plan explicitly warns that existing `templates` stage G1 has a required minimum for the old candidate matrix and cannot be directly reused for the three-representation learning curve (`../sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:301`). The Sprint dependency just says “gate与T1 evidence” without schema (`../3-Plan/sprint-m4-01.md:11`). | Define the exact accepted gate artifact: stage name, files such as `g1.json`/`t1-validation.json`, upstream requirements, prepared/freeze identity checks, and whether old G1 is only a prerequisite evidence or a new sample-efficiency gate must be built. Add negative tests for wrong prepared, wrong freeze, assessment-used evidence, and old G1 candidate-matrix confusion. |
| Medium | Correctness / Batch Semantics | 4. `FR-SE-04` §范围 7–8, §失败语义; development plan §8.2 | The common template grid failure behavior is ambiguous. The docs say one shared grid is built from all successful cells, weak units are not deleted, and common-grid failure becomes a named batch terminal, but they do not define whether already successful model/calibration cells remain complete, whether every planned inference/template cell receives a terminal ledger status, or what the outer `sample-efficiency-batch` run status is. | FR-SE-04 requires a single batch common grid and says failure produces a named batch terminal without deleting weak units (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:26`). Failure semantics say common template failure uses `insufficient_statistics` or `template_stat_model_unvalidated` and forms a batch terminal (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:53`). Ledger completeness requires every planned cell to have exactly one terminal state (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:29`, `../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:30`). | Add a state matrix for batch-level terminal versus per-cell terminal. Specify how model/calibration successes are preserved, which later stages are marked blocked/skipped, whether outer manifest status is `complete` or a `ResearchStateError` status, and how `sample_efficiency_completeness` treats grid-level failure. |
| Medium | Security / Destructive Operation | 5. `FR-SE-04` §范围 12; `sprint-m4-01` §5.1 | The `--clean` proof is directionally safe but not precise enough for a destructive implementation. It does not define the allowed file set under a batch target, how to identify “unpublished/failed”, what to do with partial staging directories, or the behavior when `--clean` and `--plan-only` are both supplied. | FR-SE-04 allows clean only under output-root with matching `batch-plan.json`, matching batch ID, failed/unpublished status, and no complete research manifest (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:31`). Sprint tests list dangerous clean cases but not the proof algorithm or allowed directory contents (`../3-Plan/sprint-m4-01.md:28`, `../3-Plan/sprint-m4-01.md:30`). Repository instructions require preserving frozen/failed runs and generated evidence (`../../AGENTS.md:29`, `../../AGENTS.md:78`; `../../neural/AGENTS.md:38`). | Add a clean contract: mutually exclusive flags, allowed states, exact marker files, allowed child paths, handling of staging dirs, path canonicalization, symlink checks at every component, and refusal for unknown files. Require dry-run/plan output for clean proof in tests before deletion. |
| Medium | Concurrency / Determinism | 6. `FR-SE-04` §范围 13; development plan §8.3 | The multiworker contract does not state how workers receive verified inputs without sharing mutable run publication state, nor how worker failures are serialized into deterministic ledger entries. This is risky because cells perform training and inference with RNG and large payloads, while parent-only publication is required. | FR-SE-04 says one parallel layer, deterministic `ordered_map()`, parent publishes, and workers must not publish shared runs (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:32`). The development plan says workers return pure results or temporary payload and parent publishes artifacts in plan order (`../sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:305`, `../sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:310`). The M4 FR does not define worker result schema, temp storage policy, RNG/thread setup, or how failures are captured without producing partial manifests. | Define a worker-result schema and temp payload policy before implementation. Require tests comparing `workers=1` and `workers>1` plans/ledger identities, including one deterministic failure cell. State that M4 may initially enforce `workers=1` if multiworker proof is deferred. |
| Medium | Scope / Verification | 7. `FR-SE-04` §最小验证; `sprint-m4-01` §7 | The focused verification omits existing M3 lineage tests and low-level inference/template tests even though M4 composes those APIs into batch artifacts. This weakens regression protection for the exact provenance and assessment-closure boundaries M4 depends on. | FR-SE-04 focused command includes workflow/script/training_subsets/training tests (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:60`). Sprint focused command includes the same reduced set (`../3-Plan/sprint-m4-01.md:51`). M3’s accepted verification explicitly included `test_sample_efficiency_lineage.py`, `test_calibration.py`, `test_templates.py`, and `test_inference.py`, because those protect raw lineage and model-self boundaries (`../3-Plan/Done/sprint-m3-01.md:97`). | Include M3 lineage and downstream tests in the M4 focused command, or add equivalent coverage to `test_sample_efficiency_workflow.py`. At minimum include `tests/research/test_sample_efficiency_lineage.py`, `tests/research/test_templates.py`, and `tests/research/test_inference.py`. |
| Low | Clarity / User Interface | 8. `FR-SE-04` §范围 2, §输出; `sprint-m4-01` §5.1 | The distinction between batch `--config`, `--resources`, and scientific `--sample-efficiency-protocol` is not fully defined. The docs say science grid only comes from protocol, but do not list which non-scientific settings may be in batch config versus resources, making it easy for implementation to accidentally allow cell filtering, seed override, or path-derived science. | FR-SE-04 says CLI has `--config`, `--resources`, and protocol, and paths/resources come from args/config while scientific grid only comes from verified protocol (`../1-Requirement/FR-SE-04-learning-curve-batch-workflow.md:21`). Sprint risk control says CLI/config cannot override fraction/seed/representation/architecture (`../3-Plan/sprint-m4-01.md:66`). No schema lists allowed config keys. | Add a strict batch config schema with allowed non-scientific keys only: output naming, progress, worker counts/resource references, optional synthetic test limits if any. Explicitly reject fractions, draws, network seeds, representations, architecture, thresholds, or grid controls in batch config. |
