# Sprint M2-01 代码评审（gpt-5.6-sol）

评审范围为 `src/research/training_subsets.py`、`tests/research/test_training_subsets.py`、`docs/sw-dev/artifact-schema.md` §8 及当前 M2 文档契约。对照 FR-SE-02、FR-SE-01、现有 `h4l-events-v1`/M1 loader 和 `ResearchRun`/`LoadedRun` 语义；只评审 M2，不涉及 M3 训练或后续批处理。本轮按要求未运行测试。

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Contract correctness | **F-01** — `training_subsets.py:107-119,148-155`；artifact-schema §8 | 发布的 plan 缺少 FR 要求的每个 subset 总计摘要，只有两个逐 label 摘要，因此 schema 实现不完整，M3/后续报告不能从受绑定 payload 直接取得总组数、总行数和总体权重支持。 | FR-SE-02 需求 6 明确要求“每subset逐类与总计”。`_summary()`只循环 label 0/1 并返回 `summary_by_label`；subset entry 没有 `summary_total`。测试第 132–133 行自行对两个 label 求和，仅验证行数大于组数，没有断言总计 payload 或各权重公式。artifact-schema §8 也只描述“逐类”摘要，遗漏了 FR 的总计。 | 增加规范化 `summary_total`，按 FR 的事件组级公式从全部选中组重算 group/row count、signed/absolute/sumw2/neff 和 m4l min/max；reader 等值重算。补充精确数值测试，覆盖同组正负行抵消，并同步 artifact-schema §8。
| High | Input validation | **F-02** — `training_subsets.py:44-77` | M2 reader 接受不符合现有 prepared `h4l-events-v1` 身份契约的流：允许同一文件混合 `development` 与 `exploratory` split，且不拒绝重复 `event_id`。攻击者可用 `ResearchRun` 发布自洽 receipt，再让 overlay 绑定重算 population，从而绕过“已验证 prepared run”的前提。 | `_read_train_rows()`只检查 `split in {development, exploratory}`，而现有 `data._research_split()`要求由 base protocol 决定的唯一 split，`load_research_data()`也精确比较该值。代码只用 `seen_rows`检查 `source_row_id`，现有 `_validate_identity()`同时拒绝重复 `source_row_id` 和 `event_id`。当前测试全部固定 `development` 且 event ID 唯一，没有负例。 | 复用或抽取现有 identity validator，要求每行 split 精确等于该 base protocol 的 `_research_split`，同时拒绝重复 `event_id` 和 `source_row_id`。增加混合/错误 split、重复 event ID、重复 source row ID 测试，并断言 `training_subset_binding_mismatch`。
| High | Security / binding | **F-03** — `training_subsets.py:170-178,195-207` | freeze 重建绕过了 M1 的严格文件 loader；带重复 JSON key或非规范 JSON 的 `freeze.json` 可在 receipt 自洽时被接受。传入现成 `LoadedRun` 时也不会重新执行 `read_run()` 的 manifest digest、dataset、protocol、stage/status 校验。 | `freeze_run.read_json("freeze.json")`使用通用 `json.loads`，先丢失重复 key，再把 dict 交给 `CompactCandidateFreeze`；M1 的 `load_compact_candidate_freeze()`才使用 `_unique_object`拒绝重复 key。类似地，`read_training_subsets()`仅在 `run` 不是 `LoadedRun` 时调用 `read_run()`；`build_training_subset_payloads()`和 publisher 也把 `LoadedRun` 类型本身当作已验证证明，但该 dataclass 可直接实例化。FR 要求 publisher/reader验证 freeze receipt并重建 M1 freeze，并要求输入通过 `read_run` 验证。 | 对 prepared、freeze 和 output run 都从其 `.path` 重新调用 `read_run(..., stages=(精确stage,))`，再使用重载对象；freeze 文件用 `load_compact_candidate_freeze(freeze_run.file("freeze.json"), base_protocol=base)`解析。增加重复 key freeze、伪造 `LoadedRun` manifest digest、错误 dataset/protocol/status/stage 的负向测试。
| Medium | Determinism | **F-04** — `training_subsets.py:136-159` | full alias ID 没有按 FR 规定规范化到浮点 `1.0`，合法 overlay 使用整数 `1` 时会哈希不同 preimage。 | FR-SE-01 的 fraction validator接受 `int` 或 `float`，且只要求末项与 1 相等；Python canonical JSON 会区分 `1` 与 `1.0`。FR-SE-02 需求 5 明确规定 full `alias_id` 绑定 `[subset_id, original_draw, 1.0]`，实现却传入原始 `fraction`。当前 fixture固定使用 `1.0`，未覆盖整数端点。 | 在 full 分支将 membership/entry/ledger 的 fraction 和 alias preimage统一规范为 `1.0`，或在 M1协议层明确拒绝整数 fraction并更新契约。增加以整数 `1` 结尾的合法 overlay 测试，断言 full ID和序列化采用规范端点。
| Medium | Canonical parsing | **F-05** — `training_subsets.py:210-217` | membership reader只比较解析后的对象，没有验证 header/record 的 canonical 行字节；空白、不同键序或重复同值 key 的非规范 JSONL在 receipt 自洽时可通过，削弱字节级确定性和严格 schema。 | publisher逐行写 `canonical(...)`，FR-SE-02 要求 canonical 排序；reader使用普通 `json.loads`后直接与 `expected_members`比较。JSON 对象键序和重复同值 key 在解析后消失，等值比较无法证明发布字节符合 canonical 格式。 | 读取时保留每行文本，使用拒绝重复 key 的 loader，并断言 `canonical(parsed).decode() == line_text`；header同样处理。增加 receipt 自洽但含空白、乱序键、重复 key 的 membership artifact 负例。
| Medium | Test coverage | **F-06** — `test_training_subsets.py:112-195`；Sprint §5.2/§10 | 测试名称和 Sprint 完成声明覆盖了尚未实际执行的验收点：没有输入行序对照；“identity conflict”测试只注入 NaN；“semantic tampering”只触发旧 receipt mismatch，未验证重签 receipt 后的内容重算；membership、ledger、overlay snapshot和 upstream替换也未覆盖。 | `_bindings(..., order_seed=...)`参数从未在测试中使用。第 176 行测试名含 identity conflict，但第 181 行仅改 `physical_weight`。第 169–173 行修改 plan 后没有更新 manifest receipt/artifact identity，异常来自 `LoadedRun.file()`的 digest 检查，未到达 reader 的 semantic recomputation。FR 验收要求行序不变、receipt/摘要篡改、角色/label冲突；Sprint §5.2已标记完成并称 focused 103 passed。 | 增加两个不同行序 prepared run的 membership/summary比较（对 prepared artifact ID 变化需比较规范化科学内容或构造相同受绑定 bytes）；分别构造 role/label group冲突；为 plan、membership、ledger、overlay和 upstream篡改生成 receipt/manifest均自洽的 artifact，验证语义重算仍拒绝。完成这些测试后再保留 Sprint 的已完成声明。

除上述问题外，主选择算法按 FR-SE-01 preimage分 label排序、以稳定 group ID打破 hash tie、采用 floor 前缀、跨 draw 去重 full，并在汇总前按 canonical identity顺序累计组权重；这些实现方向符合 M2 的确定性和 assessment 隔离要求。
