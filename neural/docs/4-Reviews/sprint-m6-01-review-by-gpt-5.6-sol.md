# Sprint M6-01 文档评审（GPT-5.6-sol / high）

评审范围为 `FR-SE-06-controls-and-confirmation.md` 与 `sprint-m6-01.md`，并对照样本效率开发总计划、已实现的 M1—M5 public readers/artifacts、artifact schema、`neural/AGENTS.md` 科学安全约束和 sprint-workflow。本次仅做静态文档评审；未运行测试、训练或读取任何研究数据。容量公式已按实际含 `m4l` 的输入维数和 `ResearchClassifier` 的 LayerNorm 参数复核，`5568/(d+67)` 与 7937 参数目标一致，不构成 finding。

| ID | Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|---|
| M6-DOC-001 | High | Experiment coordinates | `FR-SE-06`「容量控制」第 9—11 行；`sprint-m6-01` §3—4 | capacity control 没有冻结 partial-fraction 的 draw 坐标，因而“对应/同一 raw 主 cell”并不唯一，无法唯一生成计划或证明 pairing。 | overlay 的 `capacity_control` 只注册 `sample_fractions` 和 `network_seeds`；M2/M4 对每个小于 1 的 fraction 会为所有 `sample_draw_seeds` 生成不同 subset/cell，只有 full endpoint 才折叠为单一 `full`。现有 M4 cell 身份包含 `sample_draw_seed_or_full`、`training_subset_id`、`membership_digest` 和 `pairing_id`，而 FR 的 capacity “registered coordinates”没有规定遍历全部 draw、固定某个 draw，还是另设列表。不同实现可得到不同训练次数和机制结论。 | 在协议或 FR 中把 capacity cell 笛卡尔积写死：建议每个已注册 partial fraction 使用主计划的全部 `sample_draw_seeds`，full endpoint 只用 canonical `full`；每个 cell 必须复制并重验 M4 plan 的 subset artifact/ID、membership digest、draw-or-full 和 raw pairing coordinates。把 canonical 顺序、planned/canonical cell 数及 disabled 空计划也列为 exact contract 和 golden test。 |
| M6-DOC-002 | High | Public interface / lineage | `FR-SE-06`「容量控制」第 9—11 行；已实现 `sample_efficiency_training.py` 第 19—21、90—186、199—236 行 | FR 只定义一个聚合 capacity artifact，没有定义新宽度模型如何通过版本化 public builder/reader、模型 schema 和下游 lineage；现有 M3 接口会明确拒绝该 variant。 | `publish_subset_discriminant`/`validate_discriminant_v2` 仅允许 `architecture_variant="baseline-fixed64x64x32"`，并把 architecture 固定为 `[d,64,64,32,1]`，参数量由固定 `ResearchClassifier(d)` 重算；`experiment_cell_id`/`pairing_id` 的 preimage 也固定含 `"raw"` 与 architecture variant。capacity 又需要新模型、校准、template、T1 inference 和 W68，但 FR 只给顶层 `h4l-sample-efficiency-capacity-control-v1` 的概括字段，未给各 stage schema、ID preimage、direct upstream、metric pointer 或 reader replay。绕过现有 reader 直接拼 dict 会失去 M3—M5 的可信边界。 | 在实现前冻结 capacity 专用的版本化 model builder/reader（或明确扩展 v2 为新 schema），定义 generalized classifier 的精确层序列、state/scaler/checkpoint绑定、architecture/parameter-count replay、cell/pairing ID preimage和不可伪造 lineage。为 calibration/template/inference 各定义 envelope、stage、direct upstream、终态和 reader 重算；顶层 ledger 应保存每个 stage artifact ID 与 W68 pointer，并证明 capacity 结果不能进入 M5/confirmation。 |
| M6-DOC-003 | High | Scientific metric definition | `FR-SE-06`「精简physical CDF检查」第 15—17 行；总计划 §11 | 两个 CDF 指标和 `calibration_uncertainty_dominant` 判据没有可执行定义，无法得到唯一数值或客观终态。 | “calibration category target”“实际 absolute-weight acceptance”“background mass-bin acceptance 相对 raw 形变”未规定 category thresholds 从 raw bundle复用还是在 physical score 上重新拟合、target 是每类 0.5 还是协议内其他量、分子/分母、按质量 bin 还是全局、先绝对值还是先求最大、raw acceptance 为零时的处理，以及负 `physical_weight` 和 event-group 聚合方式。现有 `fit_calibration(target="physical")` 用 signed weight 拟合 mapping，而 FR 的第一指标改用 absolute weight；现有 raw template builder 又只接受 `RawCalibrationBundle`。文档也没有冻结何种 support 数值触发 `calibration_uncertainty_dominant`。 | 给出逐项公式和 golden vector：mapping 后如何产生 category、每个 category/质量 bin 的目标、absolute-weight 分母及零分母终态、raw-relative deformation 的精确归一化与 max 顺序。明确使用 base calibration 的既有 `min_effective_count`/`min_cancellation_ratio`，还是新增预注册阈值来判定 uncertainty dominant；列出 `insufficient_statistics`、`nonpositive_calibration_yield`、`outside_calibration_support` 等如何映射到该终态，禁止运行后凭观察解释。 |
| M6-DOC-004 | High | CDF provenance | `FR-SE-06`「精简physical CDF检查」第 15—17 行；已实现 M4 common-grid 与 raw calibration contracts | CDF check 的 single-cell payload不足以证明 physical mapping、分类后的 template frame、raw 对照和最终指标来自同一 verified cell，且“相同共同质量网格”没有指定是重用哪个已封存 grid artifact。 | M4 common grid 是由全部且仅 raw calibration-success cells联合构造的独立 run，manifest upstream 绑定 plan、training-subsets和参与的 raw calibration runs。physical mapping是新派生物；现有 `prepare_sample_efficiency_template_frame` 强制 `require_raw_calibration_bundle`，不能承载 physical mapping。FR 只说每 cell 绑定 raw model/calibration artifact、physical mapping ID和共同质量网格，未要求绑定 prepared calibration/template role receipt、M4 common-grid artifact ID、raw template artifact/metric source，也未定义 physical calibration/template 的独立发布与 reader 重放。 | 冻结一个明确 stage DAG：verified M4 batch/cell → prepared role + raw model → physical-calibration run → categorized-template/check run；直接 upstream 必须包括实际使用的 model、prepared、raw calibration/template和 M4 common-grid run。定义 grid 只复用 M4 `final_mass_edges` 且核对 `grid_id`，不重新按 physical cells 合并；reader 应重新预测、应用 mapping、重算两项指标并拒绝自洽重签或 artifact substitution。 |
| M6-DOC-005 | High | Confirmation provenance | `FR-SE-06`「Confirmation input」「预注册CI与确认payload」第 21—37 行；总计划 §13.1/M7 traceability要求 | confirmation 把外部预计算的 paired W68 difference 数组当作最终统计输入，却没有定义受信 producer、独立模型/模板/inference artifact拓扑或从 receipts 到数组元素的可重放关系；payload 内自报 receipts 不能证明科学来源。 | identity 区只含 event-group identity，payload 是聚合后的 bootstrap differences，两者没有逐 event/replicate 关系。M6 输出 manifest 的直接 upstream 又只规定 freeze、M4 batch和 M5 report，未包含任何 independent-evaluation run。攻击者可构造任意有限差数组和与其一起自签的 `payload_id`/receipt 字段；严格 JSON、文件 SHA 和 claim 只能证明文件未变化，不能证明 W68 来自冻结 compact/reference 模型、共同 resample plan和独立总体。这样 M7 无法沿 protocol/population/model/calibration/template/inference 追溯结论。 | 二选一并冻结：由仓库在 durable claim 后从 assessment events按已冻结 procedure计算两表示的 bootstrap W68；或定义独立、不可变、receipt-bound evaluation package，其 manifest 是 confirmation 的直接 upstream/外部信任锚，逐 replicate记录或可重放绑定 population、两模型、calibration、template、common grid、inference、multiplicity-plan ID及 W68 pointer。decoder 必须从实际 artifact receipts重验数组，而不是只校验 payload 内声明。 |
| M6-DOC-006 | High | Scientific independence | `FR-SE-06`「独立性与durable claim」第 27 行；总计划 §10.2 第 390—396 行 | 仅比较 population digest 是否相等不能排除事件部分重叠，也不能证明“换划分仍是同一总体”；当前独立性守卫会让不同 digest 的重复事件通过。 | `population_digest(groups,dataset)` 只对整个排序 group 集合取 SHA-256。候选总体 `{a,b,c}` 与 discovery 总体 `{a,b}` digest 不同，但共享事件；同一事件集合增删一个组也会变成新 ID。freeze 只保存 population ID 列表，没有旧 population 的 group membership；preclaim 虽能看到 confirmation identities，却无权威旧 identity set可作 disjointness检查。总计划明确 confirmation population 不得出现在 discovery/learning lineage，并明确同一批事件换划分仍视为同一总体，这比 digest 不等更强。 | 为每个排除 population 绑定可验证的 immutable identity-set artifact/receipt，并在 claim 前对 canonical `event_group_id` 集合做 disjointness检查；或要求由受控 dataset contract提供不可重叠 population namespace和可验证 provenance。claim key还应绑定这些 exclusion artifact IDs/digests。加入 partial overlap、superset/subset、同组不同 role/split、不同排序的 decode-sentinel测试。 |
| M6-DOC-007 | High | Claim / publication state machine | `FR-SE-06`「独立性与durable claim」「CLI与输出」第 29、37、41 行；现有 `ResearchRun` transaction contract | 文档没有保证在消耗 durable claim 之前已有一个可发布失败证据的独占输出位置；claim 后的路径冲突、进程崩溃或发布初始化失败会留下无法关联的永久 claim，却违反“任一 claim 后失败发布新的失败 run”。 | FR 要求 confirmation run 只在 payload验证和计算完成后发布，同时 claim 必须先于 decode；output name 是调用配置，且 immutable run target 只能 exclusive-create。若 claim fsync 后才发现 target 已存在、路径非法或 `ResearchRun.__enter__`/protocol snapshot写入失败，就既不能覆盖目标，也未必有 run记录失败。claim key 不含 output run identity，claim record也未定义 attempt/run链接或恢复状态，因此 reader无法区分进行中、崩溃、失败和成功。 | 冻结原子顺序：先完全验证路径并 exclusive claim一个 staging/run transaction，再在其中写 preclaim metadata；随后 durable-create global claim并把 attempt ID/output path写入 claim，再 decode。规定每个 postclaim异常如何在已占有 run中发布 terminal manifest，崩溃恢复如何识别并封存，claim 与 run如何双向绑定，以及 fsync 文件/目录或最终 publish失败时的状态。加入 output occupied、claim后立即崩溃、目录 fsync失败、final rename失败的验收。 |
| M6-DOC-008 | High | Input grammar / access boundary | `FR-SE-06`「Confirmation input与identity-first边界」第 21—23 行 | confirmation 文件格式允许零 identity，且没有冻结字节级语法和资源上限；不同 scanner/decoder可能对 payload边界产生分歧，破坏 claim-before-decode sentinel。 | 文档明确允许“零个或多个” identity 行，因此空集合能形成合法 `population_digest` 并进入 claim；但非劣性数组仍可在 payload中存在。`canonical header`/`canonical payload`没有列 exact keys、编码、BOM/newline/final newline、TAB/分隔符合法性、duplicate JSON key策略或最大行/文件/replicate大小。preclaim需要在不解析最后一行的同时计算 whole-file SHA，而 postclaim decoder必须保证读取的正是同一稳定文件；文档未要求打开一次后以 file handle/identity锁定，路径可在两阶段间被替换。 | 要求至少一个 identity且 label 0/1 均满足评估所需支持；发布完整 ABNF/伪代码和 header/payload exact schemas，固定 UTF-8无BOM、LF、唯一 delimiter、恰好一行 payload、duplicate-key拒绝和尺寸上限。preclaim 与 decode 应使用同一个已打开的普通文件 handle，并在两阶段核对 stat/file identity、size和 SHA；拒绝 symlink/reparse/hard-link替换。加入 delimiter伪装、额外 payload行、BOM/CRLF、超长行、空 identities和 preclaim后替换测试。 |
| M6-DOC-009 | High | CLI / terminal contract | `FR-SE-06`「CLI与输出」第 41—52 行；`sprint-m6-01` §3—5 | 一个“exact config”同时服务 controls 和 confirmation，但 mode、条件字段、输出 schema、run stage、reader状态及退出码均未冻结，无法形成唯一 CLI 或稳定验收。 | FR 没有说明 `--controls-only` 与 `--confirm` 是否互斥、二者都不提供/同时提供时怎样处理；confirmation input 在 controls-only 必须零访问，却又列为 exact config 必填项。capacity/CDF各自可 enabled/disabled，但 controls 输出是一 run 还是两个 run、disabled payload文件名/direct upstream/status均不明确。`noninferiority_unconfirmed`、`calibration_uncertainty_dominant`、`external pending`和 synthetic rule pass 应对应 manifest `complete`、scientific terminal还是失败及何种 exit code也未规定；synthetic仍使用 `confirmed_noninferior...` 机器状态，容易被下游误当科学确认。 | 使用两个互斥 subcommand/config schema，或为单一 config定义 exact mode-dependent union和字段 nullability；列出所有 stage、文件名、schema exact keys、direct upstream、reader API和稳定 exit codes。将科学判定与证据范围分开：例如 synthetic 输出 `synthetic_rule_satisfied`，只有满足 authority evidence gate 才能出现 confirmation状态；为 disabled、external pending、unconfirmed和 uncertainty-dominant明确 manifest/CLI语义。 |
| M6-DOC-010 | Medium | Repeat semantics | `FR-SE-06` 第 29、35、37、48 行；`sprint-m6-01` 第 28—29 行 | `repeat=true` 的允许范围与“claim 后失败不可重试”相互矛盾，也未规定重复 attempt 的不可变记录。 | 第 29 行允许完全相同 freeze/claim key显式重放；第 37/48 行又说任一 claim 后失败保留 claim且不可重试为新设计。相同 key 的 postclaim decode/publish失败是否可 repeat、成功后是否可 repeat、repeat 是否新 output name、是否再次读取 payload、claim如何记录多个 attempt均未定义。单个 `O_EXCL` canonical JSON又不能安全追加 attempt。 | 用状态表枚举 first attempt、in-progress/crashed、successful、postclaim-failed及同/异 key的行为。若只允许成功结果的字节重放，明确不得再次 decode；若允许相同冻结配置重算，要求新 immutable attempt run引用原 claim，claim保持只读并由另一个 append-only attempt ledger记录。把相同 key失败后、成功后、并发 repeat和不同 output name纳入验收。 |
| M6-DOC-011 | Medium | Verification plan | `sprint-m6-01` §4 | 测试清单没有覆盖上述可信边界和 M1—M5 public reader回归的具体模块，`focused` 也没有可执行命令，容易只验证 happy path。 | Sprint 只列主题和“focused、pip check、全量pytest”；没有要求重签攻击、stage/upstream/artifact substitution、partial-overlap population、claim后崩溃、preclaim-to-decode文件替换、disabled payload sentinel，以及新 capacity model经过 public reader重验。M6恰好跨越最敏感的 assessment边界和多个 immutable artifacts。 | 在 Sprint 冻结 focused pytest模块/命令和最小负例矩阵；至少覆盖 protocol、training_subsets、sample_efficiency_training/workflow/reporting、calibration/templates/inference、artifacts/transaction、assessment guards和新 controls/confirmation模块。明确 Windows reparse/symlink差异测试、完整重签攻击、crash injection以及“decode sentinel未触发”的断言，并把 full suite 与 scientific/ARM64 evidence分开报告。 |
