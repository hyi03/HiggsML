# Sprint M4-01 代码评审（GPT-5.6-sol）

评审范围为相对提交 `8bd4cb1` 的当前未提交 M4 实现，并对照已确认的 `FR-SE-04`、Sprint、artifact schema 与仓库约束。本次仅做静态评审，未运行测试。

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Evidence integrity | `src/research/sample_efficiency_workflow.py:68-89`; `tests/research/test_sample_efficiency_workflow.py:25-39` | Gate/T1 验证没有执行正式 T1 schema，未证明证据属于当前 dataset/protocol 或来自独立验证。当前代码只检查非空 `evidence_id` 和五个常量字段；缺少 `schema_version`、`dataset`、`protocol_sha256`、`independent_reference`、`validation_summary` 仍会通过，未知字段也不会拒绝。`g1.json` 同样只检查 `status` 与 `assessment_used`。 | `config/validation/t1_validation_v1.schema.json:6-45` 要求 exact keys，并在 validated 状态要求 protocol digest、独立引用、reviewer/time 和至少一个 numerical test。测试夹具正好省略这些字段，但 plan 与 E2E 测试仍接受它，直接证明不完整证据可以授权 T1 执行。旧 `scripts/h4l_run.py:149-156` 已使用 Draft 2020-12 schema 验证，新的入口没有等价检查。 | 在任何输出创建或模型调用前，使用正式 schema 验证 gate snapshot 和外部 T1 文件，额外核对 `dataset`、`protocol_sha256 == base.digest` 及 validated 条件；定义并校验 `g1.json` exact schema。增加对完整自洽重签 run 的缺字段、未知字段、错误 protocol/dataset/independent-reference 测试。 |
| High | State-machine integrity | `src/research/sample_efficiency_workflow.py:508-652` | Batch reader 没有把 ledger 的状态字段与实际 stage 链严格对应。complete cell 可把 `grid_id` 改成 `null`、把 `status` 改成失败状态或填写任意 `reason`；scientific-terminal cell 可保留 terminal stage 之后的 artifact，reader 仍会接受。 | 通用身份检查只要求 `grid_id` 属于 `{None, grid_id}`、`cell_status` 二选一和 `exit_code == 0`（512-517）。complete 分支只检查四个 artifact 非空、`terminal_stage` 为空和 `blocked_stages` 为空（630-639），没有要求 `grid_id/status/reason` 精确为发布值。terminal 分支只检查 terminal stage、blocked 列表、允许状态和 reason 类型（640-652），没有拒绝 terminal 之后的 artifact，也没有按终止阶段/status 推导允许的 artifact 前缀。自洽重算 ledger/summary/manifest 后可伪造 M5 消费的 cell 语义。 | 从已验证 artifact 链及 template/inference replay 结果推导唯一允许的 outcome：complete 必须是共同 grid ID、`status="complete"`、`reason=null`；terminal 必须使用阶段对应的 artifact 存在矩阵、正确 grid ID 时机，并禁止任何 blocked-stage artifact。逐状态加入自洽重签测试。 |
| High | Destructive-operation safety | `src/cli/sample_efficiency.py:57-90,152-155` | `--clean` 的生产边界由用户提供的 `--output-root` 自己定义，CLI 没有把它锚定到仓库 `neural/runs`；同时检查只覆盖 root 本身和其后代组件，未覆盖 root 的祖先，并在检查结束后用路径名执行 `shutil.rmtree`，存在 Windows junction/reparse point 检查后替换窗口。 | `main()` 将 `Path(values["output_root"]).resolve()` 同时作为目标父目录和 `allowed_root` 传入，所以任意绝对目录都可成为删除授权根，只需放入可重算 ID 的 marker。FR-SE-04 §12 要求生产路径位于 `neural/runs`，测试才可显式注入其他 allowed root；旧 `scripts/h4l_run.py:19-20,266,309-313` 使用固定 `RUNS_ROOT`。`clean_unstarted_batch()` 在 65-87 行完成目录、entry、reparse 和 marker 检查后，90 行才按路径递归删除，期间对象可被替换。 | CLI 使用固定、规范化的 `PROJECT_ROOT/runs` 作为生产 `allowed_root`，另保留仅供直接测试调用的显式 root；逐组件检查包括 root 的祖先。删除前在受控根内原子重命名已验证目标并再次核对对象身份/reparse 属性，再以不跟随链接的方式删除；增加根外路径、祖先 junction、目标/`staging` 检查后替换测试。 |
| High | Test authenticity | `tests/research/test_sample_efficiency_workflow.py:42-106`; `tests/research/test_h4l_learning_curve_script.py:12-45` | 新测试没有证明 Sprint 声明的安全和完整性边界，且现有“tamper”用例只命中通用文件 receipt。它没有测试 M4 semantic reader 对可重签攻击的拒绝。 | ledger 测试直接改写 `batch-ledger.json` 后立即读取，没有更新该文件 receipt、batch manifest artifact ID 或上游 manifest，因此在进入 ledger schema/state logic 前就会被 `LoadedRun.file()` 拒绝。测试总计只覆盖确定计划、一个 T1 常量差异、workers、成功 E2E、简单 clean 和新脚本 help；缺少 common-grid participant/upstream/path 重签、状态矩阵、缺失/重复/未知 cell、assessment decode sentinel、Windows reparse/race 和旧 `h4l_run.py` characterization。测试 T1 fixture 本身不符合正式 schema，反而固化了第一项缺陷。 | 加入会重新计算 payload ID、file receipt、manifest artifact ID 及所有受影响下游 ID/upstream 的攻击夹具，确保失败发生在目标 semantic check。按 Sprint §5.1/§5.2 的清单补足 gate/T1、grid、ledger 状态、assessment poison、clean 与旧脚本兼容测试。 |
| Medium | Strict JSON schema | `src/research/artifacts.py:27-32`; all new M4 `read_json()` call sites | M4 公共 reader 无法拒绝重复 JSON key，违反 FR-SE-04 §16 的 strict-schema 要求。 | 共享 `read_json()` 使用普通 `json.loads(..., parse_constant=...)`，没有 `object_pairs_hook`；重复 key 会按 last-wins 静默折叠。攻击者可构造含重复字段的 payload，再更新 file receipt 和 manifest ID；后续 `_keys()`、digest 和 expected-value 比较看到的只是折叠后的字典，因此会接受原始非规范 JSON。 | 为 research artifact 读取加入递归 duplicate-key 检测（共享 strict loader 或 M4 专用 loader），继续拒绝 NaN/Infinity，并加入自洽重签的顶层与嵌套重复键测试。 |
| Medium | Common-grid schema replay | `src/research/sample_efficiency_workflow.py:469-480,653-672` | Reader 没有验证 common-grid payload 的 `initial_mass_edges` 和 `reason`，因此正式 grid 记录可在自洽重签后与 sealed plan/实际终态不一致。 | replay 使用 `plan["common_grid_contract"]["initial_mass_edges"]` 计算并只比较 `status`、`final_mass_edges`、`merge_history`（663-671）；从未将 `grid["initial_mass_edges"]` 与 plan 比较，也未按 status 推导 `reason`。`grid_id` 是 payload 自摘要，重新计算它及 manifest/downstream IDs 不能替代字段语义验证。 | 要求 `grid.initial_mass_edges` 精确等于 plan contract，并把 `reason` 固定为 valid 时 `null`、terminal 时由 replay 得到的规范原因；对所有 grid 字段执行类型/有限值检查，增加自洽重签测试。 |
| Medium | Failure audit trail | `src/research/sample_efficiency_workflow.py:320-350,442-443` | Common-grid 输入构建和算法执行发生在任何 grid/batch `ResearchRun` 事务之外；此处的 binding 错误或未知异常会留下已发布 plan/cell runs，但没有记录失败状态的 research manifest。最外层 `except BaseException: raise` 不产生证据。 | `frames` comprehension（320-323）可在 role/model/calibration replay 中抛错；`common_mass_grid()`（324-333）只捕获 `ResearchStateError`，其他异常直接跳到 442-443。grid run 直到计算完成后的 346 行才创建，batch run 更晚到 432 行。此行为不满足 FR-SE-04 §54-58、§76-82 对 binding/unknown failure 保留不可覆盖失败证据的约定。 | 在计算开始前建立独立的 batch-attempt/controller transaction，或让 common-grid stage transaction 包住 frame 构建与 grid 计算，使异常由 manifest-last 失败记录捕获；reader 继续只消费完整 batch run。增加 grid 构建中 binding error 与 unexpected error 的失败证据测试。 |
| Low | Configuration usability | `config/research_sample_efficiency_batch.example.json:10` | 示例配置引用不存在的 T1 文件，按文档复制运行会在开始时失败。 | 仓库只有 `config/validation/t1_validation_v1.schema.json` 与 `t1_validation.pending.json`，没有示例中的 `config/validation/t1_validation_v1.json`。 | 改成明确的占位符或指向实际 receipt-bound validated evidence 的示例路径，并在文档中说明 pending evidence 不能执行 T1。 |
