# Sprint M3-01 代码评审（gpt-5.6-sol）

评审范围：未提交的 Sprint M3-01 实现，对照 `FR-SE-03`、`sprint-m3-01.md` 与 `artifact-schema.md` §9，重点复核 provenance、assessment 前置关闭、严格 schema/重签攻击、v1 兼容、summary 算法、run/upstream/receipt 绑定、跨层 lineage、异常状态及测试真实性。本次仅做静态代码评审，没有运行测试或读取研究数据。

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| P1 | Provenance / Trust boundary | **F-01** — `src/research/sample_efficiency_lineage.py:29-74`; `src/research/calibration.py:225-239` | `ExperimentLineage` 可由任意调用方直接构造，`lineage_from_model()` 也只从普通 `dict` 复制字段，因此自签 model 可以伪造“来自 verified model run”的可信 lineage。下游仅凭 `isinstance(ExperimentLineage)` 和 model ID 相等就接受，未证明 `source_model_artifact_id` 对应已重验的 run。 | FR-SE-03 §12 明确要求对象只能由 verified model-run reader 构造，§7 禁止仅凭自签 model dict 声称 run provenance。当前构造函数只检查键、类型和 64 位十六进制形状；`lineage_from_model()` 不接收或重验 `LoadedRun`，也不调用 `validate_discriminant_v2()`。攻击者可以填写任意合法形状的 artifact/model/protocol/subset ID，构造实例后让 `build_raw_calibration_bundle()` 继续计算。 | 把 lineage 变成只能由 `read_subset_discriminant_run()` 产生的 capability：工厂必须接收并重新验证 model `LoadedRun`、三上游和 model payload，构造函数使用模块私有 token 或私有实现阻止普通 raw dict 获得可信类型。下游最好接收 `LoadedSubsetDiscriminant`（或同等不可伪造的 verified handle），并逐字段验证 model、run artifact ID 与 lineage。 |
| P1 | Scientific access control | **F-02** — `src/research/inference.py:104-166`; `src/research/assessment.py:28-35,95-191` | v2 lineage-bearing template 仍可进入 M3 明确关闭的 assessment/mismatch 路径；省略可选 `experiment_lineage` 还可让同一 template 绕过 model-self lineage 校验。 | `run_asimov()` 只在调用方显式传入 lineage 时校验；template 已含 `experiment_lineage` 但参数为 `None` 时仍直接 `build_model()`。`run_toys()` 没有 lineage 参数或 v2 检测，并显式允许 `expectation_kind in {"model_self","assessment","mismatch"}`。现有 `categorize_bundle()`/`infer_assessment()` 同样不识别 sample-efficiency lineage，会读取 mother/assessment 并运行推断。FR-SE-03 §§14-15 要求 v2 assessment/mismatch 在 claim 或 payload 读取前以 `training_subset_binding_mismatch` 关闭。 | 在所有可消费 bundle/template 的入口先检测 `experiment_lineage`。对 v2，M3 只允许带可信 expected lineage 的 raw → template → `model_self_asimov`；`run_toys()`、`infer_assessment()`、stress/T2 和 claim ingress 必须在 `build_model()`、mother decode、RNG 或 claim 前拒绝。保留无 lineage 的旧 v1 分支以维持兼容。 |
| P1 | Cross-layer integrity | **F-03** — `src/research/calibration.py:225-239`; `src/research/templates.py:144-169` | raw calibration bundle 没有覆盖其内容和 lineage 的外层 digest，template binder 也不验证 calibration 的 model/threshold 内容；篡改阈值或替换嵌套 model 后仍可在原 lineage 下签出新 template。 | `fit_thresholds()` 的 `threshold_id` 在 lineage 加入前生成，raw `mapping_id` 只是 `"raw:" + model_id`，返回 bundle 没有 `calibration_id`。`bind_template_lineage()` 只比较 bundle 的 lineage、`transform`、`candidate_id` 和 `mapping_id`，完全不校验 `bundle["model"]`、`model_id`、`key`、threshold digest及这些字段与 lineage 的一致性。测试也使用手工 template，再调用 binder，没有证明 template 来自未篡改的直接 calibration 上游。FR-SE-03 §13 要求每层从直接可信上游逐字段验证，且 mapping/threshold/template/result digest 覆盖 lineage。 | 定义不可变、严格 schema 的 raw calibration value object，并增加覆盖完整 bundle（含 canonical lineage、model identity、thresholds）的 `calibration_id`。template 入口先验证该对象、v2 model、threshold digest、experiment key及全部交叉绑定，再从已验证 bundle 计算/绑定 template；不要让普通可变 dict 仅凭几个字符串成为可信直接上游。 |
| P1 | Strict schema / Re-signing | **F-04** — `src/research/sample_efficiency_training.py:89-155,240-264` | v2 reader 的语义重算不完整：重签 model 可使用 overlay 未注册的 `network_seed` 并通过 reader；条件字段和 partial fraction 也没有按契约严格验证。 | `_train_model()` 发布时检查 `network_seed in overlay["network_seeds"]`，但 `validate_discriminant_v2(..., overlay=...)` 只比较 overlay digest，reader 最后只要求 model seed 等于调用参数。因此把 `seed/network_seed` 改为 `-1`、重算 experiment/pairing/model ID和run receipt，再以 `network_seed=-1` 调 reader即可通过。根键逻辑以“artifact 是否自带 `history_contract`”决定允许集合，删除该字段并重签仍通过，即使 base protocol 要求它；standalone validator 把所有非 `1.0` float 当 partial，未要求 `0 < fraction < 1.0`。这些均违反 FR-SE-03 §§8-10 的协议字段和重签语义校验。 | 在 reader/validator 中验证 seed 属于 overlay 注册集合，fraction 满足 full/partial 的严格范围并在有 overlay 时属于注册网格；根据已验证 base protocol 决定 `history_contract` 必须出现或必须缺失，并严格校验其 schema。为没有 base/overlay 上下文的 predictor 限定可验证的本地不变量，涉及 provenance/协议成员关系的下游只接受 verified reader handle。 |
| P2 | Run binding | **F-05** — `src/research/sample_efficiency_training.py:251-255` | model-run upstream 校验把 manifest 列表压成 `(stage, artifact_id)` 集合，既忽略记录的 path，也会折叠重复项，不满足“三个上游路径及集合精确绑定”。 | `actual_upstreams = {(stage, artifact_id) ...}` 与 expected set 比较。重签 manifest 后，把 upstream `path` 改成另一目录，或追加同 stage/ID 的重复记录，比较仍相等；`read_run(run_path)` 只验证 model run 自身 receipt，并不会跟随这些 manifest path。FR-SE-03 §7 要求重验三上游路径和各 receipt，artifact schema §9 也声明精确绑定。 | 将 manifest upstream 当严格列表或 `Counter` 校验，拒绝重复/未知项；逐项比较 stage、artifact ID 和规范化后的 path 与本次重新验证的 `LoadedRun.path`，并保持恰好 prepare、compact-freeze、training-subsets 各一项。 |
| P2 | Exception state / Fail-fast | **F-06** — `src/research/sample_efficiency_training.py:89-155`; `src/research/calibration.py:225-239`; `src/research/templates.py:144-157` | 多个 malformed/伪造输入会抛原生 `KeyError`/`AttributeError`，或先执行预测/校准再以 binding error 失败，破坏固定 `training_subset_binding_mismatch` 和“下游计算前失败”语义。 | `validate_discriminant_v2()` 对 `scaler`、`training_summary_total` 直接调用 `.get()`，严格 JSON 只保证它们是某种合法 JSON 值；例如重签后把其中一个改成 list 会逃过 `_finite_json()` 并触发 `AttributeError`。`build_raw_calibration_bundle()` 在确认 lineage 类型前下标访问它；普通 dict lineage 可先通过 model-ID 比较，直到预测和 `fit_thresholds()` 完成后才在 binder 失败。v1 model 也未在入口拒绝，可先预测后因缺 `experiment_cell_id` 抛 `KeyError`。 | 为每个 public M3 入口先做完整类型/schema/stage检查，并统一转为 `ResearchError(status="training_subset_binding_mismatch")`；在任何预测、阈值拟合、模板构建、RNG 或 payload访问前验证 v2 schema、可信 lineage capability及允许的 raw/model-self 阶段。为所有嵌套对象定义精确键和类型校验，避免依赖 `.get()` 的偶然异常。 |
| P2 | Test authenticity / Coverage | **F-07** — `tests/research/test_sample_efficiency_training.py:27-131`; `tests/research/test_sample_efficiency_lineage.py:39-79` | focused tests没有执行 Sprint/FR 列出的关键对抗路径，现有“prepared feature tamper”只证明 stale receipt 会失败，无法证明 receipt 自洽重签后的语义关闭。 | feature test直接修改 `events.jsonl` 后不更新 file receipt/manifest artifact ID，因此失败发生在通用 `read_run()` receipt 检查；没有覆盖 source-row/validation 自洽篡改、缺失/额外/非train组、伪造 `TrainingSubset`、未知/缺失/非有限 v2 字段、out-of-protocol seed、upstream path/重复项、v1 固定 bytes/model ID、capacity/assessment 前置拒绝。lineage tests只比较两个合法 lineage、拒绝 non-raw 参数和修改内嵌 lineage；没有尝试直接构造 lineage、重签 calibration/template/result内容或经现有 assessment入口访问。 | 增加 receipt/manifest/model digest 全部重签后的攻击 fixtures，并用观察钩子证明失败发生在训练 RNG、`load_research_data` 的禁止角色 payload decode、`build_model`、mother decode和 claim 之前。补齐 Sprint §5.1/§5.2 与 FR 验收点的参数化矩阵，同时固定一个已知 v1 canonical payload/model ID及预测输出以证明兼容。 |
