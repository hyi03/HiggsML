# Sprint M3-01 Code Review by GPT-5.5

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Provenance / Security | 1. [calibration.py](../../src/research/calibration.py):223-239; [sample_efficiency_lineage.py](../../src/research/sample_efficiency_lineage.py):63-82; [test_sample_efficiency_lineage.py](../../tests/research/test_sample_efficiency_lineage.py):44-47 | Raw calibration lineage can be attached to a calibration frame that is not proven to come from the prepared run bound by the v2 model. The helper checks only model ID/lineage and then scores whatever same-schema frame it receives; it never verifies `population_id`, prepared artifact identity, role source, or receipt-backed origin for the calibration rows. | FR-SE-03 requires downstream lineage to be copied from direct trusted upstreams and M3 to reject downstream lineage mismatches before payload access (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:12`, `../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:13`). `build_raw_calibration_bundle()` accepts an arbitrary `frame`, calls `predict_discriminant()`, and binds lineage if `model_id` matches. The test passes `frame.loc[frame.role == "calibration"].copy()` from the fixture but does not cover a calibration frame loaded from another prepared run with the same dataset. | Require a receipt-backed prepared run or a validated source identity object for downstream calibration, then check its population/prepared artifact against `experiment_lineage`. At minimum, require `frame.attrs["population_id"]` and source artifact identity to match, and add a negative test using a second prepared run’s calibration rows. |
| High | Provenance / Test Authenticity | 2. [templates.py](../../src/research/templates.py):142-156; [test_sample_efficiency_lineage.py](../../tests/research/test_sample_efficiency_lineage.py):26-36, [test_sample_efficiency_lineage.py](../../tests/research/test_sample_efficiency_lineage.py):49-51 | Template lineage can be bound to a hand-made template that was never produced from the bound calibration/template roles. The helper only compares `candidate_id`, `mapping_id`, and raw lineage on the calibration bundle; it does not verify prepared/population identity, template role source, mass grid provenance, or a direct upstream artifact. The current test demonstrates this gap by constructing a synthetic one-bin template in memory and successfully binding lineage to it. | FR-SE-03 says template entries must copy lineage from direct trusted upstreams and M4 publishers later record direct upstream runs, but M3 still must provide safe computation interfaces (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:13`). `bind_template_lineage()` accepts `dict(template)` after only candidate/mapping checks. `_valid_template()` in the test is not produced by `build_templates()` and has no prepared/subset provenance. | Move lineage binding into a template-building helper that receives the verified calibration bundle and the verified template frame/run context, or require template payloads to carry and validate `prepared_artifact_id`, dataset, template role, mass edges, and source lineage before binding. Replace the hand-made happy-path template with a template generated by the real template builder, and keep hand-made templates only for negative tests. |
| High | Schema / Re-sign Attack | 3. [discriminants.py](../../src/research/discriminants.py):214-220; [sample_efficiency_training.py](../../src/research/sample_efficiency_training.py):89-156; [test_sample_efficiency_training.py](../../tests/research/test_sample_efficiency_training.py):119-131 | `predict_discriminant()` accepts a naked `research-discriminant-v2` dict and validates it without the trusted `TrainingSubset`, base protocol, overlay, or freeze descriptor. In that mode `validate_discriminant_v2()` cannot verify representation mapping against freeze, summary equality against M2, or selection binding; a re-signed v2 model can alter `candidate`/`representation`/`groups` consistently with its own model ID while retaining a misleading `representation_id` and pass prediction. | FR-SE-03 requires v2 prediction to strictly recompute representation, seed, architecture, parameter count, subset/experiment/pairing ID, and summary structure, and says self-signed dicts must not claim run provenance (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:7`, `../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:11`). `predict_discriminant()` calls `validate_discriminant_v2(artifact)` with no trusted selection/base/overlay. In that branch, `validate_discriminant_v2()` uses `names = model["ordered_inputs"]` and skips `_representation(selection, overlay, ...)`. The current negative predict test mutates `sample_draw_seed_or_full`, which the ID formula catches, but it does not mutate representation/candidate/groups under a recomputed `model_id`. | Make low-level `predict_discriminant()` reject v2 artifacts unless they have already been loaded through `read_subset_discriminant_run()` or pass a trusted validation context. If direct v2 prediction remains supported, require a compact descriptor in the model and fully validate `representation_id -> candidate/representation/groups/ordered_inputs` even without external selection. Add a re-signed representation mismatch test. |
| Medium | Integrity / Strict Schema | 4. [calibration.py](../../src/research/calibration.py):234-239; [templates.py](../../src/research/templates.py):151-156; [inference.py](../../src/research/inference.py):117-124 | Calibration bundles do not get a content digest after lineage binding, while template and inference outputs do. This leaves one downstream layer without the content-level re-sign protection promised by the M3 schema: changing thresholds or mapping metadata in a receipt-bound calibration JSON could still satisfy `require_experiment_lineage()` because that helper checks only the copied lineage dict. | FR-SE-03 says model/mapping/threshold/template/result content digests cover the lineage field (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:13`). `bind_template_lineage()` adds `template_id`, and `run_asimov()` adds `result_id`. `build_raw_calibration_bundle()` returns a lineage-bound payload with thresholds but no `calibration_id` or equivalent digest. | Add `calibration_id = digest_json(payload_without_calibration_id)` after binding lineage, and provide `require_raw_calibration_lineage()` that verifies both lineage and calibration digest. Add a semantic tamper test that changes thresholds, recomputes file receipt if needed, and expects rejection. |
| Medium | Safety / Scope | 5. [inference.py](../../src/research/inference.py):101-124; [templates.py](../../src/research/templates.py):158-168 | The M3 model-self inference API is only gated by the optional `experiment_lineage` argument. If callers omit it, v2-derived templates can still be passed through the legacy `run_asimov()` path with no lineage result, and there is no guard that rejects v2 lineage templates when lineage is absent. This weakens the FR requirement that v2 model-self results carry and validate lineage. | FR-SE-03 requires each model-self inference outer result to copy lineage and M3 to keep sample-efficiency paths raw/model-self only (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:13`, `../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:14`). `run_asimov()` performs lineage checks only when `experiment_lineage is not None`; otherwise it returns the legacy result. A template produced by `bind_template_lineage()` carries `experiment_lineage`, but `run_asimov(bound_template, protocol=base)` without the lineage argument will not reject or propagate it. | If a template contains `experiment_lineage`, require the caller to pass the matching `ExperimentLineage` and always bind `result_id`; otherwise reject. Add a test that omits `experiment_lineage` for a bound v2 template and expects `training_subset_binding_mismatch`. |
| Medium | Compatibility / Validation | 6. [sample_efficiency_training.py](../../src/research/sample_efficiency_training.py):89-156 | `validate_discriminant_v2()` treats `history_contract` as optional based solely on whether the field is present, not on the bound base protocol. For protocols with diagnostics enabled, a re-signed v2 model can drop `history_contract` and still pass validation if other fields are consistent. | The current v1 training code includes `history_contract` when `diagnostics` exists in the protocol. FR-SE-03 says v2 root keys include current v1 required keys and `history_contract` under the base-protocol condition (`../1-Requirement/FR-SE-03-subset-bound-discriminant-v2.md:8`). `validate_discriminant_v2()` builds the allowed set with `({"history_contract"} if "history_contract" in model else set())` and does not check base protocol diagnostics for required presence. | When `base` is supplied, require `history_contract` iff the base protocol would make v1 training emit it, and validate its version/source fields. Add a re-signed model test that removes `history_contract` under the default protocol. |
| Low | Test Coverage | 7. [test_sample_efficiency_training.py](../../tests/research/test_sample_efficiency_training.py):104-116; [test_sample_efficiency_lineage.py](../../tests/research/test_sample_efficiency_lineage.py):59-79 | The focused tests cover receipt breakage and several re-signed fields, but they do not cover the two highest-risk provenance substitutions: same-dataset calibration/template data from a different prepared run, and re-signed representation/candidate/group mismatch during naked v2 prediction. | Current training tamper test mutates prepared bytes and relies on receipt mismatch. Current lineage tests substitute lineage between two models and reject non-raw transform, but they use the same prepared fixture and a hand-made template. | Add negative tests for cross-prepared calibration frame substitution, hand-made template binding rejection, and re-signed v2 representation/candidate/groups mismatch passed to `predict_discriminant()`. |
