| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| Medium | Correctness | `neural/src/research/sample_efficiency_protocol.py:230-239` | CR-01: The overlay accepts the freeze payload SHA as the freeze artifact ID, so the new manifest-vs-payload binding invariant is not enforced. | The updated FR explicitly separates `compact_candidate_freeze_artifact_id` from `compact_candidate_freeze_sha256` and says the two identities have different meanings and the payload SHA must not stand in for the manifest artifact ID (`neural/docs/1-Requirement/FR-SE-01-sample-efficiency-contracts.md:106-108`). Existing research run artifact IDs are manifest digests (`neural/src/research/artifacts.py:68-71`, `neural/src/research/artifacts.py:178-179`), while `CompactCandidateFreeze.digest` is the payload digest excluding its own digest field (`neural/src/research/sample_efficiency_protocol.py:141-142`, `neural/src/research/sample_efficiency_protocol.py:187-189`). `_validate_overlay` only checks each raw field against the caller-supplied expected value, so a caller can pass `compact_freeze_artifact_id=compact_freeze.digest` and a matching raw payload; validation succeeds even though the overlay no longer records a distinct manifest artifact identity. The current tests cover binding mismatches but not equality/substitution between the artifact ID and payload SHA (`neural/tests/research/test_sample_efficiency_protocol.py:171-177`). | Add a cross-field check in `_validate_overlay` rejecting `compact_candidate_freeze_artifact_id == compact_candidate_freeze_sha256` (or, equivalently, rejecting the expected artifact ID when it equals `compact_freeze.digest`). Add a focused test that builds an otherwise valid overlay with the artifact ID set to the freeze digest and expects `ResearchError`. This does not require receipt validation; it only enforces the M1 metadata boundary that the two identifiers cannot be collapsed. |
| Info | Consistency | `neural/src/research/sample_efficiency_protocol.py`; `neural/config/research_sample_efficiency_protocol_v1.json`; `neural/tests/research/test_sample_efficiency_protocol.py`; `neural/tests/research/test_sample_efficiency_characterization.py`; `neural/docs/sw-dev/artifact-schema.md:108-125`; `neural/docs/1-Requirement/FR-SE-01-sample-efficiency-contracts.md:69-110` | CR-02: No additional correctness or safety regression was found in the bounded M1 metadata scope. | The implementation rejects unknown/missing/duplicate fields, non-finite JSON, bool-as-number, invalid fractions/seeds, missing freeze, candidate tampering, base/freeze/overlay binding mismatch, unfilled template loading, and unsupported legacy protocol drift. The updated docs also preserve the explicit deferred boundary for runtime experiments, receipt validation, CI/statistical execution, assessment access, and scientific preregistration. | Keep the current M1 boundary intact while fixing CR-01; future receipt validation, subset generation, model v2, reports, and confirmation claims should remain in later milestones. |
