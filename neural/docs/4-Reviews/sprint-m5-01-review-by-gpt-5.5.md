# Sprint M5-01 独立文档评审（gpt-5.5/high）

评审对象：`docs/1-Requirement/FR-SE-05-paired-aggregation-reporting.md` 与 `docs/3-Plan/sprint-m5-01.md`。对照依据：根 `AGENTS.md`、`neural/AGENTS.md`、`docs/sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md`、已归档 M4 FR/Sprint/代码确认与 `docs/sw-dev/artifact-schema.md` §10。评审只覆盖文档合同与计划，不修改目标文件。

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Artifact reader boundary | `FR-SE-05:10-13`, `FR-SE-05:55-57`, `FR-SE-04 Done:28-35`, `FR-SE-04 Done:70` | M5 reader 的信任边界不闭合。FR 同时要求入口接受 prepared/freeze/subsets/gate/T1/base/overlay、report run 直接 upstream 仅为 M4 batch、公共 reader 又必须重放 M4 reader 与 cell payload；但没有定义 M5 report reader 是接收一个 verified M4 batch handle、重复接收全部 M4 inputs，还是从 M4 manifests 自行遍历恢复这些输入。仅保存 M4 batch artifact ID 和 digests 不足以说明 reader 如何重新满足 M4 `read_learning_curve_batch()` 的前置验证。 | M5 FR 第10行要求入口接受完整 M4 上下文；第11行又规定 report manifest 直接 upstream 仅为完整 M4 batch；第57行要求公共 reader 验证指针重放、bootstrap重放、配对/聚合/CSV重算。M4 已归档合同第28—35行要求每层 reader 验证 upstream path/multiplicity、gate/T1、strict JSON；第70行说明 M5 只消费 verified M4 reader 返回值。当前 M5 文档没有把“verified M4 batch handle”作为不可伪造输入类型，也没有给出 report reader 的参数和遍历规则。 | 明确 M5 publisher 与 reader API。推荐：M5 publisher 只接受 `LoadedLearningCurveBatch` 这类由 M4 reader mint 的可信 handle；M5 report reader 则接收 report path 加同一组 M4 verification inputs，先重跑 M4 reader 得到 handle，再验证 report。若希望 report reader 仅靠 report path 工作，则必须在 report manifest/contract 中记录并严格校验可遍历的 M4 upstream 路径、base/overlay/T1 snapshots 与 artifact IDs，并把该 traversal 算法写入 FR。 |
| High | Record schema / terminal cells | `FR-SE-05:19-29`, `FR-SE-05:37-47`, `sprint-m5-01:19-22` | `actual_train_group_count` 对 terminal cells 的定义不完整，导致“每个 plan cell 恰有一行”和 summary 聚合无法实现。FR 把该字段列为每条 record 的精确字段，又说 complete cell 的实际组数来自模型并与 training subset summary 重验；但 M2 低统计或模型前 terminal cell 没有模型 artifact。summary 又要求按 actual count 聚合 complete 与 terminal 计数。 | 第19—25行要求每个 canonical plan cell 一行并包含 `actual_train_group_count`；第27行只说明 complete cell 的实际训练组数来自模型 `training_summary_total.group_count`；第27行还规定 terminal cell 数值与 bootstrap 为 null，但没有说明 `actual_train_group_count` 是否仍必须非 null。第39行要求 summary 按 `(representation_id,sample_fraction_target,actual_train_group_count)` 聚合 complete 与 terminal 计数。 | 固定 `actual_train_group_count` 的所有状态来源：优先从 verified model summary 读取；无模型的 terminal cell 从 M2 training subset summary 或 plan cell 绑定的 subset summary 重算；如果某类 terminal 无法确定，则字段显式为 null，并禁止该 cell 进入按 actual count 的曲线聚合，同时另行计入 missing-count terminal。把 nullability、ID preimage 和 summary 分母写清。 |
| High | Statistical reproducibility | `FR-SE-05:31-36`, `FR-SE-05:37-47`, `sprint-m5-01:21-22`, development plan `339-351` | bootstrap 与经验标准差的数学细节仍不足以生成可重放 artifact。文档规定 event-group bootstrap、mean/std/quantiles 和 network/subset 标准差，但没有冻结 weighted AUC 公式、tie handling、无效 replicate 的统计处理、std 的 ddof、quantile 插值算法、NaN/null 排除规则和 record/summary 中状态字段的 exact schema。 | 第33行描述按 event group 有放回抽样并用 `abs(physical_weight) × multiplicity` 计算 AUC；第35行要求输出 mean/std/quantile_16/quantile_84；第41—42行要求 network/subset 样本标准差；第47行要求只使用 complete/estimated 值并显式输出分母。开发总计划第339—351行要求误差来源分开且未实现层写 `not_estimated`。缺少上述低层算法时，不同库版本或实现选择会产生 receipt 自洽但数值不同的报告。 | 在 FR 中补充 `weighted_auc_algorithm`、排序/tie 规则、权重为零与单类 replicate 的无效状态、bootstrap `std_ddof`、quantile method、样本标准差 ddof、浮点 canonical 化与 null 排除规则。建议所有统计结果都带 `method_id`，reader 按同一算法逐值重放。 |
| Medium | Pairing / aggregation identity | `FR-SE-05:21-29`, `FR-SE-05:37-47`, development plan `320-337` | aggregation key 和 pairing key 的关系还不够精确，尤其是按 actual count 聚合后如何保留 draw/network 配对信息。FR 先按 exact `(fraction, draw/full, network_seed, architecture)` 做 engineered19 配对，再按 `(representation, fraction, actual_count)` 聚合；但没有定义聚合 delta/relative W68 时的分母是 complete cells、paired cells 还是所有 planned cells，也没有定义 engineered19 自身是否计入 pair completeness 的分母。 | 第29行规定只在完全匹配键且双方 complete 时计算 delta/relative，缺任一侧为 `paired_cell_missing`；第39行要求 summary 聚合均值 W68/AUC/relative/delta 和配对完整率；第47行要求分母、缺失数与状态显式输出。开发总计划第320—337行强调主记录键和严格配对，但 M5 FR 没有把聚合分母公式固定下来。 | 为每个 summary row 明确 `planned_count, complete_count, pair_eligible_count, paired_count, baseline_missing_count, target_missing_count, terminal_count` 等分母；delta/relative 均值只用 `pair_status=paired`，pair completeness 定义为 `paired_count / pair_eligible_count`，并明确 engineered19 自身是否作为 paired=1 的 baseline self-pair 参与统计。 |
| Medium | Q* semantics | `FR-SE-05:49-53`, `config/research_sample_efficiency_protocol_v1.json`, development plan `366-368` | Q* 规则方向正确，但输入曲线和重复 actual-count 情况未定义。协议字段是 `quality_target.w68`，FR 写“按实际组数升序使用聚合 mean W68”，同时 summary rows 仍按 representation/fraction/actual-count 组织；若不同 fraction 经 floor 后得到相同 actual count，或同一 representation 有多个 summary row 共享 actual count，Q* 插值输入不唯一。 | 第51行规定按实际组数升序用 mean W68，精确命中或相邻点包围时报告；第39行 summary key 仍包含 `sample_fraction_target`；开发总计划第368行禁止外推。已实现协议的 `quality_target` 字段为 `{enabled,w68,interpolation}`，没有 fraction 优先级或重复 actual-count 处理策略。 | 定义 Q* 的曲线输入为每个 representation 的唯一 `(actual_train_group_count, mean_w68)` 序列；若多个 summary rows 映射同一 actual count，要么先用固定规则合并，要么把重复 actual count 作为 schema error。补充 exact-hit 多点、相等 W68 plateau、缺失 mean W68、terminal-only row 的处理状态。 |
| Medium | Report artifact integrity | `FR-SE-05:49-57`, `sprint-m5-01:24,29-30` | PNG 和 Markdown 的 reader 验证不足。FR 要求 CSV/PNG/Markdown 都从 exact schema 的 summary curve rows 生成，但 reader 只明确“PNG/Markdown receipt存在”，这无法防止 receipt 自洽的误导性图表或 Markdown 结论漂移，尤其 AGENTS 明确禁止夸大为 ATLAS 结果或物理测量。 | 第53行规定 PNG 两个 panel、缺失值不连线，Markdown 只陈述合同/完整性/失败/误差/Q* 状态且不生成 MC 节省倍数；第57行 reader 对 CSV 是逐值重放，对 PNG/Markdown 只说 receipt 存在。Sprint 第24行要求从 verified summary rows 生成 CSV/PNG/Markdown，第30行测试只列 receipt自洽指标/配对/聚合/CSV篡改，没有提 PNG/Markdown 语义篡改。 | 将 PNG/Markdown 生成确定化并纳入 reader 重放，至少保存 `render_source_id`、template version、curve rows digest 和 renderer config，并让 reader 重建 Markdown 字节或验证所有声明字段来自 summary/Q* payload。PNG 若不能跨平台 byte-stable，需保存机器可读 plot spec JSON 并验证 PNG receipt 仅作展示缓存。 |
| Medium | CLI / config boundary | `FR-SE-05:59-61`, `sprint-m5-01:25,34`, M4 Done `37-41` | M5 CLI surface 没有达到 M4 同等精确度。FR 说“复用 M4严格输入配置并新增 `--batch-run`、`--output-root`、`--output-name`”，但没有列出 exact config schema、显式参数优先级、dataset/base/overlay一致性、allowed root、是否新增 console entry point、是否允许 `--config` 中包含 M4 执行输出字段。 | M4 已归档合同第37—41行精确列出 batch config keys、禁止科学参数、execution identity 和 `--no-progress` 不进入 plan。M5 FR 第61行只给出新增参数和禁止科学参数，没有定义 `h4l-sample-efficiency-report-config-v1` 的键集合，也没有说明报告 output root 是否固定在 `neural/runs` 或测试 allowed_root。 | 增加 M5 report config exact schema、CLI 参数全集、路径优先级和输出 identity 规则。明确不新增 console entry point还是新增，明确 allowed root 策略，禁止 filter/seed/Q*/replicate/scientific overrides，并要求 `--dataset` 与 base/overlay/M4 batch contract 一致。 |
| Medium | Testability | `FR-SE-05:63-72`, `sprint-m5-01:27-34` | 验收测试清单方向正确，但缺少 several core negative cases：M4 reader 输入边界、terminal actual-count 来源、bootstrap 数值算法跨库稳定性、PNG/Markdown 语义漂移、Q* duplicate actual-count。若不补，M5 可以通过 happy-path synthetic 但无法证明 reader 抵抗 receipt 自洽语义漂移。 | Sprint 第29—34行列出 E2E、cell扰动、pairing、bootstrap、Q*、CLI/assessment sentinel；FR 第65—70行列出顺序、配对、bootstrap、full endpoint、Q*、focused tests。但缺少 report reader 在 M4 inputs 被替换、terminal 无 model、weighted AUC ties、quantile method、Markdown overclaim 和 plot spec tamper 的测试。 | 把这些负例纳入 Sprint 测试：替换 prepared/overlay/gate 后 report reader 必须拒绝；M2 low-stat terminal 仍有可解释 count 或明确 null；weighted AUC tie/zero-weight/single-class replicate 固定；Markdown 禁止出现 ATLAS/measurement/节省倍数字样；Q* duplicate n 触发固定处理；PNG plot spec/curve digest 篡改拒绝。 |
| Low | Documentation clarity | `FR-SE-05:15-18`, `sprint-m5-01:15,24` | 文档说 M5 输出 `report-contract.json` 并在 Sprint 纳入 schema 文档，但没有明确 `artifact-schema.md` 要新增 §11，也没有说明 report stage 的 artifact schema 与 M4 §10 的链接关系。 | FR 第17行定义 `h4l-sample-efficiency-report-contract-v1`，Sprint 第15行写“schema文档”，第24行写 report publisher/reader，但没有指定更新 `docs/sw-dev/artifact-schema.md` 或新章节标题。M4 已把 batch 合同记录在 artifact-schema §10。 | 在 Sprint scope 中明确更新 `docs/sw-dev/artifact-schema.md` §11“配对聚合报告”，并列出 report contract、records JSONL、summary、curve CSV/plot spec、Markdown 的 schema/ID/reader responsibilities。 |
