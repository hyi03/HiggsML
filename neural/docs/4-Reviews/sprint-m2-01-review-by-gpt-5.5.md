# Sprint M2-01 Document Review by GPT-5.5

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Contract blocker | 1. FR-SE-02 §需求描述 2/5 (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:29`, `../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:32`); sprint §5.1/§9 (`../3-Plan/Done/sprint-m2-01.md:23`, `../3-Plan/Done/sprint-m2-01.md:46`) | M2 requires weight and mass summaries while also requiring the implementation to never decode payload. That is not satisfiable with the existing prepared `events.jsonl` format. | The current identity envelope fields are only `event_id`, `source_row_id`, `event_group_id`, `split`, `dataset`, `label`, and `role` (`../../src/research/data.py:20`, `../../src/research/data.py:21`). `write_research_data()` writes those identity fields before the tab and puts all remaining fields in payload (`../../src/research/data.py:421`, `../../src/research/data.py:424`). `load_research_data()` only sees `physical_weight` and `m4l` after decoding payload (`../../src/research/data.py:467`, `../../src/research/data.py:480`). FR-SE-02 says the identity envelope provides `physical_weight` and `m4l` for summary while not reading payload (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:32`). The development plan similarly says M2 reads train identity and necessary statistics, but the current artifact format does not place those statistics in identity (`../../docs/sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:162`, `../../docs/sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:209`). | Resolve the data-source contract before implementation. Either revise M2 to decode only train payload for `physical_weight`/`m4l` while preserving assessment poison tests, or define a new prepared identity/stat sidecar or `events.jsonl` schema version that includes the necessary summary fields in the identity-side record. If current `h4l-events-v1` prepared runs remain the M2 input, remove weight/mass summaries from FR-SE-02 or make them explicitly unavailable. |
| High | Binding / verifiability | 2. FR-SE-02 §需求描述 2 (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:29`); sprint §5.2 (`../3-Plan/Done/sprint-m2-01.md:29`) | The requirement says the identity reader must verify a per-row identity digest, but existing prepared events do not store a per-row identity digest or any row-level digest inventory. This creates an impossible test expectation unless M2 defines where the digest is stored and what it binds. | Existing `events.jsonl` has one canonical header line, then `canonical(identity) + "\t" + canonical(payload)` per row (`../../src/research/data.py:406`, `../../src/research/data.py:420`, `../../src/research/data.py:424`). The only published receipt for the stream is the whole file SHA-256 and byte size in the `research-run-v1` manifest (`../../src/research/artifacts.py:144`, `../../src/research/artifacts.py:157`). `read_run`/`LoadedRun.file()` can verify the file receipt, but there is no stored expected digest for each identity row to compare against. | Amend FR-SE-02 to say exactly what is verified for `h4l-events-v1`: manifest file receipt, header dataset/protocol digest, canonical identity field set, row uniqueness, and recomputed population identity. If M2 truly needs row-level identity digests, define them as fields in the new training-subset membership artifact or in a new prepared-event schema, and add the corresponding writer/reader contract. |
| Medium | Ambiguous terminal state | 3. FR-SE-02 §需求描述 6 and §失败与降级 (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:33`, `../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:50`); sprint §5.2/§6 (`../3-Plan/Done/sprint-m2-01.md:29`, `../3-Plan/Done/sprint-m2-01.md:34`) | Low-statistics behavior is underspecified at the run level. The docs say to record `training_subset_insufficient_statistics` and keep a ledger terminal state, but do not say whether the `research-run-v1` manifest status is `complete`, a terminal `ResearchStateError` status, or a complete run with failed cells in the ledger. This affects whether downstream M3 can read the artifact normally. | Existing `ResearchRun` writes `status="complete"` unless an exception changes manifest status (`../../src/research/artifacts.py:93`, `../../src/research/artifacts.py:165`). Scientific terminal states raised as `ResearchStateError` publish a manifest but `read_run()` rejects them unless terminal access is explicitly allowed, as covered by the artifact tests (`../../src/research/artifacts.py:162`, `../../src/research/artifacts.py:185`; `../../tests/research/test_artifacts.py:75`, `../../tests/research/test_artifacts.py:83`). FR-SE-02 currently names `training_subset_insufficient_statistics` but does not map it to either manifest status or per-ledger cell status. | Specify the M2 state model. For example: successful subset publication keeps manifest `status="complete"` and records per-subset/per-draw statuses in `training-subset-ledger.json`; if no usable subset exists, publish a durable terminal manifest status and require M3 readers to open it with explicit terminal handling. Add acceptance tests for the chosen model. |
| Medium | Determinism / schema precision | 4. FR-SE-02 §需求描述 3/4/7 (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:30`, `../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:31`, `../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:34`); FR-SE-01 v1 software fields (`../1-Requirement/Done/FR-SE-01-sample-efficiency-contracts.md:76`, `../1-Requirement/Done/FR-SE-01-sample-efficiency-contracts.md:78`); development plan §6.2/§6.3 (`../../docs/sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:166`, `../../docs/sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:191`) | The 100% endpoint canonicalization is directionally correct but not precise enough for implementation: the docs do not define the exact `subset_id`, `membership_digest`, and ledger alias bytes for partial draws versus the canonical `full` endpoint. That leaves room for implementations that pass nesting checks but produce incompatible artifact IDs or duplicate full memberships. | FR-SE-01 defines `full_endpoint=single_full_identity` and says the pairing key uses `sample_draw_seed_or_full` (`../1-Requirement/Done/FR-SE-01-sample-efficiency-contracts.md:76`, `../1-Requirement/Done/FR-SE-01-sample-efficiency-contracts.md:78`). FR-SE-02 says full membership is published once and each draw points to the same full subset (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:31`). The development plan requires 100% not to count as multiple draw replicas (`../../docs/sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:189`, `../../docs/sw-dev/h4l-compact-kinematics-sample-efficiency-development-plan.md:198`). None of the M2 docs defines the canonical hash input for `subset_id` or whether membership rows use `draw_or_full="full"` for `fraction=1.0` while ledger rows retain original draw aliases. | Add a small canonicalization subsection before implementation: define `draw_or_full`, `subset_id`, `membership_digest`, and ledger alias hash inputs for `f < 1` and `f = 1.0`; state whether `fraction` is represented as numeric `1.0` or a canonical JSON number; require tests that two draw seeds produce one byte-identical full membership file segment and distinct ledger aliases. |
| Low | Traceability / documentation scope | 5. Sprint §3 and FR-SE-02 §输出 (`../3-Plan/Done/sprint-m2-01.md:13`, `../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:46`); artifact schema §7 (`../../docs/sw-dev/artifact-schema.md:113`, `../../docs/sw-dev/artifact-schema.md:130`) | The sprint includes “artifact/schema设计文档” but does not name the required artifact-schema update or the exact new file-contract content. This is easy to miss because the current artifact schema explicitly says future subset/model/report payloads are not implemented. | `artifact-schema.md` currently lists only M1 sample-efficiency protocol/freeze payloads and states future subset/model/report/confirmation payloads are not implemented (`../../docs/sw-dev/artifact-schema.md:113`, `../../docs/sw-dev/artifact-schema.md:130`). FR-SE-02 introduces three new schemas and three filenames (`../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:34`, `../1-Requirement/Done/FR-SE-02-deterministic-training-subsets.md:46`), while sprint §3 only says “artifact/schema设计文档” without a concrete checklist. | Add an explicit sprint task to update `docs/sw-dev/artifact-schema.md` with `training-subsets.json`, `training-subset-membership.jsonl`, `training-subset-ledger.json`, schema identifiers, status fields, receipt expectations, and reader validation responsibilities. |
