# Sprint M5-01 独立代码评审（gpt-5.5/high）

评审范围：当前未提交的 Sprint M5-01 实现，相对 commit `8ac1df1`。对照依据：已确认 `FR-SE-05`、`sprint-m5-01`、`sprint-m5-01-review-confirm`、M4 已归档合同、`artifact-schema.md` §10/§11 以及 AGENTS 科学安全要求。本次为静态评审，未运行长测试。

| Severity | Type | Location | Issue | Evidence | Recommendation |
|---|---|---|---|---|---|
| High | Correctness / Terminal cells | `neural/src/research/sample_efficiency.py:346-351`, `neural/src/research/training_subsets.py:249-251`, `FR-SE-05:13,27,69` | M5 无法报告 M2 低统计 terminal cell。`_build()` 对每个 plan cell 都调用 `load_training_subset()` 取得 actual count；但 M2 reader 对非 `planned` subset 会抛 `ResearchStateError(status="training_subset_insufficient_statistics")`，不会返回 summary。因此只要 M4 ledger 中存在合法的 model-stage terminal cell，M5 report publisher/reader 会提前退出，无法满足“terminal cell 保留为记录”和“model-stage terminal 也有确定组数”。CLI 还会把该 `ResearchStateError` 当作 exit 0 返回，造成无报告但成功退出的风险。 | M5 FR 第13行要求失败/terminal cell 都保留为记录，第27行规定所有 cell 的实际训练组数以 M2 subset summary 为权威来源，第69行验收要求 terminal 不被删除。当前代码在检查 `outcome["cell_status"]` 前已经进入 `load_training_subset()`；而 `load_training_subset()` 明确对 `entry["status"] != "planned"` 抛 terminal state。 | 不要用 `load_training_subset()` 读取 terminal subset 的 summary。M5 应先调用 `read_training_subsets()` 获得完整 M2 plan/ledger，并按 `training_subset_id` 建立 summary lookup；只有需要模型训练语义的 planned/complete cell 才调用 `load_training_subset()`。CLI 对内部 M5 构建阶段出现的合法 M4 terminal 不应返回无报告的 exit 0。增加 M2 low-stat terminal batch 的 report 测试。 |
| High | Human output integrity | `neural/src/research/sample_efficiency.py:303-320`, `neural/src/research/sample_efficiency.py:470-473`, `FR-SE-05:55,61`, `artifact-schema.md:§11` | PNG 只验证 manifest receipt 存在，不验证其绑定的 plot spec。实现写 PNG 时把 `plot_spec_id` 放进 metadata `Description`，但 reader 只调用 `run.file("sample-efficiency-curves.png")`。攻击者可以替换 PNG 为任意图并重签 report manifest；reader 会接受，违反“PNG receipt 与 plot-spec digest”以及 human outputs 语义边界。 | `_plot()` 在第319—320行写入 `Description=summary["plot_spec"]["plot_spec_id"]`；`read_sample_efficiency_report()` 第473行只验证文件 receipt，未读取 PNG metadata，也未与 `plot_spec_id` 比较。FR 第55行规定 summary 保存 exact plot spec，PNG 是非权威缓存但 reader 需要认证 plot spec/CSV 并检查 PNG receipt；第61行要求 PNG receipt 与 plot-spec digest。 | Reader 至少应打开 PNG metadata 并验证 `Description == summary["plot_spec"]["plot_spec_id"]`，同时拒绝缺失/不匹配。更稳妥的是生成并保存一个权威 `plot-spec.json`，PNG 仅作为缓存，reader 验证 PNG metadata 和 receipt 均绑定该 spec。加入 self-resigned PNG replacement 负例。 |
| Medium | Exact artifact schema | `neural/src/research/sample_efficiency.py:461-473`, `FR-SE-05:12,57,61`, `artifact-schema.md:§11` | Report reader 没有拒绝未知额外文件。FR 固定 M5 输出六类文件加 `protocol.json`，reader 应验证 report manifest 文件集合精确；当前只逐字节比较已知五个文本/JSON/CSV/Markdown，并确认 PNG 文件可通过 receipt，manifest 中的额外文件会被忽略。 | `ResearchRun` 会把所有注册文件写入 manifest；`read_sample_efficiency_report()` 没有检查 `set(run.manifest["files"])`。FR 第12行固定输出文件，第57行要求 summary/contract exact schema，第61行要求任何语义漂移拒绝。未知文件会破坏固定 artifact surface，也可能携带误导性报告内容。 | 在 reader 中要求 manifest files 精确等于 `{protocol.json, report-contract.json, sample-efficiency-records.jsonl, sample-efficiency-summary.json, sample-efficiency-curves.csv, sample-efficiency-curves.png, sample-efficiency-report.md}`。增加 self-signed extra-file tamper 测试。 |
| Medium | Summary schema / evaluation uncertainty | `neural/src/research/sample_efficiency.py:192-202`, `neural/src/research/sample_efficiency.py:205-210`, `FR-SE-05:45,57` | Summary row 的 `evaluation_uncertainty` 没有携带 FR 要求的 requested/valid replicate counts 和 resample plan binding。实现只把 aggregate replicate 值传给 `_stats()`，得到 `used_count/mean/std/quantiles/status`；row 内没有 `requested_replicates`、`valid_replicates` 或 `resample_plan_id`。顶层 summary 有 `resample_plan_id`，但每个 curve row 无法独立说明该误差层的有效预算。 | FR 第45行要求 evaluation uncertainty 对每个 replicate 聚合后保留 requested/valid replicate 数与 resample plan ID；第57行要求嵌套对象固定键和按状态 nullability。代码第200—202行仅生成 `_stats(aggregate, requested=...)`，其返回键见第61—62行，没有 requested/valid/plan 字段。 | 为 curve-row `evaluation_uncertainty["validation_absolute_weight_auc"]` 增加 `requested_replicates, valid_replicates, resample_plan_id`，并把 exact nested schema 写入 validator/reader 重放。CSV 可继续只导出状态/std，但 JSON summary 应保留完整预算。 |
| Medium | Plot spec contract | `neural/src/research/sample_efficiency.py:213-220`, `FR-SE-05:55`, `artifact-schema.md:§11` | `plot_spec` 过于弱，未覆盖 FR 要求的 series、数据值、缺失值不连线和误差语义。当前 spec 只有两个 panel、curve row IDs、claim scope 和 ID；它不绑定每个 representation 的 series、x/y 值、空值处理、误差字段或 legend 语义。即使 summary byte-stable，plot spec 也不能作为 PNG/plot 语义认证。 | FR 第55行要求 plot spec 覆盖 panel/series/curve-row IDs/轴标签/误差语义，PNG 两个 panel、缺失不连线。代码第213—219行没有 series 列表，也没有 error/missing-value policy；`_plot()` 第309—314行直接 plot all rows，不在 spec 中记录这些 series。 | 扩展 `plot_spec`：为每个 panel 记录 series per representation、ordered curve_row_ids、x/y metric、missing policy、error layer fields/legend policy。`plot_spec_id` 应覆盖这些字段，reader 用它认证 PNG metadata。 |
| Medium | Markdown contract | `neural/src/research/sample_efficiency.py:289-300`, `FR-SE-05:55`, `sprint-m5-01:24,34` | Markdown 报告内容低于合同要求。固定模板只写 claim scope、planned records、每个 representation 的 Q* status、一句误差层说明和 contract ID；缺少完整性/失败表、terminal/failed cell摘要、分层误差状态细节、Q* row IDs，以及禁止夸大结论的可审计上下文。因为 reader 只重放同一个稀疏模板，无法证明 human report 满足 FR 的信息要求。 | FR 第55行要求 Markdown 只陈述合同、完整性、失败表、误差层状态与 Q* 状态；Sprint 第24行把 Markdown 作为 verified summary rows 生成物，第34行要求 Markdown 重签漂移拒绝。当前模板第290—299行没有失败表或 completeness counts。 | 扩展 Markdown 模板：写入 batch/report IDs、record/curve counts、complete/terminal counts、failure table by status/terminal_stage、每个 representation 的 Q* status 和 row IDs、每个 curve row 的误差层状态摘要，并保持 MC-only demo 声明。增加 Markdown 内容 golden 或字段覆盖测试。 |
| Medium | Test authenticity | `neural/tests/research/test_sample_efficiency_report.py:73-104`, `neural/tests/research/test_h4l_sample_efficiency_report_script.py:8-20`, `sprint-m5-01:29-35` | 当前 M5 tests 没有覆盖多项已确认的关键风险。唯一 E2E 使用全部 complete 的 synthetic batch，只篡改 Markdown；没有覆盖 M2 low-stat terminal、self-signed PNG、extra manifest file、summary/record/CSV self-signed semantic tamper、M4 input替换、use-time model/inference替换、0/1 effective bootstrap replicate、CLI production root/fresh-child 实执行。 | Sprint 第29—35行列出 terminal、receipt自洽指标/配对/聚合/CSV篡改、bootstrap golden、Q* 重复 actual count、不完整点、M4输入替换/use-time payload替换、PNG plot spec/Markdown 重签漂移和 CLI safety。当前测试文件第73—104行只验证 happy-path、reader重放和 Markdown tamper；script test 只检查 parser 和 help。 | 增加 targeted static-sized tests 覆盖上述负例，尤其先补低统计 terminal 和 PNG/extra-file tamper。CLI 至少要有一次真实 invocation 到临时 project/runs root 或可注入 runs root 的测试路径，证明参数优先级、fresh child 和拒绝科学覆盖。 |
| Low | Numerical robustness | `neural/src/research/sample_efficiency.py:402-407`, `FR-SE-05:29,49` | `relative_w68` 没有显式保护 baseline W68 为零或非正的情况。M4 W68 通常应为正，但当前代码直接 `w68 / baseline_w68`；若 synthetic 或边界 inference 产生 0 宽度，会触发 native `ZeroDivisionError` 或无穷值，而不是稳定的 binding/incomplete 状态。 | FR 第29行定义 `relative_w68=W68(R)/W68(engineered19)`，第49行禁止非有限值进入 artifact。代码第407行没有检查 denominator。 | 在计算 relative 前要求 `baseline_w68 > 0` 且 finite，否则以 `learning_curve_incomplete` 或 `training_subset_binding_mismatch` 拒绝，并增加边界测试。 |
